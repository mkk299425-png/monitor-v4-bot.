-- =============================================
-- MONETOR X (FULLY FIXED + MERGED TOGGLES + AUTO REFRESH ON DEATH)
-- Original Code Fully Preserved & Corrected
-- =============================================

local Players = game:GetService("Players")
local LP = Players.LocalPlayer
local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local RepStorage = game:GetService("ReplicatedStorage")
local LogService = game:GetService("LogService")
local MarketplaceService = game:GetService("MarketplaceService")

local Configs_HUB = {
    Cor_Hub = Color3.fromRGB(15, 15, 15),
    Cor_Options = Color3.fromRGB(30, 30, 30),
    Cor_Stroke = Color3.fromRGB(255, 255, 255),
    Cor_Text = Color3.fromRGB(255, 255, 255),
    Cor_DarkText = Color3.fromRGB(180, 180, 180),
    Corner_Radius = UDim.new(0, 15),
    Text_Font = Enum.Font.FredokaOne
}

-- =============================================
-- HELPER FUNCTIONS
-- =============================================
local function Create(instance, parent, props)
    local new = Instance.new(instance)
    if props then
        for prop, value in pairs(props) do
            new[prop] = value
        end
    end
    new.Parent = parent
    return new
end

local function SetProps(instance, props)
    if instance and props then
        for prop, value in pairs(props) do
            instance[prop] = value
        end
    end
    return instance
end

local function Corner(parent, props)
    local new = Create("UICorner", parent)
    new.CornerRadius = Configs_HUB.Corner_Radius
    if props then SetProps(new, props) end
    return new
end

local function Stroke(parent, props)
    local new = Create("UIStroke", parent)
    new.Color = Configs_HUB.Cor_Stroke
    new.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    new.Thickness = 2
    local gradient = Instance.new("UIGradient")
    gradient.Color = ColorSequence.new{
        ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 255, 255)),
        ColorSequenceKeypoint.new(0.25, Color3.fromRGB(180, 180, 180)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(15, 15, 15)),
        ColorSequenceKeypoint.new(0.75, Color3.fromRGB(90, 90, 90)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 255, 255))
    }
    gradient.Rotation = 0
    gradient.Parent = new
    coroutine.wrap(function()
        while new.Parent do
            gradient.Rotation = (gradient.Rotation + 2) % 360
            RunService.RenderStepped:Wait()
        end
    end)()
    if props then SetProps(new, props) end
    return new
end

local function CreateTween(instance, prop, value, time, tweenWait)
    local tween = TweenService:Create(instance,
    TweenInfo.new(time, Enum.EasingStyle.Linear),
    {[prop] = value})
    tween:Play()
    if tweenWait then tween.Completed:Wait() end
end

local function TextSetColor(instance)
    instance.MouseEnter:Connect(function()
        CreateTween(instance, "TextColor3", Color3.fromRGB(180, 180, 180), 0.4, false)
    end)
    instance.MouseLeave:Connect(function()
        CreateTween(instance, "TextColor3", Configs_HUB.Cor_Text, 0.4, false)
    end)
end

-- =============================================
-- SCREEN GUI SETUP
-- =============================================
local ScreenGui = Create("ScreenGui", CoreGui, { Name = "Monetor_Hub" })

local ClickSound = Instance.new("Sound", ScreenGui)
ClickSound.SoundId = "rbxassetid://88442833509532"
ClickSound.Volume = 0.5

local function PlaySound() 
    pcall(function() ClickSound:Play() end) 
end

task.spawn(function()
    task.wait(2)
    for _, button in pairs(ScreenGui:GetDescendants()) do
        if button:IsA("TextButton") or button:IsA("ImageButton") then
            button.Activated:Connect(PlaySound)
        end
    end
    ScreenGui.DescendantAdded:Connect(function(newButton)
        if newButton:IsA("TextButton") or newButton:IsA("ImageButton") then
            task.wait(0.1)
            newButton.Activated:Connect(PlaySound)
        end
    end)
end)

local ScreenFind = CoreGui:FindFirstChild(ScreenGui.Name)
if ScreenFind and ScreenFind ~= ScreenGui then ScreenFind:Destroy() end

function DestroyScript() 
    pcall(function() ScreenGui:Destroy() end) 
end

-- =============================================
-- NOTIFICATION SYSTEM
-- =============================================
local Menu_Notifi = Create("Frame", ScreenGui, {
    Size = UDim2.new(0, 300, 1, 0),
    Position = UDim2.new(1, 0, 0, 0),
    AnchorPoint = Vector2.new(1, 0),
    BackgroundTransparency = 1
})
Create("UIPadding", Menu_Notifi, { PaddingLeft = UDim.new(0, 25), PaddingTop = UDim.new(0, 25), PaddingBottom = UDim.new(0, 50) })
Create("UIListLayout", Menu_Notifi, { Padding = UDim.new(0, 15), VerticalAlignment = "Bottom" })

function MakeNotifi(Configs)
    local Title = Configs.Title or "Monetor X"
    local text = Configs.Text or "تم التنفيذ"
    local timewait = Configs.Time or 5
    
    local Frame1 = Create("Frame", Menu_Notifi, {
        Size = UDim2.new(2, 0, 0, 0),
        BackgroundTransparency = 1,
        AutomaticSize = "Y",
        Name = "Title"
    })
    local Frame2 = Create("Frame", Frame1, {
        Size = UDim2.new(0, Menu_Notifi.Size.X.Offset - 50, 0, 0),
        BackgroundColor3 = Configs_HUB.Cor_Hub,
        Position = UDim2.new(0, Menu_Notifi.Size.X.Offset, 0, 0),
        AutomaticSize = "Y"
    }) 
    Corner(Frame2)
    Stroke(Frame2)
    
    Create("TextLabel", Frame2, {
        Size = UDim2.new(1, 0, 0, 25),
        Font = Configs_HUB.Text_Font,
        BackgroundTransparency = 1,
        Text = Title,
        TextSize = 20,
        Position = UDim2.new(0, 20, 0, 5),
        TextXAlignment = "Left",
        TextColor3 = Configs_HUB.Cor_Text
    })
    
    local CloseNotifi = Create("TextButton", Frame2, {
        Text = "X",
        Font = Configs_HUB.Text_Font,
        TextSize = 20,
        BackgroundTransparency = 1,
        TextColor3 = Color3.fromRGB(200, 200, 200),
        Position = UDim2.new(1, -5, 0, 5),
        AnchorPoint = Vector2.new(1, 0),
        Size = UDim2.new(0, 25, 0, 25)
    })
    
    Create("TextLabel", Frame2, {
        Size = UDim2.new(1, -30, 0, 0),
        Position = UDim2.new(0, 20, 0, CloseNotifi.Size.Y.Offset + 10),
        TextSize = 15,
        TextColor3 = Configs_HUB.Cor_DarkText,
        TextXAlignment = "Left",
        TextYAlignment = "Top",
        AutomaticSize = "Y",
        Text = text,
        Font = Configs_HUB.Text_Font,
        BackgroundTransparency = 1,
        TextWrapped = true
    })
    
    local FrameSize = Create("Frame", Frame2, {
        Size = UDim2.new(1, 0, 0, 2),
        BackgroundColor3 = Configs_HUB.Cor_Stroke,
        Position = UDim2.new(0, 2, 0, 30),
        BorderSizePixel = 0
    }) 
    Corner(FrameSize) 
    Create("Frame", Frame2, { Size = UDim2.new(0, 0, 0, 5), Position = UDim2.new(0, 0, 1, 5), BackgroundTransparency = 1 })
    
    task.spawn(function() 
        CreateTween(FrameSize, "Size", UDim2.new(0, 0, 0, 2), timewait, true) 
    end)
    
    CloseNotifi.MouseButton1Click:Connect(function()
        CreateTween(Frame2, "Position", UDim2.new(0, -20, 0, 0), 0.1, true)
        CreateTween(Frame2, "Position", UDim2.new(0, Menu_Notifi.Size.X.Offset, 0, 0), 0.5, true)
        Frame1:Destroy()
    end)
    
    task.spawn(function()
        CreateTween(Frame2, "Position", UDim2.new(0, -20, 0, 0), 0.5, true)
        CreateTween(Frame2, "Position", UDim2.new(), 0.1, true)
        task.wait(timewait)
        if Frame2 then
            CreateTween(Frame2, "Position", UDim2.new(0, -20, 0, 0), 0.1, true)
            CreateTween(Frame2, "Position", UDim2.new(0, Menu_Notifi.Size.X.Offset, 0, 0), 0.5, true)
            Frame1:Destroy()
        end
    end)
end

-- =============================================
-- BYPASS SYSTEM (FULLY RESTORED)
-- =============================================
local BypassEnabled = false

local function SafeHookFunction(func, newfunc)
    if hookfunction then
        return hookfunction(func, newfunc)
    end
    return func
end

local function FindAllACERemotes()
    local remotes = {}
    local containers = {
        RepStorage,
        workspace,
        game:GetService("Lighting"),
        game:GetService("CoreGui"),
        game:GetService("StarterGui"),
        LP:FindFirstChild("PlayerGui"),
        LP:FindFirstChild("Backpack"),
        LP:FindFirstChild("PlayerScripts"),
        LP:FindFirstChild("Character") or workspace:FindFirstChild(LP.Name)
    }
    for _, service in ipairs(game:GetChildren()) do
        if service:IsA("Service") and not table.find(containers, service) then
            table.insert(containers, service)
        end
    end
    for _, container in ipairs(containers) do
        if not container then continue end
        local success, descendants = pcall(function() return container:GetDescendants() end)
        if not success then continue end
        for _, obj in ipairs(descendants) do
            if (obj:IsA("RemoteEvent") or obj:IsA("RemoteFunction")) then
                local name = obj.Name
                if string.find(name, "ACE") or string.find(name:lower(), "ace") then
                    table.insert(remotes, obj)
                end
            end
        end
    end
    return remotes
end

local function ApplyBypassToRemote(remote)
    if not remote then return end
    if remote:IsA("RemoteEvent") then
        local oldFire = remote.FireServer
        if oldFire then
            SafeHookFunction(oldFire, function(self, ...)
                local args = {...}
                local msg = tostring(args[1] or "")
                if msg == "HB" then
                    return oldFire(self, unpack(args))
                end
                return nil
            end)
        end
    elseif remote:IsA("RemoteFunction") then
        local oldInvoke = remote.InvokeServer
        if oldInvoke then
            SafeHookFunction(oldInvoke, function(self, ...)
                local args = {...}
                if tostring(args[1]) == "HB" then return "OK" end
                return true
            end)
        end
    end
end

local function DisableSprintMovementScans()
    local ps = LP:FindFirstChild("PlayerScripts")
    if not ps then return end
    local sm = ps:FindFirstChild("SprintMovement")
    if not sm then return end
    if not getgc then return end
    local GC = getgc(true)
    for _, obj in ipairs(GC) do
        if type(obj) == "function" then
            local info = debug.getinfo(obj)
            if info and info.what == "Lua" and info.source and string.find(info.source, "SprintMovement") then
                if info.name == "scanForMovementObjects" or info.name == "scanForGuiObjects" then
                    SafeHookFunction(obj, function() return false, nil end)
                end
            end
        end
    end
end

local function DisableLogs()
    if not getconnections then return end
    local conns = getconnections(LogService.MessageOut)
    if conns then
        for _, c in ipairs(conns) do
            pcall(c.Disconnect, c)
        end
    end
    pcall(function() 
        if hookfunction then
            hookfunction(LogService.MessageOut.Connect, function() return nil end)
        end
    end)
end

local function FullBypass()
    if BypassEnabled then
        MakeNotifi({Title = "Monetor X", Text = "الحماية مكسورة مسبقاً", Time = 2})
        return
    end
    local remotes = FindAllACERemotes()
    if #remotes > 0 then
        for _, r in ipairs(remotes) do
            ApplyBypassToRemote(r)
        end
    end
    DisableSprintMovementScans()
    DisableLogs()
    BypassEnabled = true
    MakeNotifi({ Title = "Monetor X", Text = "تم فك الحماية بنجاح", Time = 2 })
end

local function MonitorNewRemotes()
    local containers = {
        RepStorage,
        workspace,
        game:GetService("Lighting"),
        game:GetService("CoreGui"),
        game:GetService("StarterGui"),
        LP:FindFirstChild("PlayerGui"),
        LP:FindFirstChild("Backpack"),
        LP:FindFirstChild("PlayerScripts")
    }
    for _, container in ipairs(containers) do
        if not container then continue end
        pcall(function()
            container.ChildAdded:Connect(function(child)
                if child:IsA("RemoteEvent") or child:IsA("RemoteFunction") then
                    if string.find(child.Name, "ACE") or string.find(child.Name:lower(), "ace") then
                        task.wait(0.5)
                        ApplyBypassToRemote(child)
                    end
                end
            end)
        end)
    end
end

LP.CharacterAdded:Connect(function()
    task.wait(1)
    local char = LP.Character
    if char then
        for _, obj in ipairs(char:GetDescendants()) do
            if (obj:IsA("RemoteEvent") or obj:IsA("RemoteFunction")) and (string.find(obj.Name, "ACE") or string.find(obj.Name:lower(), "ace")) then
                ApplyBypassToRemote(obj)
            end
        end
    end
    DisableSprintMovementScans()
end)

local ps = LP:FindFirstChild("PlayerScripts")
if ps then
    ps.ChildAdded:Connect(function(child)
        if child:IsA("LocalScript") and child.Name == "SprintMovement" then
            task.wait(1)
            DisableSprintMovementScans()
        end
    end)
end

FullBypass()
MonitorNewRemotes()

-- =============================================
-- FLIGHT SYSTEM (FULLY RESTORED)
-- =============================================
local MainFlyButton = nil
local Flying = false
local FlySpeed = 50
local bodyVelocity = nil
local bodyGyro = nil
local flightConnection = nil
local currentHumanoid = nil
local currentRoot = nil
local FlyPanelCreated = false

local function UpdateMainFlyButton()
    if MainFlyButton then
        if Flying then
            MainFlyButton.Text = " هبوط الطيران"
        else
            MainFlyButton.Text = "️ تشغيل وضع طيران"
        end
    end
end

-- Fly Control Panel (new)
local FlyPanel = Create("Frame", ScreenGui, {
    Name = "FlyControlPanel",
    Size = UDim2.new(0, 200, 0, 120),
    Position = UDim2.new(0.5, -100, 0.2, 0),
    BackgroundColor3 = Configs_HUB.Cor_Hub,
    BackgroundTransparency = 0.2,
    Active = true,
    Draggable = true,
    Visible = false,
    BorderSizePixel = 0,
    ZIndex = 50
})
Corner(FlyPanel)
Stroke(FlyPanel)

local FlyPanelTitle = Create("TextLabel", FlyPanel, {
    Size = UDim2.new(1, 0, 0, 25),
    BackgroundTransparency = 1,
    Text = "التحكم بالطيران",
    TextColor3 = Configs_HUB.Cor_Text,
    Font = Configs_HUB.Text_Font,
    TextSize = 18,
    TextXAlignment = Enum.TextXAlignment.Center,
    ZIndex = 51
})

local FlySpeedLabel = Create("TextLabel", FlyPanel, {
    Size = UDim2.new(1, 0, 0, 20),
    Position = UDim2.new(0, 0, 0, 30),
    BackgroundTransparency = 1,
    Text = "السرعة: " .. FlySpeed,
    TextColor3 = Configs_HUB.Cor_Text,
    Font = Configs_HUB.Text_Font,
    TextSize = 14,
    TextXAlignment = Enum.TextXAlignment.Center,
    ZIndex = 51
})

local FlyIncButton = Create("TextButton", FlyPanel, {
    Size = UDim2.new(0.45, 0, 0, 30),
    Position = UDim2.new(0.05, 0, 0, 55),
    BackgroundColor3 = Configs_HUB.Cor_Options,
    Text = "+10",
    TextColor3 = Configs_HUB.Cor_Text,
    Font = Configs_HUB.Text_Font,
    TextSize = 16,
    AutoButtonColor = false,
    ZIndex = 51
})
Corner(FlyIncButton)
Stroke(FlyIncButton)

local FlyDecButton = Create("TextButton", FlyPanel, {
    Size = UDim2.new(0.45, 0, 0, 30),
    Position = UDim2.new(0.5, 0, 0, 55),
    BackgroundColor3 = Configs_HUB.Cor_Options,
    Text = "-10",
    TextColor3 = Configs_HUB.Cor_Text,
    Font = Configs_HUB.Text_Font,
    TextSize = 16,
    AutoButtonColor = false,
    ZIndex = 51
})
Corner(FlyDecButton)
Stroke(FlyDecButton)

local FlyStopButton = Create("TextButton", FlyPanel, {
    Size = UDim2.new(1, -10, 0, 30),
    Position = UDim2.new(0, 5, 0, 90),
    BackgroundColor3 = Configs_HUB.Cor_Options,
    Text = " هبوط",
    TextColor3 = Color3.fromRGB(255, 100, 100),
    Font = Configs_HUB.Text_Font,
    TextSize = 14,
    AutoButtonColor = false,
    Active = true,
    ZIndex = 51
})
Corner(FlyStopButton)
Stroke(FlyStopButton)

local function UpdateFlyPanel()
    FlySpeedLabel.Text = "السرعة: " .. FlySpeed
end

FlyIncButton.Activated:Connect(function()
    FlySpeed = FlySpeed + 10
    UpdateFlyPanel()
    MakeNotifi({Title="Fly", Text="سرعة الطيران: "..FlySpeed, Time=1})
end)

FlyDecButton.Activated:Connect(function()
    if FlySpeed > 10 then
        FlySpeed = FlySpeed - 10
    end
    UpdateFlyPanel()
    MakeNotifi({Title="Fly", Text="سرعة الطيران: "..FlySpeed, Time=1})
end)

local function stopFly()
    if not Flying then
        FlyPanel.Visible = FlyPanelCreated
        UpdateMainFlyButton()
        return
    end

    Flying = false

    if flightConnection then
        flightConnection:Disconnect()
        flightConnection = nil
    end

    if bodyVelocity then
        bodyVelocity.Velocity = Vector3.zero
        bodyVelocity:Destroy()
        bodyVelocity = nil
    end

    if bodyGyro then
        bodyGyro:Destroy()
        bodyGyro = nil
    end

    if currentHumanoid then
        currentHumanoid.PlatformStand = false
        currentHumanoid:ChangeState(Enum.HumanoidStateType.GettingUp)
        currentHumanoid = nil
    end

    currentRoot = nil
    FlyPanel.Visible = true
    FlyPanelCreated = true
    FlyStopButton.Text = "️ تشغيل الطيران"
    UpdateMainFlyButton()
    MakeNotifi({Title = "Fly", Text = "تم الهبوط — اضغط نفس الزر للطيران مرة ثانية", Time = 2})
end

local function startFly()
    if Flying then return end
    local char = LP.Character
    if not char then return end
    local root = char:FindFirstChild("HumanoidRootPart") or char:FindFirstChild("Torso")
    local humanoid = char:FindFirstChildOfClass("Humanoid")
    if not root or not humanoid then return end

    stopFly()
    currentRoot = root
    currentHumanoid = humanoid

    bodyGyro = Instance.new("BodyGyro", root)
    bodyGyro.MaxTorque = Vector3.new(9e9, 9e9, 9e9)
    bodyGyro.P = 9e4
    bodyGyro.CFrame = root.CFrame

    bodyVelocity = Instance.new("BodyVelocity", root)
    bodyVelocity.MaxForce = Vector3.new(9e9, 9e9, 9e9)
    bodyVelocity.Velocity = Vector3.zero

    humanoid.PlatformStand = true
    Flying = true
    FlyPanelCreated = true
    FlyPanel.Visible = true
    FlyStopButton.Text = " هبوط"
    UpdateMainFlyButton()
    UpdateFlyPanel()

    local camera = workspace.CurrentCamera

    flightConnection = RunService.Heartbeat:Connect(function()
        if not Flying or not currentRoot or not currentHumanoid or currentHumanoid.Health <= 0 then
            stopFly()
            return
        end

        bodyGyro.CFrame = camera.CFrame

        local moveDir = currentHumanoid.MoveDirection
        if moveDir.Magnitude > 0.05 then
            local camCFrame = camera.CFrame
            local forward = camCFrame.LookVector
            local right = camCFrame.RightVector
            
            local relativeMove = camCFrame:VectorToObjectSpace(moveDir)
            local finalVel = (forward * -relativeMove.Z) + (right * relativeMove.X)
            
            bodyVelocity.Velocity = finalVel * FlySpeed
        else
            bodyVelocity.Velocity = Vector3.zero
        end
    end)

    MakeNotifi({Title = "Fly", Text = "تم تشغيل الطيران", Time = 2})
end

FlyStopButton.Activated:Connect(function()
    if Flying then
        stopFly()
    else
        startFly()
    end
end)

LP.CharacterAdded:Connect(function()
    if Flying then stopFly() end
end)

-- =============================================
-- GUI SCANNER REMOVER
-- =============================================
local PlayerGui = LP:WaitForChild("PlayerGui")
local SuspiciousKeywords = {"warning","anticheat","detected","banned","admin","notification","toast","popup","exploit"}

PlayerGui.ChildAdded:Connect(function(child)
    if child:IsA("ScreenGui") then
        local name = child.Name:lower()
        for _, kw in ipairs(SuspiciousKeywords) do
            if string.find(name, kw) then
                task.wait(0.1)
                pcall(child.Destroy, child)
                break
            end
        end
    end
end)

task.wait(0.5)
for _, child in ipairs(PlayerGui:GetChildren()) do
    if child:IsA("ScreenGui") then
        local name = child.Name:lower()
        for _, kw in ipairs(SuspiciousKeywords) do
            if string.find(name, kw) then
                pcall(child.Destroy, child)
                break
            end
        end
    end
end

-- =============================================
-- MAIN UI & INTERFACE (FULLY RESTORED)
-- =============================================
local Menu = Create("Frame", ScreenGui, {
    Size = UDim2.new(0, 530, 0, 320),
    Position = UDim2.new(0.5, -265, 0.5, -160),
    BackgroundColor3 = Configs_HUB.Cor_Hub,
    BackgroundTransparency = 0.05,
    Active = true,
    Draggable = true,
    BorderSizePixel = 0,
    ClipsDescendants = true,
    ZIndex = 1
})
Corner(Menu)
Stroke(Menu)

-- Animated red background on a black base
local BackgroundFX = Create("Frame", Menu, {
    Name = "Monetor_BackgroundFX",
    Size = UDim2.new(1, 0, 1, 0),
    Position = UDim2.new(0, 0, 0, 0),
    BackgroundColor3 = Color3.fromRGB(5, 5, 5),
    BackgroundTransparency = 0,
    BorderSizePixel = 0,
    ClipsDescendants = true,
    ZIndex = 0
})
Corner(BackgroundFX)

local RedGlowData = {
    {Size = 170, X = -0.05, Y = 0.05, DX = 0.22, DY = 0.10, Duration = 13},
    {Size = 125, X = 0.20, Y = 0.58, DX = -0.18, DY = 0.13, Duration = 16},
    {Size = 150, X = 0.58, Y = -0.10, DX = 0.16, DY = 0.18, Duration = 15},
    {Size = 105, X = 0.78, Y = 0.48, DX = -0.20, DY = -0.12, Duration = 14},
    {Size = 135, X = 0.38, Y = 0.72, DX = 0.18, DY = -0.16, Duration = 17},
    {Size = 90, X = 0.05, Y = 0.30, DX = 0.15, DY = -0.10, Duration = 12}
}

for index, data in ipairs(RedGlowData) do
    local glow = Create("Frame", BackgroundFX, {
        Name = "RedGlow_" .. index,
        Size = UDim2.new(0, data.Size, 0, data.Size),
        Position = UDim2.new(data.X, 0, data.Y, 0),
        BackgroundColor3 = Color3.fromRGB(170, 0, 0),
        BackgroundTransparency = 0.82,
        BorderSizePixel = 0,
        ZIndex = 0
    })
    Corner(glow, {CornerRadius = UDim.new(1, 0)})

    task.spawn(function()
        local baseX, baseY = data.X, data.Y
        while glow.Parent do
            local targetX = math.clamp(baseX + data.DX, -0.12, 0.90)
            local targetY = math.clamp(baseY + data.DY, -0.12, 0.82)
            local tweenOut = TweenService:Create(
                glow,
                TweenInfo.new(data.Duration, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut),
                {Position = UDim2.new(targetX, 0, targetY, 0), BackgroundTransparency = 0.74}
            )
            tweenOut:Play()
            tweenOut.Completed:Wait()
            if not glow.Parent then break end

            local tweenBack = TweenService:Create(
                glow,
                TweenInfo.new(data.Duration, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut),
                {Position = UDim2.new(baseX, 0, baseY, 0), BackgroundTransparency = 0.84}
            )
            tweenBack:Play()
            tweenBack.Completed:Wait()
        end
    end)
end

local IntroSound = Instance.new("Sound")
IntroSound.SoundId = "rbxassetid://8426701399"
IntroSound.Volume = 2
IntroSound.Parent = ScreenGui
pcall(function() IntroSound:Play() end)

local Effect = Create("Frame", ScreenGui, {
    Size = UDim2.new(0, 10, 0, 10),
    Position = UDim2.new(0.5, 0, 0.5, 0),
    AnchorPoint = Vector2.new(0.5, 0.5),
    BackgroundColor3 = Configs_HUB.Cor_Stroke,
    BackgroundTransparency = 0,
    BorderSizePixel = 0,
    ClipsDescendants = true,
    ZIndex = 100
})
local EffectCorner = Instance.new("UICorner"); EffectCorner.CornerRadius = UDim.new(0, 18); EffectCorner.Parent = Effect
local EffectStroke = Instance.new("UIStroke"); EffectStroke.Color = Configs_HUB.Cor_Stroke; EffectStroke.Thickness = 2; EffectStroke.Transparency = 0; EffectStroke.Parent = Effect

local EffectGrow = TweenService:Create(Effect, TweenInfo.new(0.55, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
    Size = UDim2.new(0, 560, 0, 350),
    BackgroundTransparency = 0.75
})
EffectGrow:Play()

task.wait(0.12)
local MenuAppear = TweenService:Create(Menu, TweenInfo.new(0.7, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0.3})
MenuAppear:Play()

task.wait(0.35)
local EffectOut = TweenService:Create(Effect, TweenInfo.new(0.25, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
    Size = UDim2.new(0, 590, 0, 350),
    BackgroundTransparency = 1
})
EffectOut:Play()
local StrokeOut = TweenService:Create(EffectStroke, TweenInfo.new(0.25, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {Transparency = 1})
StrokeOut:Play()
EffectOut.Completed:Wait()
Effect:Destroy()
IntroSound.Ended:Connect(function() IntroSound:Destroy() end)

local TopBar = Create("Frame", Menu, {
    BackgroundTransparency = 1,
    Size = UDim2.new(1, 0, 0, 30),
    Position = UDim2.new(0, 0, 0, 0),
    Visible = false,
    BorderSizePixel = 0,
    ZIndex = 10
})
local ButtonsFrame = Create("Frame", TopBar, {
    Size = UDim2.new(0, 40, 1, -5),
    Position = UDim2.new(1, -10, 0, 2.5),
    AnchorPoint = Vector2.new(1, 0),
    BackgroundTransparency = 1,
    ZIndex = 15
})
Create("ImageLabel", TopBar, {
    Size = UDim2.new(0, 24, 0, 24),
    Position = UDim2.new(0, 8, 0.5, 0),
    AnchorPoint = Vector2.new(0, 0.5),
    BackgroundTransparency = 1,
    Image = "rbxassetid://89949755377207",
    ImageColor3 = Color3.fromRGB(255, 255, 255),
    ZIndex = 20
})
local NameFrame = Create("Frame", TopBar, {
    Size = UDim2.new(0, 0, 0, 25),
    AutomaticSize = Enum.AutomaticSize.X,
    Position = UDim2.new(0, 38, 0.5, 0),
    AnchorPoint = Vector2.new(0, 0.5),
    BackgroundColor3 = Configs_HUB.Cor_Options,
    BackgroundTransparency = 0.15,
    ZIndex = 11
})
Corner(NameFrame); Stroke(NameFrame)
local Title = Create("TextLabel", NameFrame, {
    Size = UDim2.new(0, 0, 1, 0),
    AutomaticSize = Enum.AutomaticSize.X,
    Position = UDim2.new(0, 10, 0, 0),
    BackgroundTransparency = 1,
    TextColor3 = Color3.fromRGB(255, 255, 255),
    Font = Configs_HUB.Text_Font,
    TextXAlignment = Enum.TextXAlignment.Left,
    TextYAlignment = Enum.TextYAlignment.Center,
    Text = "Monetor X",
    TextSize = 20,
    ZIndex = 12
})
local Padding = Instance.new("UIPadding"); Padding.PaddingLeft = UDim.new(0, 10); Padding.PaddingRight = UDim.new(0, 10); Padding.Parent = NameFrame
local TextGradient = Instance.new("UIGradient"); TextGradient.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 255, 255)),
    ColorSequenceKeypoint.new(0.3, Color3.fromRGB(180, 180, 180)),
    ColorSequenceKeypoint.new(0.6, Color3.fromRGB(90, 90, 90)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(15, 15, 15))
}
TextGradient.Parent = Title
task.spawn(function()
    local speed = 0.008; local offset = 0
    while Title and Title.Parent do
        offset = offset + speed
        if offset >= 1 then offset = 0 end
        TextGradient.Offset = Vector2.new(-offset, 0)
        RunService.RenderStepped:Wait()
    end
end)

local Minimize_BTN = Create("TextButton", ButtonsFrame, {
    Text = "-",
    TextColor3 = Configs_HUB.Cor_Text,
    Size = UDim2.new(0.5, 0, 1, 0),
    BackgroundTransparency = 1,
    Font = Configs_HUB.Text_Font,
    TextYAlignment = "Bottom",
    TextSize = 25
})
local IsMinimized = false
Minimize_BTN.MouseButton1Click:Connect(function()
    Minimize_BTN.Text = not IsMinimized and "+" or "-"
    if IsMinimized then
        IsMinimized = false
        CreateTween(Menu, "Size", UDim2.new(0, 530, 0, 320), 0.15, false)
    else
        IsMinimized = true
        CreateTween(Menu, "Size", UDim2.new(0, 530, 0, 25), 0.15, true)
    end
end)

local Close_Button = Create("TextButton", ButtonsFrame, {
    Text = "×",
    TextYAlignment = "Bottom",
    TextColor3 = Configs_HUB.Cor_Text,
    Size = UDim2.new(0.5, 0, 1, 0),
    AnchorPoint = Vector2.new(1, 0),
    Position = UDim2.new(1, 0, 0, 0),
    BackgroundTransparency = 1,
    Font = Configs_HUB.Text_Font,
    TextSize = 25
})
local function CreateClose()
    IsMinimized = false
    CreateTween(Menu, "Size", UDim2.new(0, 530, 0, 320), 0.3, false)
    local CloseGui = Create("TextButton", Menu, {
        BackgroundTransparency = 0.5,
        BackgroundColor3 = Configs_HUB.Cor_Hub,
        Size = UDim2.new(1, 0, 1, 0),
        AutoButtonColor = false,
        Text = "",
        Visible = false
    })
    Corner(CloseGui)
    local CloseMenu = Create("Frame", CloseGui, {
        Size = UDim2.new(),
        AnchorPoint = Vector2.new(0.5, 0.5),
        Position = UDim2.new(0.5, 0, 0.5, 0),
        Transparency = 1,
        BackgroundColor3 = Configs_HUB.Cor_Hub
    })
    Corner(CloseMenu); Stroke(CloseMenu)
    Create("TextLabel", CloseMenu, {
        Size = UDim2.new(0.8, 0, 0.25, 0),
        Text = "هل تريد اغلاق السكربت؟",
        Position = UDim2.new(0.1, 0, 0.2),
        TextColor3 = Configs_HUB.Cor_Text,
        Font = Configs_HUB.Text_Font,
        TextScaled = true,
        BackgroundTransparency = 1
    })
    local Confirm = Create("TextButton", CloseMenu, {
        Size = UDim2.new(0.35, 0, 0.3, 0),
        Position = UDim2.new(0.1, 0, 0.5, 0),
        BackgroundColor3 = Configs_HUB.Cor_Hub,
        Text = "نعم",
        Font = Configs_HUB.Text_Font,
        TextColor3 = Color3.fromRGB(240, 0, 0),
        TextSize = 20
    })
    Corner(Confirm); Stroke(Confirm)
    local Cancel = Create("TextButton", CloseMenu, {
        Size = UDim2.new(0.35, 0, 0.3, 0),
        Position = UDim2.new(0.9, 0, 0.5, 0),
        AnchorPoint = Vector2.new(1, 0),
        BackgroundColor3 = Configs_HUB.Cor_Hub,
        Text = "لا",
        Font = Configs_HUB.Text_Font,
        TextColor3 = Color3.fromRGB(0, 240, 0),
        TextSize = 20
    })
    Corner(Cancel); Stroke(Cancel)
    local function SetVisible()
        if Menu.Size.Y.Offset > 100 then CloseGui.Visible = true else CloseGui.Visible = false end
    end
    SetVisible()
    Menu:GetPropertyChangedSignal("Size"):Connect(SetVisible)
    CreateTween(CloseMenu, "Transparency", 0, 0.2, false)
    CreateTween(CloseMenu, "Size", UDim2.new(0.7, 0, 0.7, 0), 0.2, false)
    Cancel.MouseButton1Click:Connect(function()
        CreateTween(CloseMenu, "Transparency", 1, 0.3, false)
        CreateTween(CloseMenu, "Size", UDim2.new(), 0.2, true)
        CloseGui:Destroy()
    end)
    Confirm.MouseButton1Click:Connect(function()
        CloseGui:Destroy()
        CreateTween(Menu, "Size", UDim2.new(), 0.3, true)
        DestroyScript()
    end)
    CloseGui.MouseButton1Click:Connect(function()
        CreateTween(CloseMenu, "Transparency", 1, 0.3, false)
        CreateTween(CloseMenu, "Size", UDim2.new(), 0.2, true)
        CloseGui:Destroy()
    end)
end
Close_Button.MouseButton1Click:Connect(CreateClose)

local AnimMenu = Create("Frame", ScreenGui, {
    Position = UDim2.new(0.5, 0, 0.5, 0),
    AnchorPoint = Vector2.new(0.5, 0.5),
    Size = UDim2.new(0, 0, 0, 100),
    BackgroundColor3 = Configs_HUB.Cor_Hub
})
Corner(AnimMenu, {CornerRadius = UDim.new(0, 6)})
Stroke(AnimMenu)
local PlayerHead = Create("ImageLabel", AnimMenu, {
    BackgroundTransparency = 1,
    Size = UDim2.new(0, 55, 0, 55),
    Position = UDim2.new(0.5, -27.5, 0, 8),
    Image = "https://www.roblox.com/headshot-thumbnail/image?userId=" .. LP.UserId .. "&width=420&height=420&format=png"
})
Corner(PlayerHead, {CornerRadius = UDim.new(1, 0)})
Stroke(PlayerHead)
local Anim_Credits = Create("TextLabel", AnimMenu, {
    Text = "مرحبا بك في MONETOR X " .. LP.Name,
    BackgroundTransparency = 1,
    Size = UDim2.new(1, 0, 0, 25),
    Position = UDim2.new(0, 0, 0, 68),
    Visible = false,
    Font = Configs_HUB.Text_Font,
    TextTransparency = 1,
    TextColor3 = Configs_HUB.Cor_Text,
    TextXAlignment = Enum.TextXAlignment.Center,
    TextYAlignment = Enum.TextYAlignment.Center,
    TextSize = 15
})
CreateTween(AnimMenu, "Size", UDim2.new(0, 180, 0, 100), 0.5, true)
Anim_Credits.Visible = true
for i = 1, 0, -0.1 do task.wait() Anim_Credits.TextTransparency = i end
task.wait(1)
for i = 0, 1, 0.1 do task.wait() Anim_Credits.TextTransparency = i end
Anim_Credits:Destroy(); PlayerHead:Destroy(); AnimMenu:Destroy()

CreateTween(Menu, "Size", UDim2.new(0, 530, 0, 35), 0.5, true)
TopBar.Visible = true
CreateTween(Menu, "Size", UDim2.new(0, 530, 0, 320), 0.3, true)
Menu.Draggable = true

local ScrollBar = Create("ScrollingFrame", Menu, {
    Size = UDim2.new(0, 140, 1, -tonumber(TopBar.Size.Y.Offset + 2)),
    Position = UDim2.new(0, 0, 1, 0),
    AnchorPoint = Vector2.new(0, 1),
    CanvasSize = UDim2.new(),
    ScrollingDirection = "Y",
    AutomaticCanvasSize = "Y",
    BackgroundTransparency = 1,
    ScrollBarThickness = 2
})
Create("UIPadding", ScrollBar, { PaddingLeft = UDim.new(0, 10), PaddingRight = UDim.new(0, 10), PaddingTop = UDim.new(0, 10), PaddingBottom = UDim.new(0, 10) })
Create("UIListLayout", ScrollBar, { Padding = UDim.new(0, 14) })

local Containers = Create("Frame", Menu, {
    Size = UDim2.new(1, -tonumber(ScrollBar.Size.X.Offset + 2), 1, -tonumber(TopBar.Size.Y.Offset + 2)),
    AnchorPoint = Vector2.new(1, 1),
    Position = UDim2.new(1, 0, 1, 0),
    BackgroundTransparency = 1
})
Corner(Containers); Stroke(Containers)

local firstVisible = true; local FirstTab = true; local textsize = 15; local textcolor = Configs_HUB.Cor_Text
Menu:GetPropertyChangedSignal("Size"):Connect(function()
    if Menu.Size.Y.Offset > 70 then ScrollBar.Visible = true; Containers.Visible = true else ScrollBar.Visible = false; Containers.Visible = false end
end)

function MakeTab(Configs)
    local TabName = Configs.Name or "Tab"
    local TabTitle = Configs.TabTitle or false
    local TabImage = Configs.Image or nil
    
    local Frame = Create("Frame", ScrollBar, { Size = UDim2.new(1, 0, 0, 30), BackgroundTransparency = 1 }) Corner(Frame) Stroke(Frame)
    local TextButton = Create("TextButton", Frame, { Size = UDim2.new(1, 0, 1, 0), BackgroundTransparency = 1, Text = "" })
    if TabImage then Create("ImageLabel", Frame, { Size = UDim2.new(0, 20, 0, 20), Position = UDim2.new(0, 5, 0.5, -10), BackgroundTransparency = 1, Image = TabImage, Name = "TabIcon" }) end
    local textOffset = TabImage and 30 or 5
    local TextLabel = Create("TextLabel", Frame, {
        Size = UDim2.new(1, -textOffset, 1, 0),
        Position = UDim2.new(0, textOffset, 0, 0),
        BackgroundTransparency = 1,
        Font = Configs_HUB.Text_Font,
        TextColor3 = textcolor,
        TextSize = textsize,
        Text = TabName,
        TextXAlignment = Enum.TextXAlignment.Left
    })
    local Container = Create("ScrollingFrame", Containers, {
        Size = UDim2.new(1, 0, 1, 0),
        ScrollingDirection = "Y",
        AutomaticCanvasSize = "Y",
        CanvasSize = UDim2.new(),
        BackgroundTransparency = 1,
        ScrollBarThickness = 2,
        Visible = firstVisible
    })
    Corner(Container); Stroke(Container)
    Create("UIPadding", Container, { PaddingLeft = UDim.new(0, 10), PaddingRight = UDim.new(0, 10), PaddingTop = UDim.new(0, 10), PaddingBottom = UDim.new(0, 10) })
    Create("UIListLayout", Container, { Padding = UDim.new(0, 5) })
    if TabTitle then
        Create("TextLabel", Container, {
            BackgroundTransparency = 1,
            Text = "#" .. string.gsub(TabName, " ", "-"),
            TextSize = 25,
            Font = Configs_HUB.Text_Font,
            TextXAlignment = "Left",
            TextColor3 = Configs_HUB.Cor_Text,
            Size = UDim2.new(1, 0, 0, 30),
            Position = UDim2.new(0, 30, 0, 0),
            Name = "Frame"
        })
    end
    TextButton.MouseButton1Click:Connect(function()
        for _, container in pairs(Containers:GetChildren()) do if container:IsA("ScrollingFrame") then container.Visible = false end end
        for _, frame in pairs(ScrollBar:GetChildren()) do
            if frame:IsA("Frame") and frame:FindFirstChild("TextLabel") and frame.TextLabel ~= TextLabel then
                CreateTween(frame.TextLabel, "TextColor3", Configs_HUB.Cor_DarkText, 0.3, false)
                frame.TextLabel.TextSize = 14
            end
        end
        Container.Visible = true
        CreateTween(TextLabel, "TextColor3", Configs_HUB.Cor_Text, 0.3, false)
        TextLabel.TextSize = 15
    end)
    firstVisible = false; FirstTab = false; textsize = 14; textcolor = Configs_HUB.Cor_DarkText
    return Container
end

function AddButton(parent, Configs)
    local ButtonName = Configs.Name or "Button!!"
    local Callback = Configs.Callback or function() end
    
    local TextButton = Create("TextButton", parent, {
        Size = UDim2.new(1, 0, 0, 25),
        BackgroundColor3 = Configs_HUB.Cor_Options,
        BackgroundTransparency = 0.5,
        Name = "Frame",
        Text = "",
        AutoButtonColor = false
    })
    Corner(TextButton); Stroke(TextButton)
    local TextLabel = Create("TextLabel", TextButton, {
        TextSize = 14,
        TextColor3 = Configs_HUB.Cor_Text,
        Text = ButtonName,
        Size = UDim2.new(1, 0, 1, 0),
        Position = UDim2.new(0, 35, 0, 0),
        BackgroundTransparency = 1,
        TextXAlignment = "Left",
        Font = Configs_HUB.Text_Font
    })
    local ImageLabel = Create("ImageLabel", TextButton, {
        Image = "rbxassetid://99956220697276",
        Size = UDim2.new(0, 20, 0, 20),
        Position = UDim2.new(0, 5, 0, 2.5),
        BackgroundTransparency = 1,
        ImageColor3 = Configs_HUB.Cor_Stroke
    })
    TextButton.MouseButton1Click:Connect(function()
        pcall(Callback)
        CreateTween(ImageLabel, "ImageColor3", Color3.fromRGB(30, 140, 200), 0.2, true)
        CreateTween(ImageLabel, "ImageColor3", Configs_HUB.Cor_Stroke, 0.2, false)
    end)
    TextSetColor(TextLabel)
    return TextButton
end

-- دالة جديدة: زر Toggle (تشغيل/إيقاف في نفس الزر)
function AddToggleButton(parent, Configs)
    local ButtonName = Configs.Name or "Toggle!!"
    local OnText = Configs.OnText or (ButtonName .. ": تشغيل")
    local OffText = Configs.OffText or (ButtonName .. ": إيقاف")
    local StartState = Configs.StartState or false
    local OnEnable = Configs.OnEnable or function() end
    local OnDisable = Configs.OnDisable or function() end
    
    local State = StartState
    
    local TextButton = Create("TextButton", parent, {
        Size = UDim2.new(1, 0, 0, 25),
        BackgroundColor3 = Configs_HUB.Cor_Options,
        BackgroundTransparency = 0.5,
        Name = "Frame",
        Text = "",
        AutoButtonColor = false
    })
    Corner(TextButton); Stroke(TextButton)
    local TextLabel = Create("TextLabel", TextButton, {
        TextSize = 14,
        TextColor3 = Configs_HUB.Cor_Text,
        Text = State and OffText or OnText,
        Size = UDim2.new(1, 0, 1, 0),
        Position = UDim2.new(0, 35, 0, 0),
        BackgroundTransparency = 1,
        TextXAlignment = "Left",
        Font = Configs_HUB.Text_Font
    })
    local ImageLabel = Create("ImageLabel", TextButton, {
        Image = "rbxassetid://99956220697276",
        Size = UDim2.new(0, 20, 0, 20),
        Position = UDim2.new(0, 5, 0, 2.5),
        BackgroundTransparency = 1,
        ImageColor3 = State and Color3.fromRGB(80, 220, 120) or Configs_HUB.Cor_Stroke
    })
    TextButton.MouseButton1Click:Connect(function()
        State = not State
        TextLabel.Text = State and OffText or OnText
        CreateTween(ImageLabel, "ImageColor3", Color3.fromRGB(30, 140, 200), 0.2, true)
        CreateTween(ImageLabel, "ImageColor3", State and Color3.fromRGB(80, 220, 120) or Configs_HUB.Cor_Stroke, 0.2, false)
        if State then
            pcall(OnEnable)
        else
            pcall(OnDisable)
        end
    end)
    TextSetColor(TextLabel)
    return {
        Button = TextButton,
        TextLabel = TextLabel,
        GetState = function() return State end,
        SetState = function(newState)
            State = newState
            TextLabel.Text = State and OffText or OnText
            ImageLabel.ImageColor3 = State and Color3.fromRGB(80, 220, 120) or Configs_HUB.Cor_Stroke
        end
    }
end

function AddTextLabel(parent, text)
    local Label = Create("TextLabel", parent, {
        Size = UDim2.new(1, 0, 0, 100),
        BackgroundTransparency = 1,
        Text = text or "Loading...",
        TextColor3 = Configs_HUB.Cor_Text,
        TextSize = 13,
        Font = Configs_HUB.Text_Font,
        TextXAlignment = Enum.TextXAlignment.Left,
        TextYAlignment = Enum.TextYAlignment.Top,
        TextWrapped = true,
        TextScaled = false,
        AutomaticSize = Enum.AutomaticSize.Y
    })
    return { Update = function(self, newText) Label.Text = newText end, Label = Label }
end

function AddPlayerProfile(parent)
    local placeName = "Unknown Place"
    pcall(function() placeName = MarketplaceService:GetProductInfo(game.PlaceId).Name end)
    local exploit = "Unknown Exploit"
    pcall(function() 
        if identifyexecutor then exploit = identifyexecutor() 
        elseif getexecutorname then exploit = getexecutorname() 
        end 
    end)
    local fps = 0; local frameCount = 0; local lastTime = tick()
    RunService.Heartbeat:Connect(function()
        frameCount = frameCount + 1
        if tick() - lastTime >= 1 then fps = frameCount; frameCount = 0; lastTime = tick() end
    end)
    local function GetInfo()
        return "User: " .. LP.Name .. "\n" ..
               "Map Name: " .. placeName .. "\n" ..
               "Account Age: " .. LP.AccountAge .. " days\n" ..
               "Script: Monetor X\n" ..
               "Exploit: " .. tostring(exploit) .. "\n" ..
               "Ping: " .. math.floor(LP:GetNetworkPing() * 1000) .. " ms\n" ..
               "FPS: " .. fps .. "\n" ..
               "Time: " .. os.date("%H:%M:%S")
    end
    local Label = AddTextLabel(parent, GetInfo())
    task.spawn(function()
        while task.wait(1) do
            if Label and Label.Update then
                pcall(function() Label.Update(GetInfo()) end)
            end
        end
    end)
    return Label
end

-- =============================================
-- UPDATED AddDropdown WITH SEARCH FEATURE
-- =============================================
function AddDropdown(parent, Configs)
    local DropdownName = Configs.Name or "Dropdown!!"
    local Options = Configs.Options or {"1", "2", "3"}
    local Default = Configs.Default or Options[1]
    local Callback = Configs.Callback or function() end
    local Searchable = Configs.Searchable or false
    
    local MainFrame = Create("Frame", parent, {
        Size = UDim2.new(1, 0, 0, 25),
        BackgroundTransparency = 1,
        Name = "DropdownContainer"
    })
    
    local TextButton = Create("TextButton", MainFrame, {
        Size = UDim2.new(1, 0, 0, 25),
        BackgroundColor3 = Configs_HUB.Cor_Options,
        Name = "Header",
        Text = "",
        AutoButtonColor = false
    }) 
    
    local dropdownStroke = Stroke(TextButton, { Color = Configs_HUB.Cor_Stroke, Thickness = 2 })
    local gradient = Instance.new("UIGradient")
    gradient.Color = ColorSequence.new{
        ColorSequenceKeypoint.new(0, Configs_HUB.Cor_Stroke),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0, 100, 255)),
        ColorSequenceKeypoint.new(1, Configs_HUB.Cor_Stroke)
    }
    gradient.Rotation = 0
    gradient.Parent = dropdownStroke
    coroutine.wrap(function()
        while dropdownStroke.Parent do
            gradient.Rotation = (gradient.Rotation + 1) % 360
            RunService.RenderStepped:Wait()
        end
    end)()
    Corner(TextButton)
    
    local TextLabel = Create("TextLabel", TextButton, {
        TextSize = 12,
        TextColor3 = Configs_HUB.Cor_Text,
        Text = DropdownName,
        Size = UDim2.new(1, -30, 1, 0),
        Position = UDim2.new(0, 10, 0, 0),
        BackgroundTransparency = 1,
        TextXAlignment = "Left",
        Font = Configs_HUB.Text_Font
    })
    TextSetColor(TextLabel)
    
    local Arrow = Create("ImageLabel", TextButton, {
        Image = "rbxassetid://6031090998",
        Size = UDim2.new(0, 20, 0, 20),
        Position = UDim2.new(1, -25, 0.5, 0),
        AnchorPoint = Vector2.new(0, 0.5),
        BackgroundTransparency = 1,
        ImageColor3 = Configs_HUB.Cor_Text
    })
    
    local DropdownFrame = Create("ScrollingFrame", MainFrame, {
        Size = UDim2.new(1, 0, 0, 0),
        Position = UDim2.new(0, 0, 0, 28),
        BackgroundColor3 = Configs_HUB.Cor_Options,
        BackgroundTransparency = 0.1,
        ClipsDescendants = true,
        Visible = false,
        ScrollBarThickness = 3,
        ScrollBarImageColor3 = Configs_HUB.Cor_Stroke,
        CanvasSize = UDim2.new(0, 0, 0, 0),
        AutomaticCanvasSize = Enum.AutomaticSize.Y,
        ZIndex = 20
    })
    Corner(DropdownFrame)
    Stroke(DropdownFrame)
    
    local DropdownList = Create("UIListLayout", DropdownFrame, {
        Padding = UDim.new(0, 2),
        SortOrder = Enum.SortOrder.LayoutOrder
    })
    
    Create("UIPadding", DropdownFrame, {
        PaddingLeft = UDim.new(0, 4),
        PaddingRight = UDim.new(0, 4),
        PaddingTop = UDim.new(0, 4),
        PaddingBottom = UDim.new(0, 4)
    })
    
    local SearchBox
    if Searchable then
        SearchBox = Create("TextBox", DropdownFrame, {
            Size = UDim2.new(1, 0, 0, 25),
            BackgroundColor3 = Configs_HUB.Cor_Hub,
            TextColor3 = Configs_HUB.Cor_Text,
            PlaceholderText = "ابحث...",
            Font = Configs_HUB.Text_Font,
            TextSize = 12,
            TextXAlignment = Enum.TextXAlignment.Left,
            ClearTextOnFocus = false,
            ZIndex = 25
        })
        Corner(SearchBox)
        Stroke(SearchBox)
        SearchBox.LayoutOrder = -1
    end
    
    local Selected = Default
    local expanded = false
    local currentSearchText = ""
    
    local function GetTargetHeight()
        local count = #Options
        local totalH = count * 22 + 8
        if Searchable then
            totalH = totalH + 30
        end
        return math.min(totalH, 150)
    end
    
    local function UpdateButtons()
        for _, child in ipairs(DropdownFrame:GetChildren()) do
            if child:IsA("TextButton") then
                child:Destroy()
            end
        end
        local filteredOptions = {}
        if currentSearchText ~= "" then
            for _, option in ipairs(Options) do
                if string.find(string.lower(option), string.lower(currentSearchText), 1, true) then
                    table.insert(filteredOptions, option)
                end
            end
        else
            filteredOptions = Options
        end
        
        for _, option in ipairs(filteredOptions) do
            local btn = Create("TextButton", DropdownFrame, {
                Size = UDim2.new(1, 0, 0, 20),
                BackgroundColor3 = Configs_HUB.Cor_Hub,
                Text = option,
                TextColor3 = Configs_HUB.Cor_Text,
                TextSize = 12,
                Font = Configs_HUB.Text_Font,
                AutoButtonColor = false,
                ZIndex = 21
            })
            Corner(btn)
            btn.MouseButton1Click:Connect(function()
                Selected = option
                TextLabel.Text = DropdownName .. ": " .. option
                Callback(option)
                expanded = false
                currentSearchText = ""
                if SearchBox then SearchBox.Text = "" end
                CreateTween(DropdownFrame, "Size", UDim2.new(1, 0, 0, 0), 0.2, true)
                DropdownFrame.Visible = false
                CreateTween(MainFrame, "Size", UDim2.new(1, 0, 0, 25), 0.2, false)
            end)
        end
        
        if expanded then
            local targetH = GetTargetHeight()
            DropdownFrame.Size = UDim2.new(1, 0, 0, targetH)
            MainFrame.Size = UDim2.new(1, 0, 0, 28 + targetH)
        end
    end
    
    UpdateButtons()
    
    if SearchBox then
        SearchBox:GetPropertyChangedSignal("Text"):Connect(function()
            currentSearchText = SearchBox.Text
            UpdateButtons()
        end)
    end
    
    TextButton.MouseButton1Click:Connect(function()
        expanded = not expanded
        if expanded then
            DropdownFrame.Visible = true
            local targetH = GetTargetHeight()
            CreateTween(DropdownFrame, "Size", UDim2.new(1, 0, 0, targetH), 0.2, false)
            CreateTween(MainFrame, "Size", UDim2.new(1, 0, 0, 28 + targetH), 0.2, false)
        else
            CreateTween(DropdownFrame, "Size", UDim2.new(1, 0, 0, 0), 0.2, true)
            DropdownFrame.Visible = false
            CreateTween(MainFrame, "Size", UDim2.new(1, 0, 0, 25), 0.2, false)
        end
    end)
    
    return {
        Update = function(self, newOptions)
            Options = newOptions
            UpdateButtons()
        end,
        GetSelected = function(self)
            return Selected
        end
    }
end

-- =============================================
-- TABS CREATION (FULLY RESTORED)
-- =============================================
local MainTab = MakeTab({Name = "الرئيسية", TabTitle = true})
local LocalPlayerTab = MakeTab({Name = "شخصيتي ‍️", TabTitle = true})
local ESPTab = MakeTab({Name = "رادار ️", TabTitle = true})
local TeleportTab = MakeTab({Name = "الانتقال ", TabTitle = true})
local TrollTab = MakeTab({Name = "Troll", TabTitle = true, Image = "rbxassetid://6031075931"})
local RightsTab = MakeTab({Name = "حقوق", TabTitle = true, Image = "rbxassetid://6031075931"})


local Monetor_CommandPanel = nil
local Monetor_CommandInput = nil
local Monetor_CommandStatus = nil
local Monetor_ChatEnabled = false
local Monetor_CommandBusy = false

local function Monetor_SetCommandStatus(message, isError)
    if Monetor_CommandStatus then
        Monetor_CommandStatus.Text = message or ""
        Monetor_CommandStatus.TextColor3 = isError
            and Color3.fromRGB(255, 120, 120)
            or Color3.fromRGB(180, 255, 180)
    end
end

-- يحاول استخدام نظام الأوامر الموجود في اللعبة.
-- لا يرسل أي شيء إلى TextChatService/Roblox Chat.
local function Monetor_ExecuteTitleCommand(text)
    -- المسار الرئيسي المؤكد من لعبتك: HDAdminHDClient → Signals → RequestCommand
    local commandText = ";titler me " .. text
    local hdClient = RepStorage:WaitForChild("HDAdminHDClient", 5)
    if not hdClient then return false, "HDAdminHDClient غير موجود" end

    local signals = hdClient:WaitForChild("Signals", 5)
    if not signals then return false, "HDAdmin Signals غير موجود" end

    local requestCommand = signals:WaitForChild("RequestCommand", 5)
    if not requestCommand or not requestCommand:IsA("RemoteFunction") then
        return false, "HDAdmin RequestCommand غير متاح"
    end

    local ok, err = pcall(function()
        requestCommand:InvokeServer(commandText)
    end)
    if ok then
        return true, "HDAdminHDClient.Signals.RequestCommand"
    end
    return false, "فشل تنفيذ HDAdmin: " .. tostring(err)
end

local function Monetor_CreateCommandPanel()
    if Monetor_CommandPanel and Monetor_CommandPanel.Parent then
        return Monetor_CommandPanel
    end

    Monetor_CommandPanel = Create("Frame", ScreenGui, {
        Name = "Monetor_TitleCommandPanel",
        Size = UDim2.new(0, 245, 0, 142),
        Position = UDim2.new(0, 12, 0.5, 0),
        AnchorPoint = Vector2.new(0, 0.5),
        BackgroundColor3 = Configs_HUB.Cor_Hub,
        BackgroundTransparency = 0.08,
        BorderSizePixel = 0,
        Active = true,
        Draggable = true,
        Visible = false,
        ZIndex = 80
    })
    Corner(Monetor_CommandPanel)
    Stroke(Monetor_CommandPanel)

    Create("TextLabel", Monetor_CommandPanel, {
        Size = UDim2.new(1, -44, 0, 28),
        Position = UDim2.new(0, 12, 0, 7),
        BackgroundTransparency = 1,
        Text = "تنفيذ الأوامر",
        TextColor3 = Configs_HUB.Cor_Text,
        Font = Configs_HUB.Text_Font,
        TextSize = 18,
        TextXAlignment = Enum.TextXAlignment.Left,
        ZIndex = 81
    })

    local close = Create("TextButton", Monetor_CommandPanel, {
        Size = UDim2.new(0, 30, 0, 28),
        Position = UDim2.new(1, -8, 0, 7),
        AnchorPoint = Vector2.new(1, 0),
        BackgroundTransparency = 1,
        Text = "×",
        TextColor3 = Configs_HUB.Cor_Text,
        Font = Configs_HUB.Text_Font,
        TextSize = 24,
        AutoButtonColor = false,
        ZIndex = 82
    })

    close.Activated:Connect(function()
        Monetor_CommandPanel.Visible = false
        Monetor_ChatEnabled = false
        Monetor_SetCommandStatus("متوقف", false)
    end)

    Create("TextLabel", Monetor_CommandPanel, {
        Size = UDim2.new(1, -24, 0, 18),
        Position = UDim2.new(0, 12, 0, 35),
        BackgroundTransparency = 1,
        Text = "لو شات Roblox مقفول، هذه اللوحة تعمل بشكل مستقل",
        TextColor3 = Configs_HUB.Cor_DarkText,
        Font = Configs_HUB.Text_Font,
        TextSize = 11,
        TextXAlignment = Enum.TextXAlignment.Left,
        ZIndex = 81
    })

    Monetor_CommandInput = Create("TextBox", Monetor_CommandPanel, {
        Size = UDim2.new(1, -86, 0, 38),
        Position = UDim2.new(0, 12, 0, 58),
        BackgroundColor3 = Configs_HUB.Cor_Options,
        BackgroundTransparency = 0.1,
        BorderSizePixel = 0,
        Text = "",
        PlaceholderText = "اكتب النص هنا...",
        PlaceholderColor3 = Color3.fromRGB(150, 150, 150),
        TextColor3 = Configs_HUB.Cor_Text,
        Font = Configs_HUB.Text_Font,
        TextSize = 14,
        TextXAlignment = Enum.TextXAlignment.Right,
        ClearTextOnFocus = false,
        MultiLine = false,
        ZIndex = 82
    })
    Corner(Monetor_CommandInput)
    Stroke(Monetor_CommandInput)

    local execute = Create("TextButton", Monetor_CommandPanel, {
        Size = UDim2.new(0, 62, 0, 38),
        Position = UDim2.new(1, -12, 0, 58),
        AnchorPoint = Vector2.new(1, 0),
        BackgroundColor3 = Configs_HUB.Cor_Options,
        BackgroundTransparency = 0.05,
        BorderSizePixel = 0,
        Text = "تنفيذ",
        TextColor3 = Configs_HUB.Cor_Text,
        Font = Configs_HUB.Text_Font,
        TextSize = 13,
        AutoButtonColor = false,
        ZIndex = 82
    })
    Corner(execute)
    Stroke(execute)

    Monetor_CommandStatus = Create("TextLabel", Monetor_CommandPanel, {
        Size = UDim2.new(1, -24, 0, 24),
        Position = UDim2.new(0, 12, 1, -28),
        BackgroundTransparency = 1,
        Text = "جاهز",
        TextColor3 = Color3.fromRGB(180, 255, 180),
        Font = Configs_HUB.Text_Font,
        TextSize = 11,
        TextXAlignment = Enum.TextXAlignment.Left,
        ZIndex = 81
    })

    local function SendTitleText()
        if Monetor_CommandBusy then return end

        local value = tostring(Monetor_CommandInput.Text or "")
        value = value:gsub("[%c]", "")
        value = value:match("^%s*(.-)%s*$") or ""

        if value == "" then
            Monetor_SetCommandStatus("اكتب نصًا أولاً", true)
            return
        end

        if #value > 80 then
            value = value:sub(1, 80)
            Monetor_CommandInput.Text = value
        end

        Monetor_CommandBusy = true
        Monetor_SetCommandStatus("جاري الارسال...", false)

        local success, route = Monetor_ExecuteTitleCommand(value)

        if success then
            Monetor_CommandInput.Text = ""
            Monetor_SetCommandStatus("تم الارسال بنجاح", false)
        else
            Monetor_SetCommandStatus(route or "تعذر الوصول إلى HDAdmin", true)
        end

        task.delay(0.35, function()
            Monetor_CommandBusy = false
        end)
    end

    execute.Activated:Connect(SendTitleText)

    Monetor_CommandInput.FocusLost:Connect(function(enterPressed)
        if enterPressed then
            SendTitleText()
        end
    end)

    return Monetor_CommandPanel
end

local function Monetor_ToggleCommandPanel(forceState)
    Monetor_CreateCommandPanel()

    if forceState == nil then
        Monetor_CommandPanel.Visible = not Monetor_CommandPanel.Visible
    else
        Monetor_CommandPanel.Visible = forceState
    end

    Monetor_ChatEnabled = Monetor_CommandPanel.Visible
    Monetor_SetCommandStatus(
        Monetor_CommandPanel.Visible and "جاهز" or "متوقف",
        false
    )
end

AddButton(MainTab, {
    Name = "️ تنفيذ الأوامر",
    Callback = function()
        Monetor_ToggleCommandPanel()
    end
})

AddButton(LocalPlayerTab, {
    Name = " الشات — تشغيل / إيقاف",
    Callback = function()
        Monetor_ToggleCommandPanel()
        MakeNotifi({
            Title = "الشات الخاص",
            Text = Monetor_ChatEnabled
                and "تم تشغيل لوحة الإدخال — بدون Roblox Chat"
                or "تم إيقاف لوحة الإدخال",
            Time = 2
        })
    end
})

-- =============================================
-- MAIN TAB (FULLY RESTORED) + NEW FEATURES
-- =============================================
AddButton(MainTab, {
    Name = " فك حماية الطيران",
    Callback = function()
        FullBypass()
    end
})

MainFlyButton = AddButton(MainTab, {
    Name = "️ تشغيل وضع طيران",
    Callback = function()
        if Flying then
            stopFly()
        else
            startFly()
        end
    end
})

AddButton(MainTab, {
    Name = "️ إظهار لوحة الطيران",
    Callback = function()
        FlyPanelCreated = true
        FlyPanel.Visible = true
        UpdateFlyPanel()
        MakeNotifi({Title="Fly", Text="تم إظهار لوحة التحكم بالطيران", Time=2})
    end
})

AddButton(MainTab, {
    Name = " زيادة سرعة الطيران (+50)",
    Callback = function()
        FlySpeed = FlySpeed + 50
        UpdateFlyPanel()
        MakeNotifi({Title="Fly", Text="سرعة الطيران الان: "..FlySpeed, Time=2})
    end
})

AddButton(MainTab, {
    Name = " تقليل سرعة الطيران (-50)",
    Callback = function()
        if FlySpeed > 50 then
            FlySpeed = FlySpeed - 50
        end
        UpdateFlyPanel()
        MakeNotifi({Title="Fly", Text="سرعة الطيران الان: "..FlySpeed, Time=2})
    end
})

AddButton(MainTab, {
    Name = " فارم فلوس",
    Callback = function()
        pcall(function()
            loadstring(game:HttpGet("https://api.rubis.app/v2/scrap/LSDSltPsDZyOKL7q/raw"))()
        end)
    end
})

local RespawnKillDropdown = AddDropdown(MainTab, {
    Name = "اختر الأمر",
    Options = {"رفريش (Respawn)", "موت سريع (Kill)"},
    Default = "رفريش (Respawn)",
    Searchable = false
})

AddButton(MainTab, {
    Name = " تنفيذ الأمر المختار",
    Callback = function()
        local selected = RespawnKillDropdown.GetSelected()
        if selected == "رفريش (Respawn)" then
            -- نرسل أمر ;refresh مباشرة عبر HDAdmin
            pcall(function()
                local hdClient = RepStorage:FindFirstChild("HDAdminHDClient")
                if hdClient then
                    local signals = hdClient:FindFirstChild("Signals")
                    if signals then
                        local requestCommand = signals:FindFirstChild("RequestCommand")
                        if requestCommand then
                            requestCommand:InvokeServer(";refresh")
                            MakeNotifi({Title="أمر", Text="تم إرسال ;refresh بنجاح", Time=2})
                        else
                            MakeNotifi({Title="خطأ", Text="RequestCommand غير موجود", Time=2})
                        end
                    else
                        MakeNotifi({Title="خطأ", Text="HDAdmin Signals غير موجود", Time=2})
                    end
                else
                    MakeNotifi({Title="خطأ", Text="HDAdminHDClient غير موجود", Time=2})
                end
            end)
        elseif selected == "موت سريع (Kill)" then
            if LP.Character and LP.Character:FindFirstChild("Humanoid") then
                LP.Character.Humanoid.Health = 0
                MakeNotifi({Title="أمر", Text="تم القتل السريع", Time=2})
            else
                MakeNotifi({Title="خطأ", Text="لا يوجد شخصية أو Humanoid", Time=2})
            end
        end
    end
})

-- =============================================
-- LOCAL PLAYER TAB — MERGED TOGGLES (تشغيل/إيقاف في نفس الزر)
-- =============================================
local SpeedEnabled = false
local JumpEnabled = false
local NoclipEnabled = false
local InfJumpEnabled = false

AddToggleButton(LocalPlayerTab, {
    Name = "سرعة عالية",
    OnText = "سرعة عالية: تشغيل",
    OffText = "سرعة عالية: إيقاف",
    StartState = false,
    OnEnable = function()
        SpeedEnabled = true
        MakeNotifi({Title="Player", Text="تم تفعيل السرعة العالية", Time=2})
    end,
    OnDisable = function()
        SpeedEnabled = false
        if LP.Character and LP.Character:FindFirstChild("Humanoid") then
            LP.Character.Humanoid.WalkSpeed = 16
        end
        MakeNotifi({Title="Player", Text="تم إرجاع السرعة الطبيعية", Time=2})
    end
})

AddToggleButton(LocalPlayerTab, {
    Name = "قفز عالي",
    OnText = "قفز عالي: تشغيل",
    OffText = "قفز عالي: إيقاف",
    StartState = false,
    OnEnable = function()
        JumpEnabled = true
        MakeNotifi({Title="Player", Text="تم تفعيل القفز العالي", Time=2})
    end,
    OnDisable = function()
        JumpEnabled = false
        if LP.Character and LP.Character:FindFirstChild("Humanoid") then
            LP.Character.Humanoid.JumpPower = 50
        end
        MakeNotifi({Title="Player", Text="تم إرجاع القفز الطبيعي", Time=2})
    end
})

AddToggleButton(LocalPlayerTab, {
    Name = "Noclip (اختراق الجدران)",
    OnText = "Noclip: تشغيل",
    OffText = "Noclip: إيقاف",
    StartState = false,
    OnEnable = function()
        NoclipEnabled = true
        MakeNotifi({Title="Player", Text="Noclip On", Time=2})
    end,
    OnDisable = function()
        NoclipEnabled = false
        MakeNotifi({Title="Player", Text="Noclip Off", Time=2})
    end
})

AddToggleButton(LocalPlayerTab, {
    Name = "قفز لا نهائي بالهواء",
    OnText = "قفز لا نهائي: تشغيل",
    OffText = "قفز لا نهائي: إيقاف",
    StartState = false,
    OnEnable = function()
        InfJumpEnabled = true
        MakeNotifi({Title="Player", Text="Inf Jump On", Time=2})
    end,
    OnDisable = function()
        InfJumpEnabled = false
        MakeNotifi({Title="Player", Text="Inf Jump Off", Time=2})
    end
})

RunService.Stepped:Connect(function()
    if LP.Character and LP.Character:FindFirstChild("Humanoid") then
        if SpeedEnabled then
            LP.Character.Humanoid.WalkSpeed = 100
        end
        if JumpEnabled then
            LP.Character.Humanoid.JumpPower = 150
        end
    end
    
    if NoclipEnabled and LP.Character then
        for _, v in pairs(LP.Character:GetDescendants()) do
            if v:IsA("BasePart") then
                v.CanCollide = false
            end
        end
    end
end)

UserInputService.JumpRequest:Connect(function()
    if InfJumpEnabled and LP.Character and LP.Character:FindFirstChild("Humanoid") then
        LP.Character.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
    end
end)

-- =============================================
-- ESP TAB (RADAR LITE) — ثابت الحجم + تحديث سريع + استهداف دائم
-- =============================================
local ESP_Active = false
local ESP_ShowNames = true
local ESP_ShowBody = true
local ESP_ShowHealth = true
local ESP_ShowDistance = true
local ESP_MaxDistance = 250

local ESP_UpdateInterval = 0.08
local ESP_TARGET_DISTANCE = 1000000000

local ESP_FOLDER_NAME = "Monetor_ESP_RADAR"
local ESP_TargetUserId = nil
local ESP_TargetName = nil
local ESP_TargetOnline = false
local ESP_TargetDropdown = nil

local function GetESPColor(player)
    local ok, color = pcall(function()
        if player.Team and player.Team.TeamColor then
            return player.Team.TeamColor.Color
        end
        return Color3.fromRGB(0, 170, 255)
    end)
    return ok and color or Color3.fromRGB(0, 170, 255)
end

local function GetCharacterRoot(character)
    return character and (character:FindFirstChild("HumanoidRootPart")
        or character:FindFirstChild("UpperTorso")
        or character:FindFirstChild("Torso"))
end

local function IsTarget(player)
    return player and ESP_TargetUserId ~= nil and player.UserId == ESP_TargetUserId
end

local function GetOrCreateESPFolder(character)
    if not character then return nil end
    local folder = character:FindFirstChild(ESP_FOLDER_NAME)
    if folder and folder:IsA("Folder") then
        return folder
    end

    if folder then
        pcall(function() folder:Destroy() end)
    end

    folder = Instance.new("Folder")
    folder.Name = ESP_FOLDER_NAME
    folder.Parent = character
    return folder
end

local function RemoveESP(player)
    if not player or not player.Character then return end
    local character = player.Character
    local folder = character:FindFirstChild(ESP_FOLDER_NAME)
    if folder then
        pcall(function() folder:Destroy() end)
    end

    local oldName = character:FindFirstChild("Monetor_ESP_Name", true)
    if oldName then
        pcall(function() oldName:Destroy() end)
    end
end

local function ClearAllESP()
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LP then
            RemoveESP(player)
        end
    end
end

local function UpdateNameVisual(player, billboard, distance)
    if not player or not billboard then return end

    local card = billboard:FindFirstChild("Card")
    if not card then return end

    local nameLabel = card:FindFirstChild("PlayerName")
    local infoLabel = card:FindFirstChild("Info")
    local targetBadge = card:FindFirstChild("TargetBadge")
    local humanoid = player.Character and player.Character:FindFirstChildOfClass("Humanoid")

    local target = IsTarget(player)
    local baseColor = target and Color3.fromRGB(255, 55, 55) or GetESPColor(player)

    if nameLabel then
        nameLabel.Text = player.DisplayName ~= "" and player.DisplayName or player.Name
        nameLabel.TextColor3 = target and Color3.fromRGB(255, 85, 85) or Color3.fromRGB(255, 255, 255)

        local accent = nameLabel:FindFirstChild("NameStroke")
        if accent and accent:IsA("UIStroke") then
            accent.Color = baseColor
        end
    end

    if targetBadge then
        targetBadge.Visible = target
        if target then
            targetBadge.Text = "TARGET"
        end
    end

    if infoLabel then
        local infoParts = {}
        if ESP_ShowHealth and humanoid then
            table.insert(infoParts, "HP " .. math.max(0, math.floor(humanoid.Health + 0.5)))
        end
        if ESP_ShowDistance and distance and distance < math.huge then
            table.insert(infoParts, math.floor(distance + 0.5) .. "m")
        end
        infoLabel.Text = table.concat(infoParts, "  •  ")
        infoLabel.Visible = (#infoParts > 0)
        infoLabel.TextColor3 = target and Color3.fromRGB(255, 180, 180) or Color3.fromRGB(215, 225, 235)
    end
end

local function CreateESPName(player)
    if not ESP_Active or not ESP_ShowNames or player == LP then return end

    local character = player.Character
    if not character then return end

    local head = character:FindFirstChild("Head")
    if not head then return end

    local folder = GetOrCreateESPFolder(character)
    if not folder then return end

    local billboard = folder:FindFirstChild("NameBillboard")
    if billboard and not billboard:IsA("BillboardGui") then
        pcall(function() billboard:Destroy() end)
        billboard = nil
    end

    if not billboard then
        billboard = Instance.new("BillboardGui")
        billboard.Name = "NameBillboard"
        billboard.Adornee = head
        billboard.Parent = folder

        billboard.Size = UDim2.new(0, 150, 0, 38)
        billboard.StudsOffsetWorldSpace = Vector3.new(0, 3.15, 0)
        billboard.AlwaysOnTop = true
        billboard.LightInfluence = 0
        billboard.MaxDistance = ESP_MaxDistance
        billboard.ResetOnSpawn = false
        billboard.Enabled = true
        billboard.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

        local card = Instance.new("Frame")
        card.Name = "Card"
        card.Size = UDim2.fromScale(1, 1)
        card.BackgroundTransparency = 1
        card.BorderSizePixel = 0
        card.Parent = billboard

        local nameLabel = Instance.new("TextLabel")
        nameLabel.Name = "PlayerName"
        nameLabel.Size = UDim2.new(1, -8, 0, 19)
        nameLabel.Position = UDim2.new(0, 4, 0, 0)
        nameLabel.BackgroundTransparency = 1
        nameLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        nameLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
        nameLabel.TextStrokeTransparency = 0.08
        nameLabel.Font = Enum.Font.GothamBold
        nameLabel.TextSize = 14
        nameLabel.TextScaled = false
        nameLabel.TextWrapped = false
        nameLabel.TextXAlignment = Enum.TextXAlignment.Center
        nameLabel.TextYAlignment = Enum.TextYAlignment.Center
        nameLabel.Text = player.DisplayName ~= "" and player.DisplayName or player.Name
        nameLabel.Parent = card

        local accent = Instance.new("UIStroke")
        accent.Name = "NameStroke"
        accent.Thickness = 0.9
        accent.Transparency = 0.10
        accent.Parent = nameLabel

        local infoLabel = Instance.new("TextLabel")
        infoLabel.Name = "Info"
        infoLabel.Size = UDim2.new(1, -8, 0, 15)
        infoLabel.Position = UDim2.new(0, 4, 0, 20)
        infoLabel.BackgroundTransparency = 1
        infoLabel.TextColor3 = Color3.fromRGB(215, 225, 235)
        infoLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
        infoLabel.TextStrokeTransparency = 0.25
        infoLabel.Font = Enum.Font.GothamMedium
        infoLabel.TextSize = 11
        infoLabel.TextScaled = false
        infoLabel.TextWrapped = false
        infoLabel.TextXAlignment = Enum.TextXAlignment.Center
        infoLabel.TextYAlignment = Enum.TextYAlignment.Center
        infoLabel.Text = ""
        infoLabel.Parent = card

        local targetBadge = Instance.new("TextLabel")
        targetBadge.Name = "TargetBadge"
        targetBadge.Size = UDim2.new(0, 52, 0, 13)
        targetBadge.Position = UDim2.new(1, -54, 0, -7)
        targetBadge.BackgroundTransparency = 0.18
        targetBadge.BackgroundColor3 = Color3.fromRGB(170, 0, 0)
        targetBadge.BorderSizePixel = 0
        targetBadge.TextColor3 = Color3.fromRGB(255, 255, 255)
        targetBadge.TextStrokeTransparency = 0.35
        targetBadge.Font = Enum.Font.GothamBold
        targetBadge.TextSize = 8
        targetBadge.Text = "TARGET"
        targetBadge.Visible = false
        targetBadge.ZIndex = 3
        targetBadge.Parent = card

        local badgeCorner = Instance.new("UICorner")
        badgeCorner.CornerRadius = UDim.new(0, 4)
        badgeCorner.Parent = targetBadge
    end

    billboard.Adornee = head
    billboard.Size = UDim2.new(0, 150, 0, 38)
    billboard.StudsOffsetWorldSpace = Vector3.new(0, 3.15, 0)
    billboard.MaxDistance = IsTarget(player) and ESP_TARGET_DISTANCE or ESP_MaxDistance
    billboard.Enabled = ESP_ShowNames

    UpdateNameVisual(player, billboard)
end

local function CreateESPBody(player)
    if not ESP_Active or not ESP_ShowBody or player == LP then return end

    local character = player.Character
    if not character then return end

    local folder = GetOrCreateESPFolder(character)
    if not folder then return end

    local highlight = folder:FindFirstChild("BodyHighlight")
    if highlight and not highlight:IsA("Highlight") then
        pcall(function() highlight:Destroy() end)
        highlight = nil
    end

    if not highlight then
        highlight = Instance.new("Highlight")
        highlight.Name = "BodyHighlight"
        highlight.Adornee = character
        highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
        highlight.FillTransparency = 0.84
        highlight.OutlineTransparency = 0.03
        highlight.Parent = folder
    end

    local color = IsTarget(player) and Color3.fromRGB(255, 40, 40) or GetESPColor(player)
    highlight.FillColor = color
    highlight.OutlineColor = color
    highlight.Enabled = true
end

local function CreateESP(player)
    if player == LP or not ESP_Active then return end
    if not player.Character then return end

    CreateESPBody(player)
    CreateESPName(player)
end

local function RefreshTargetVisuals()
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LP and player.Character then
            local folder = player.Character:FindFirstChild(ESP_FOLDER_NAME)
            if folder then
                local billboard = folder:FindFirstChild("NameBillboard")
                if billboard and billboard:IsA("BillboardGui") then
                    billboard.MaxDistance = IsTarget(player) and ESP_TARGET_DISTANCE or ESP_MaxDistance
                    billboard.Size = UDim2.new(0, 150, 0, 38)
                    billboard.Enabled = ESP_Active and ESP_ShowNames
                    UpdateNameVisual(player, billboard)
                end

                local highlight = folder:FindFirstChild("BodyHighlight")
                if highlight and highlight:IsA("Highlight") then
                    local color = IsTarget(player) and Color3.fromRGB(255, 40, 40) or GetESPColor(player)
                    highlight.FillColor = color
                    highlight.OutlineColor = color
                    highlight.Enabled = ESP_Active and ESP_ShowBody
                end
            end
        end
    end
end

local function UpdateRadarTargetList()
    local options = {}
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LP then
            table.insert(options, player.Name)
        end
    end

    if #options == 0 then
        options = {"لا يوجد لاعبين"}
    end

    if ESP_TargetDropdown then
        pcall(function()
            ESP_TargetDropdown:Update(options)
        end)
    end
end

AddToggleButton(ESPTab, {
    Name = "الرادار المطور",
    OnText = "الرادار: تشغيل",
    OffText = "الرادار: إيقاف",
    StartState = false,
    OnEnable = function()
        ESP_Active = true
        for _, player in ipairs(Players:GetPlayers()) do
            if player ~= LP then
                CreateESP(player)
            end
        end
        RefreshTargetVisuals()
        MakeNotifi({Title="ESP", Text="تم تشغيل الرادار المطور", Time=3})
    end,
    OnDisable = function()
        ESP_Active = false
        ClearAllESP()
        MakeNotifi({Title="ESP", Text="تم إيقاف الرادار", Time=2})
    end
})

AddToggleButton(ESPTab, {
    Name = "الأسماء",
    OnText = "الأسماء: تشغيل",
    OffText = "الأسماء: إيقاف",
    StartState = true,
    OnEnable = function()
        ESP_ShowNames = true
        for _, player in ipairs(Players:GetPlayers()) do
            if player ~= LP and player.Character and ESP_Active then
                CreateESPName(player)
            end
        end
        MakeNotifi({Title="ESP", Text="الأسماء: تشغيل", Time=2})
    end,
    OnDisable = function()
        ESP_ShowNames = false
        for _, player in ipairs(Players:GetPlayers()) do
            if player ~= LP and player.Character then
                local folder = player.Character:FindFirstChild(ESP_FOLDER_NAME)
                local billboard = folder and folder:FindFirstChild("NameBillboard")
                if billboard and billboard:IsA("BillboardGui") then
                    billboard.Enabled = false
                end
            end
        end
        MakeNotifi({Title="ESP", Text="الأسماء: إيقاف", Time=2})
    end
})

AddToggleButton(ESPTab, {
    Name = "الجسم",
    OnText = "الجسم: تشغيل",
    OffText = "الجسم: إيقاف",
    StartState = true,
    OnEnable = function()
        ESP_ShowBody = true
        for _, player in ipairs(Players:GetPlayers()) do
            if player ~= LP and player.Character and ESP_Active then
                CreateESPBody(player)
            end
        end
        MakeNotifi({Title="ESP", Text="الجسم: تشغيل", Time=2})
    end,
    OnDisable = function()
        ESP_ShowBody = false
        for _, player in ipairs(Players:GetPlayers()) do
            if player ~= LP and player.Character then
                local folder = player.Character:FindFirstChild(ESP_FOLDER_NAME)
                local highlight = folder and folder:FindFirstChild("BodyHighlight")
                if highlight then pcall(function() highlight:Destroy() end) end
            end
        end
        MakeNotifi({Title="ESP", Text="الجسم: إيقاف", Time=2})
    end
})

AddToggleButton(ESPTab, {
    Name = "الصحة",
    OnText = "الصحة: تشغيل",
    OffText = "الصحة: إيقاف",
    StartState = true,
    OnEnable = function()
        ESP_ShowHealth = true
        MakeNotifi({Title="ESP", Text="الصحة: تشغيل", Time=2})
    end,
    OnDisable = function()
        ESP_ShowHealth = false
        MakeNotifi({Title="ESP", Text="الصحة: إيقاف", Time=2})
    end
})

AddToggleButton(ESPTab, {
    Name = "المسافة",
    OnText = "المسافة: تشغيل",
    OffText = "المسافة: إيقاف",
    StartState = true,
    OnEnable = function()
        ESP_ShowDistance = true
        MakeNotifi({Title="ESP", Text="المسافة: تشغيل", Time=2})
    end,
    OnDisable = function()
        ESP_ShowDistance = false
        MakeNotifi({Title="ESP", Text="المسافة: إيقاف", Time=2})
    end
})

ESP_TargetDropdown = AddDropdown(ESPTab, {
    Name = " اختر المستهدف",
    Options = {"لا يوجد لاعبين"},
    Searchable = true,
    Callback = function(value)
        if value == "لا يوجد لاعبين" then return end

        local targetPlayer = Players:FindFirstChild(value)
        if not targetPlayer or targetPlayer == LP then
            MakeNotifi({Title="Target", Text="اللاعب غير متوفر حالياً", Time=2})
            return
        end

        ESP_TargetUserId = targetPlayer.UserId
        ESP_TargetName = targetPlayer.Name
        ESP_TargetOnline = true

        if ESP_Active then
            CreateESP(targetPlayer)
        end
        RefreshTargetVisuals()

        MakeNotifi({
            Title=" Target",
            Text="تم استهداف " .. targetPlayer.Name .. " — يتبعه الرادار مهما كانت المسافة",
            Time=3
        })
    end
})

AddButton(ESPTab, {
    Name = " تثبيت المستهدف / تحديثه",
    Callback = function()
        RefreshTargetVisuals()
        UpdateRadarTargetList()
        if ESP_TargetName then
            MakeNotifi({Title="Target", Text="المستهدف الحالي: " .. ESP_TargetName, Time=2})
        else
            MakeNotifi({Title="Target", Text="لم يتم اختيار مستهدف", Time=2})
        end
    end
})

AddButton(ESPTab, {
    Name = " إلغاء الاستهداف",
    Callback = function()
        ESP_TargetUserId = nil
        ESP_TargetName = nil
        ESP_TargetOnline = false
        RefreshTargetVisuals()
        MakeNotifi({Title="Target", Text="تم إلغاء الاستهداف", Time=2})
    end
})

local ESP_DistanceBox = Create("TextBox", ESPTab, {
    Size = UDim2.new(1, 0, 0, 30),
    BackgroundColor3 = Configs_HUB.Cor_Options,
    BackgroundTransparency = 0.25,
    BorderSizePixel = 0,
    Text = tostring(ESP_MaxDistance),
    PlaceholderText = "اكتب مدى الرادار بالمتر (مثال: 250)",
    TextColor3 = Configs_HUB.Cor_Text,
    PlaceholderColor3 = Color3.fromRGB(150, 150, 150),
    TextSize = 13,
    Font = Configs_HUB.Text_Font,
    ClearTextOnFocus = false,
    TextXAlignment = Enum.TextXAlignment.Center,
    ZIndex = 20
})
Corner(ESP_DistanceBox)
Stroke(ESP_DistanceBox)

local function ApplyESPDistance(value)
    value = math.floor(tonumber(value) or 250)
    value = math.clamp(value, 10, 5000)
    ESP_MaxDistance = value
    ESP_DistanceBox.Text = tostring(value)

    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LP and player.Character then
            local folder = player.Character:FindFirstChild(ESP_FOLDER_NAME)
            local billboard = folder and folder:FindFirstChild("NameBillboard")
            if billboard and billboard:IsA("BillboardGui") then
                billboard.MaxDistance = IsTarget(player) and ESP_TARGET_DISTANCE or ESP_MaxDistance
            end
        end
    end

    MakeNotifi({Title="ESP", Text="مدى الرادار صار: " .. ESP_MaxDistance .. " متر", Time=2})
end

ESP_DistanceBox.FocusLost:Connect(function()
    ApplyESPDistance(ESP_DistanceBox.Text)
end)

AddButton(ESPTab, {
    Name = " تطبيق مدى الرادار",
    Callback = function()
        ApplyESPDistance(ESP_DistanceBox.Text)
    end
})

AddTextLabel(ESPTab, " LITE للموبايل • افتراضي 250 متر • تحديث تلقائي سريع • المستهدف أحمر ودائم حتى خارج المدى")

Players.PlayerAdded:Connect(function(player)
    UpdateRadarTargetList()

    if ESP_TargetUserId and player.UserId == ESP_TargetUserId then
        ESP_TargetName = player.Name
        ESP_TargetOnline = true
        MakeNotifi({Title=" Target", Text="رجع المستهدف " .. player.Name .. " للسيرفر — تم استرجاع الاستهداف", Time=4})
    end

    player.CharacterAdded:Connect(function()
        task.wait(0.25)
        if ESP_Active then
            CreateESP(player)
            if IsTarget(player) then
                MakeNotifi({Title=" Target", Text="تم استرجاع رادار المستهدف " .. player.Name, Time=2})
            end
        end
    end)
end)

Players.PlayerRemoving:Connect(function(player)
    local wasTarget = IsTarget(player)
    if wasTarget then
        ESP_TargetOnline = false
        MakeNotifi({
            Title=" Target",
            Text="خرج المستهدف " .. (ESP_TargetName or player.Name) .. " من السيرفر — الاستهداف محفوظ لعودته",
            Time=5
        })
    end

    RemoveESP(player)
    UpdateRadarTargetList()
end)

for _, player in ipairs(Players:GetPlayers()) do
    if player ~= LP then
        player.CharacterAdded:Connect(function()
            task.wait(0.25)
            if ESP_Active then
                CreateESP(player)
                if IsTarget(player) then
                    MakeNotifi({Title=" Target", Text="تم استرجاع رادار المستهدف " .. player.Name, Time=2})
                end
            end
        end)
    end
end

UpdateRadarTargetList()

local ESP_UpdateClock = 0
RunService.Heartbeat:Connect(function(dt)
    if not ESP_Active then return end

    ESP_UpdateClock += dt
    if ESP_UpdateClock < ESP_UpdateInterval then return end
    ESP_UpdateClock = 0

    local myCharacter = LP.Character
    local myRoot = GetCharacterRoot(myCharacter)
    if not myRoot then return end

    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LP then
            local character = player.Character
            local folder = character and character:FindFirstChild(ESP_FOLDER_NAME)
            local head = character and character:FindFirstChild("Head")

            if character and head and folder then
                local billboard = folder:FindFirstChild("NameBillboard")
                local root = GetCharacterRoot(character)
                local distance = root and (root.Position - myRoot.Position).Magnitude or math.huge
                local target = IsTarget(player)

                if billboard and billboard:IsA("BillboardGui") then
                    billboard.Adornee = head
                    billboard.Size = UDim2.new(0, 150, 0, 38)
                    billboard.StudsOffsetWorldSpace = Vector3.new(0, 3.15, 0)
                    billboard.MaxDistance = target and ESP_TARGET_DISTANCE or ESP_MaxDistance
                    billboard.Enabled = ESP_ShowNames and (target or distance <= ESP_MaxDistance)
                    UpdateNameVisual(player, billboard, distance)
                end

                local highlight = folder:FindFirstChild("BodyHighlight")
                if highlight and highlight:IsA("Highlight") then
                    local color = target and Color3.fromRGB(255, 40, 40) or GetESPColor(player)
                    highlight.FillColor = color
                    highlight.OutlineColor = color
                    highlight.Enabled = ESP_ShowBody and (target or distance <= ESP_MaxDistance)
                end
            elseif target and character and head then
                CreateESP(player)
            end
        end
    end
end)

-- =============================================
-- END OF LIGHT RADAR
-- =============================================

-- =============================================
-- ️ COMBAT TAB (قتال) — دم حديد + درع + قوة خارقة + Auto Refresh + Auto Refresh On Death
-- =============================================
local CombatTab = MakeTab({Name = "قتال ️", TabTitle = true})

local GodMode = false
local ShieldMode = false
local SuperDamage = false
local AimbotMode = false
local CombatSpeed = false
local CombatJump = false

local godConnection = nil
local shieldConnection = nil
local aimbotConnection = nil

-- =============================================
--  Auto Refresh (رفريش تلقائي عند الدم)
-- =============================================
local AutoRefreshEnabled = false
local AutoRefreshButton = nil
local AutoRefreshConnection = nil
local AutoRefreshCharacterConnection = nil
local AutoRefreshHealthConnection = nil
local REFRESH_THRESHOLD = 50
local AutoRefreshTriggered = false
local LastRefreshTime = 0
local REFRESH_COOLDOWN = 0.15

-- =============================================
--  Auto Refresh On Death (رفريش تلقائي عند الموت) — جديد
-- =============================================
local AutoRefreshOnDeathEnabled = false
local AutoRefreshOnDeathConnection = nil
local AutoRefreshOnDeathCharConnection = nil

-- حماية من تكرار/تداخل InvokeServer بدون تأخير إرسال الرفريش
local RefreshBusy = false
local DeathRefreshBusy = false

local function GetAutoRefreshHumanoid()
    local character = LP.Character
    if not character then return nil end
    return character:FindFirstChildOfClass("Humanoid")
end

local function SendRefreshCommand()
    if RefreshBusy then
        return false, "refresh قيد التنفيذ"
    end

    RefreshBusy = true

    local ok, success, message = pcall(function()
        local hdClient = RepStorage:FindFirstChild("HDAdminHDClient")
        if not hdClient then return false, "HDAdminHDClient غير موجود" end

        local signals = hdClient:FindFirstChild("Signals")
        if not signals then return false, "HDAdmin Signals غير موجود" end

        local requestCommand = signals:FindFirstChild("RequestCommand")
        if not requestCommand or not requestCommand:IsA("RemoteFunction") then
            return false, "RequestCommand غير موجود"
        end

        requestCommand:InvokeServer(";refresh")
        return true, "تم إرسال ;refresh"
    end)

    RefreshBusy = false

    if not ok then
        return false, tostring(success)
    end
    return success == true, message or (success and "تم إرسال ;refresh" or "تعذر إرسال ;refresh")
end

local function GetAutoRefreshButtonLabel()
    return AutoRefreshButton and AutoRefreshButton:FindFirstChild("TextLabel")
end

local function SetAutoRefreshButtonText(text)
    if not AutoRefreshButton then return end
    local label = GetAutoRefreshButtonLabel()
    if label then
        label.Text = text
    end
end

local function CheckHealthAndRefresh()
    if not AutoRefreshEnabled then return end

    local humanoid = GetAutoRefreshHumanoid()
    if not humanoid or humanoid.MaxHealth <= 0 or humanoid.Health <= 0 then
        AutoRefreshTriggered = false
        return
    end

    local healthPercent = (humanoid.Health / humanoid.MaxHealth) * 100

    if healthPercent <= REFRESH_THRESHOLD then
        if not AutoRefreshTriggered and (os.clock() - LastRefreshTime) >= REFRESH_COOLDOWN then
            AutoRefreshTriggered = true
            LastRefreshTime = os.clock()

            local success, message = SendRefreshCommand()
            MakeNotifi({
                Title = success and "Auto Refresh" or "Auto Refresh — خطأ",
                Text = success
                    and ("تم إرسال ;refresh فوراً — الدم: " .. math.floor(healthPercent) .. "%")
                    or ("تعذر إرسال ;refresh: " .. tostring(message)),
                Time = success and 2 or 3
            })
        end
    else
        AutoRefreshTriggered = false
    end
end

local function DisconnectAutoRefreshConnections()
    if AutoRefreshConnection then
        AutoRefreshConnection:Disconnect()
        AutoRefreshConnection = nil
    end
    if AutoRefreshCharacterConnection then
        AutoRefreshCharacterConnection:Disconnect()
        AutoRefreshCharacterConnection = nil
    end
    if AutoRefreshHealthConnection then
        AutoRefreshHealthConnection:Disconnect()
        AutoRefreshHealthConnection = nil
    end
end

local function StartAutoRefreshMonitor()
    DisconnectAutoRefreshConnections()
    AutoRefreshTriggered = false
    LastRefreshTime = 0

    AutoRefreshConnection = RunService.RenderStepped:Connect(CheckHealthAndRefresh)

    AutoRefreshCharacterConnection = LP.CharacterAdded:Connect(function(character)
        AutoRefreshTriggered = false
        local humanoid = character:WaitForChild("Humanoid", 5)
        if humanoid and AutoRefreshEnabled then
            if AutoRefreshHealthConnection then
                AutoRefreshHealthConnection:Disconnect()
            end
            AutoRefreshHealthConnection = humanoid.HealthChanged:Connect(function()
                CheckHealthAndRefresh()
            end)
        end
    end)

    local humanoid = GetAutoRefreshHumanoid()
    if humanoid then
        AutoRefreshHealthConnection = humanoid.HealthChanged:Connect(function()
            CheckHealthAndRefresh()
        end)
    end
end

-- =============================================
--  Auto Refresh On Death Functions (رفريش عند الموت)
-- =============================================
local function DisconnectAutoRefreshOnDeathConnections()
    if AutoRefreshOnDeathConnection then
        AutoRefreshOnDeathConnection:Disconnect()
        AutoRefreshOnDeathConnection = nil
    end
    if AutoRefreshOnDeathCharConnection then
        AutoRefreshOnDeathCharConnection:Disconnect()
        AutoRefreshOnDeathCharConnection = nil
    end
end

local function HookAutoRefreshOnDeathToHumanoid(humanoid)
    if AutoRefreshOnDeathConnection then
        AutoRefreshOnDeathConnection:Disconnect()
        AutoRefreshOnDeathConnection = nil
    end
    if not humanoid then return end

    AutoRefreshOnDeathConnection = humanoid.Died:Connect(function()
        if not AutoRefreshOnDeathEnabled then return end

        -- يمنع إرسال أكثر من refresh لنفس الموت، بدون أي تأخير
        if DeathRefreshBusy or RefreshBusy then
            return
        end

        DeathRefreshBusy = true

        -- الإرسال يبدأ فور إطلاق Died، لكن خارج callback حتى لا نعلّق حدث الموت
        task.spawn(function()
            local success, message = SendRefreshCommand()

            MakeNotifi({
                Title = success and "Auto Refresh (Death)" or "Auto Refresh (Death) — خطأ",
                Text = success
                    and "تم إرسال ;refresh فوراً عند الموت"
                    or ("تعذر إرسال ;refresh: " .. tostring(message)),
                Time = success and 2 or 3
            })

            DeathRefreshBusy = false
        end)
    end)
end

local function StartAutoRefreshOnDeathMonitor()
    DisconnectAutoRefreshOnDeathConnections()
    DeathRefreshBusy = false

    -- ربط الموت الحالي
    local humanoid = GetAutoRefreshHumanoid()
    if humanoid then
        HookAutoRefreshOnDeathToHumanoid(humanoid)
    end

    -- إعادة ربط النظام مع كل شخصية جديدة
    AutoRefreshOnDeathCharConnection = LP.CharacterAdded:Connect(function(character)
        DeathRefreshBusy = false
        local newHumanoid = character:WaitForChild("Humanoid", 5)
        if newHumanoid and AutoRefreshOnDeathEnabled then
            HookAutoRefreshOnDeathToHumanoid(newHumanoid)
        end
    end)
end

-- زر: رفريش تلقائي عند الموت (Toggle)
AddToggleButton(CombatTab, {
    Name = "رفريش تلقائي عند الموت",
    OnText = "رفريش عند الموت: تشغيل",
    OffText = "رفريش عند الموت: إيقاف",
    StartState = false,
    OnEnable = function()
        AutoRefreshOnDeathEnabled = true
        StartAutoRefreshOnDeathMonitor()
        MakeNotifi({
            Title = "Auto Refresh (Death)",
            Text = "تم التفعيل — عند الموت يُرسل ;refresh تلقائياً",
            Time = 4
        })
    end,
    OnDisable = function()
        AutoRefreshOnDeathEnabled = false
        DisconnectAutoRefreshOnDeathConnections()
        MakeNotifi({
            Title = "Auto Refresh (Death)",
            Text = "تم إيقاف رفريش عند الموت",
            Time = 2
        })
    end
})

-- زر: رفريش تلقائي عند الدم (Toggle)
AddToggleButton(CombatTab, {
    Name = "رفريش تلقائي (عند الدم)",
    OnText = "رفريش تلقائي: تشغيل",
    OffText = "رفريش تلقائي: إيقاف",
    StartState = false,
    OnEnable = function()
        AutoRefreshEnabled = true
        StartAutoRefreshMonitor()
        MakeNotifi({
            Title = "Auto Refresh",
            Text = "تم التفعيل — يراقب الدم كل فريم، وعند 50% أو أقل يرسل ;refresh فوراً",
            Time = 4
        })
        CheckHealthAndRefresh()
    end,
    OnDisable = function()
        AutoRefreshEnabled = false
        DisconnectAutoRefreshConnections()
        AutoRefreshTriggered = false
        MakeNotifi({Title="Auto Refresh", Text="تم إيقاف الرفريش التلقائي", Time=2})
    end
})

-- =============================================
-- الدوال القتالية الأخرى
-- =============================================
local function CreateShield()
    if LP.Character then
        local shield = LP.Character:FindFirstChild("Monetor_Shield")
        if not shield then
            shield = Instance.new("ForceField")
            shield.Name = "Monetor_Shield"
            shield.Visible = true
            shield.Parent = LP.Character
        end
    end
end

local function RemoveShield()
    if LP.Character then
        local shield = LP.Character:FindFirstChild("Monetor_Shield")
        if shield then
            shield:Destroy()
        end
    end
end

AddToggleButton(CombatTab, {
    Name = "دم حديد (God Mode)",
    OnText = "دم حديد: تشغيل",
    OffText = "دم حديد: إيقاف",
    StartState = false,
    OnEnable = function()
        GodMode = true
        if godConnection then godConnection:Disconnect() end
        godConnection = RunService.Heartbeat:Connect(function()
            if LP.Character and LP.Character:FindFirstChild("Humanoid") then
                if LP.Character.Humanoid.Health < LP.Character.Humanoid.MaxHealth then
                    LP.Character.Humanoid.Health = LP.Character.Humanoid.MaxHealth
                end
            end
        end)
        MakeNotifi({Title="قتال", Text=" تم تفعيل الدم الحديد — ما تموت!", Time=3})
    end,
    OnDisable = function()
        GodMode = false
        if godConnection then godConnection:Disconnect() godConnection = nil end
        MakeNotifi({Title="قتال", Text="تم إيقاف الدم الحديد", Time=2})
    end
})

AddToggleButton(CombatTab, {
    Name = "درع قوي (ForceField)",
    OnText = "درع قوي: تشغيل",
    OffText = "درع قوي: إيقاف",
    StartState = false,
    OnEnable = function()
        ShieldMode = true
        CreateShield()
        MakeNotifi({Title="قتال", Text="️ تم تفعيل الدرع — محمي من الضربات!", Time=3})
    end,
    OnDisable = function()
        ShieldMode = false
        RemoveShield()
        MakeNotifi({Title="قتال", Text="تم إيقاف الدرع", Time=2})
    end
})

AddToggleButton(CombatTab, {
    Name = "قوة ضربة سوبر",
    OnText = "قوة ضربة: تشغيل",
    OffText = "قوة ضربة: إيقاف",
    StartState = false,
    OnEnable = function()
        SuperDamage = true
        if LP.Character then
            for _, tool in ipairs(LP.Character:GetChildren()) do
                if tool:IsA("Tool") and tool:FindFirstChild("Handle") then
                    local handle = tool.Handle
                    if handle:IsA("BasePart") then
                        handle.Touched:Connect(function(hit)
                            if SuperDamage then
                                local humanoid = hit.Parent and hit.Parent:FindFirstChildOfClass("Humanoid")
                                if humanoid and humanoid ~= LP.Character.Humanoid then
                                    humanoid.Health = 0
                                end
                            end
                        end)
                    end
                end
            end
        end
        MakeNotifi({Title="قتال", Text=" ضربتك صارت تقتل بضربة وحدة!", Time=3})
    end,
    OnDisable = function()
        SuperDamage = false
        MakeNotifi({Title="قتال", Text="تم إيقاف قوة الضربة", Time=2})
    end
})

AddToggleButton(CombatTab, {
    Name = "سرعة قتالية",
    OnText = "سرعة قتالية: تشغيل",
    OffText = "سرعة قتالية: إيقاف",
    StartState = false,
    OnEnable = function()
        CombatSpeed = true
        if LP.Character and LP.Character:FindFirstChild("Humanoid") then
            LP.Character.Humanoid.WalkSpeed = 150
        end
        MakeNotifi({Title="قتال", Text=" سرعتك صارت 150 — ما يلحقونك!", Time=3})
    end,
    OnDisable = function()
        CombatSpeed = false
        if LP.Character and LP.Character:FindFirstChild("Humanoid") then
            LP.Character.Humanoid.WalkSpeed = 16
        end
        MakeNotifi({Title="قتال", Text="تم إرجاع السرعة", Time=2})
    end
})

AddToggleButton(CombatTab, {
    Name = "قفز خارق",
    OnText = "قفز خارق: تشغيل",
    OffText = "قفز خارق: إيقاف",
    StartState = false,
    OnEnable = function()
        CombatJump = true
        if LP.Character and LP.Character:FindFirstChild("Humanoid") then
            LP.Character.Humanoid.JumpPower = 300
        end
        MakeNotifi({Title="قتال", Text=" قفزك صار خارق — تقفز فوق البنايات!", Time=3})
    end,
    OnDisable = function()
        CombatJump = false
        if LP.Character and LP.Character:FindFirstChild("Humanoid") then
            LP.Character.Humanoid.JumpPower = 50
        end
        MakeNotifi({Title="قتال", Text="تم إرجاع القفز", Time=2})
    end
})

RunService.Stepped:Connect(function()
    if LP.Character and LP.Character:FindFirstChild("Humanoid") then
        if CombatSpeed then
            LP.Character.Humanoid.WalkSpeed = 150
        end
        if CombatJump then
            LP.Character.Humanoid.JumpPower = 300
        end
    end
end)

LP.CharacterAdded:Connect(function(character)
    task.wait(0.5)
    if ShieldMode then
        CreateShield()
    end
end)

-- =============================================
-- ️  تطوير (MASS LAUNCH + SPIN) — MERGED TOGGLE
-- =============================================
local TATWIR_Active = false
local TATWIR_SpinSpeed = 200
local TATWIR_SpeedBox = nil
local TATWIR_Toggle = nil
local TATWIR_SpinConnection = nil
local TATWIR_TouchConnection = nil
local TATWIR_BodyAngular = nil

local function TATWIR_Cleanup()
    TATWIR_Active = false

    if TATWIR_SpinConnection then
        TATWIR_SpinConnection:Disconnect()
        TATWIR_SpinConnection = nil
    end

    if TATWIR_TouchConnection then
        TATWIR_TouchConnection:Disconnect()
        TATWIR_TouchConnection = nil
    end

    if TATWIR_BodyAngular then
        pcall(function() TATWIR_BodyAngular:Destroy() end)
        TATWIR_BodyAngular = nil
    end

    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= LP and plr.Character then
            for _, part in ipairs(plr.Character:GetDescendants()) do
                if part:IsA("BasePart") then
                    pcall(function()
                        if part.Name == "HumanoidRootPart" then
                            part.CanCollide = false
                        else
                            part.CanCollide = true
                        end
                    end)
                end
            end
        end
    end

    if LP.Character then
        local myRoot = LP.Character:FindFirstChild("HumanoidRootPart")
        if myRoot then
            local oldBV = myRoot:FindFirstChild("Tatwir_Spin")
            if oldBV then pcall(function() oldBV:Destroy() end) end
        end
    end
end

local function TATWIR_LaunchPlayer(targetChar)
    if not targetChar or targetChar == LP.Character then return end
    local targetRoot = targetChar:FindFirstChild("HumanoidRootPart") or targetChar:FindFirstChild("Torso")
    if not targetRoot then return end

    local oldBV = targetRoot:FindFirstChild("Tatwir_Launch")
    if oldBV then pcall(function() oldBV:Destroy() end) end

    local bv = Instance.new("BodyVelocity")
    bv.Name = "Tatwir_Launch"
    bv.MaxForce = Vector3.new(9e9, 9e9, 9e9)
    bv.Velocity = Vector3.new(0, 250, 0) + Vector3.new(
        math.random(-50, 50),
        0,
        math.random(-50, 50)
    )
    bv.Parent = targetRoot

    task.delay(0.35, function()
        if bv and bv.Parent then
            bv:Destroy()
        end
    end)
end

local function TATWIR_Start()
    if TATWIR_Active then return end
    local char = LP.Character
    if not char then
        MakeNotifi({Title="تطوير", Text="ما موجودة شخصية", Time=2})
        return
    end
    local myRoot = char:FindFirstChild("HumanoidRootPart")
    local myHum = char:FindFirstChildOfClass("Humanoid")
    if not myRoot or not myHum then
        MakeNotifi({Title="تطوير", Text="ما موجود HumanoidRootPart", Time=2})
        return
    end

    TATWIR_Active = true

    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= LP and plr.Character then
            for _, part in ipairs(plr.Character:GetDescendants()) do
                if part:IsA("BasePart") then
                    pcall(function()
                        part.CanCollide = true
                    end)
                end
            end
        end
    end

    local oldSpin = myRoot:FindFirstChild("Tatwir_Spin")
    if oldSpin then pcall(function() oldSpin:Destroy() end) end

    TATWIR_BodyAngular = Instance.new("BodyAngularVelocity")
    TATWIR_BodyAngular.Name = "Tatwir_Spin"
    TATWIR_BodyAngular.MaxTorque = Vector3.new(9e9, 9e9, 9e9)
    TATWIR_BodyAngular.AngularVelocity = Vector3.new(0, math.rad(TATWIR_SpinSpeed), 0)
    TATWIR_BodyAngular.P = 9e9
    TATWIR_BodyAngular.Parent = myRoot

    TATWIR_TouchConnection = myRoot.Touched:Connect(function(hit)
        if not TATWIR_Active then return end
        local model = hit:FindFirstAncestorOfClass("Model")
        if not model or model == char then return end
        if not model:FindFirstChildOfClass("Humanoid") then return end
        local plr = Players:GetPlayerFromCharacter(model)
        if not plr or plr == LP then return end
        TATWIR_LaunchPlayer(model)
    end)

    TATWIR_SpinConnection = RunService.Heartbeat:Connect(function()
        if not TATWIR_Active then return end

        if TATWIR_BodyAngular and TATWIR_BodyAngular.Parent then
            TATWIR_BodyAngular.AngularVelocity = Vector3.new(0, math.rad(TATWIR_SpinSpeed), 0)
        end

        for _, plr in ipairs(Players:GetPlayers()) do
            if plr ~= LP and plr.Character then
                for _, part in ipairs(plr.Character:GetDescendants()) do
                    if part:IsA("BasePart") and part.CanCollide == false and part.Name ~= "HumanoidRootPart" then
                        pcall(function() part.CanCollide = true end)
                    end
                end
            end
        end
    end)

    MakeNotifi({
        Title = "تطوير",
        Text = "تم التفعيل — كل اللاعبين ملموسين، الدوران: " .. TATWIR_SpinSpeed .. "°/ث، لمس أي لاعب يطير في الجو!",
        Time = 4
    })
end

local function TATWIR_Stop()
    if not TATWIR_Active then
        MakeNotifi({Title="تطوير", Text="موقّف أصلاً", Time=2})
        return
    end
    TATWIR_Cleanup()

    MakeNotifi({Title="تطوير", Text="تم الإيقاف — رجع كل شي طبيعي", Time=2})
end

-- زر Toggle لتطوير
TATWIR_Toggle = AddToggleButton(CombatTab, {
    Name = "تطوير",
    OnText = "تطوير: تشغيل",
    OffText = "تطوير: إيقاف",
    StartState = false,
    OnEnable = function()
        TATWIR_Start()
    end,
    OnDisable = function()
        TATWIR_Stop()
    end
})

-- صندوق سرعة الدوران (Spin Speed)
TATWIR_SpeedBox = Create("TextBox", CombatTab, {
    Size = UDim2.new(1, 0, 0, 30),
    BackgroundColor3 = Configs_HUB.Cor_Options,
    BackgroundTransparency = 0.25,
    BorderSizePixel = 0,
    Text = tostring(TATWIR_SpinSpeed),
    PlaceholderText = "اكتب سرعة الدوران (مثال: 200)",
    TextColor3 = Configs_HUB.Cor_Text,
    PlaceholderColor3 = Color3.fromRGB(150, 150, 150),
    TextSize = 13,
    Font = Configs_HUB.Text_Font,
    ClearTextOnFocus = false,
    TextXAlignment = Enum.TextXAlignment.Center,
    ZIndex = 20
})
Corner(TATWIR_SpeedBox)
Stroke(TATWIR_SpeedBox)

local function TATWIR_ApplySpeed()
    local v = tonumber(TATWIR_SpeedBox.Text) or TATWIR_SpinSpeed
    v = math.clamp(math.floor(v), 0, 100000)
    TATWIR_SpinSpeed = v
    TATWIR_SpeedBox.Text = tostring(v)
    if TATWIR_BodyAngular then
        TATWIR_BodyAngular.AngularVelocity = Vector3.new(0, math.rad(v), 0)
    end
    MakeNotifi({Title="تطوير", Text="سرعة الدوران صارت: " .. v .. "°/ث", Time=2})
end

TATWIR_SpeedBox.FocusLost:Connect(function()
    TATWIR_ApplySpeed()
end)

AddButton(CombatTab, {
    Name = " تطبيق سرعة الدوران",
    Callback = function()
        TATWIR_ApplySpeed()
    end
})

AddButton(CombatTab, {
    Name = " زيادة سرعة الدوران (+50)",
    Callback = function()
        TATWIR_SpinSpeed = TATWIR_SpinSpeed + 50
        TATWIR_SpeedBox.Text = tostring(TATWIR_SpinSpeed)
        if TATWIR_BodyAngular then
            TATWIR_BodyAngular.AngularVelocity = Vector3.new(0, math.rad(TATWIR_SpinSpeed), 0)
        end
        MakeNotifi({Title="تطوير", Text="سرعة الدوران: " .. TATWIR_SpinSpeed .. "°/ث", Time=1})
    end
})

AddButton(CombatTab, {
    Name = " تقليل سرعة الدوران (-50)",
    Callback = function()
        TATWIR_SpinSpeed = math.max(0, TATWIR_SpinSpeed - 50)
        TATWIR_SpeedBox.Text = tostring(TATWIR_SpinSpeed)
        if TATWIR_BodyAngular then
            TATWIR_BodyAngular.AngularVelocity = Vector3.new(0, math.rad(TATWIR_SpinSpeed), 0)
        end
        MakeNotifi({Title="تطوير", Text="سرعة الدوران: " .. TATWIR_SpinSpeed .. "°/ث", Time=1})
    end
})

LP.CharacterAdded:Connect(function()
    if TATWIR_Active then
        TATWIR_Stop()
        if TATWIR_Toggle and TATWIR_Toggle.SetState then
            TATWIR_Toggle.SetState(false)
        end
    end
end)

-- =============================================
-- TELEPORT TAB (FULLY RESTORED) with Searchable Dropdown
-- =============================================
local TPSelectedPlayer = ""

local TPDropdown = AddDropdown(TeleportTab, {
    Name = "اختر لاعب للانتقال إليه",
    Options = {},
    Searchable = true,
    Callback = function(value)
        TPSelectedPlayer = value
    end
})

AddButton(TeleportTab, {
    Name = " انتقال فوري للاعب المختار",
    Callback = function()
        if TPSelectedPlayer == "" or TPSelectedPlayer == "لا يوجد لاعبين" then
            MakeNotifi({Title="تنبيه", Text="يرجى اختيار لاعب", Time=2})
            return
        end
        
        local targetPlayer = Players:FindFirstChild(TPSelectedPlayer)
        if targetPlayer and targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart") then
            if LP.Character and LP.Character:FindFirstChild("HumanoidRootPart") then
                LP.Character.HumanoidRootPart.CFrame = targetPlayer.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, 3)
                MakeNotifi({Title="Teleport", Text="تم الانتقال إلى " .. TPSelectedPlayer, Time=2})
            end
        else
            MakeNotifi({Title="خطأ", Text="اللاعب غير موجود أو لم يترسبن", Time=2})
        end
    end
})

-- =============================================
-- TROLL TAB (FULLY RESTORED) with Searchable Player Dropdown
-- =============================================
local selectedPlayerName = ""
local playerDropdown = nil
local isInvisActive = false
local invisConnection = nil
local followConnection = nil
local targetDeathConnection = nil
local EscapePosition = Vector3.new(284, 40, 118)

local function StopInvisibilityAndFollow(teleportToSafeSpot)
    isInvisActive = false
    if invisConnection then invisConnection:Disconnect() invisConnection = nil end
    if followConnection then followConnection:Disconnect() followConnection = nil end
    if targetDeathConnection then targetDeathConnection:Disconnect() targetDeathConnection = nil end
    
    local char = LP.Character
    if char then
        local root = char:FindFirstChild("HumanoidRootPart") or char:FindFirstChild("Torso")
        
        if teleportToSafeSpot and root then
            root.CFrame = CFrame.new(EscapePosition)
        end

        local humanoid = char:FindFirstChildOfClass("Humanoid")
        if humanoid then humanoid.CameraOffset = Vector3.zero end
        workspace.CurrentCamera.CameraSubject = humanoid or root
        
        for _, v in pairs(char:GetDescendants()) do
            if v:IsA("BasePart") and v.Transparency == 0.5 then
                v.Transparency = 0
            end
        end
    end
end

local function UpdatePlayerList()
    local playerNames = {}
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LP then
            table.insert(playerNames, player.Name)
        end
    end
    if #playerNames == 0 then table.insert(playerNames, "لا يوجد لاعبين") end
    
    if playerDropdown then pcall(function() playerDropdown:Update(playerNames) end) end
    if TPDropdown then pcall(function() TPDropdown:Update(playerNames) end) end
end

playerDropdown = AddDropdown(TrollTab, {
    Name = "اختر لاعب",
    Options = {},
    Searchable = true,
    Callback = function(value)
        selectedPlayerName = value
    end
})

AddButton(TrollTab, {
    Name = "️ اقتل اللاعب (تتبع + إخفاء + هروب فوري)",
    Callback = function()
        if selectedPlayerName == "" or selectedPlayerName == "لا يوجد لاعبين" then
            MakeNotifi({Title = "تنبيه", Text = "يرجى اختيار لاعب أولاً من القائمة أعلاه", Time = 3})
            return
        end

        pcall(function()
            local args = {";r6"}
            local signal = RepStorage:FindFirstChild("HDAdminHDClient")
            if signal then
                signal = signal:FindFirstChild("Signals")
                if signal then
                    signal = signal:FindFirstChild("RequestCommand")
                    if signal then
                        signal:InvokeServer(unpack(args))
                    end
                end
            end
        end)

        MakeNotifi({Title = "Troll", Text = "جاري التحول إلى R6 وتفعيل الهجوم على " .. selectedPlayerName .. "...", Time = 4})

        task.spawn(function()
            task.wait(3)
            
            local myChar = LP.Character
            if not myChar or not myChar:FindFirstChild("Torso") then
                MakeNotifi({Title = "تنبيه", Text = "يرجى التأكد من تحول شخصيتك لـ R6 وإعادة الضغط", Time = 3})
                return
            end

            local targetPlayer = Players:FindFirstChild(selectedPlayerName)
            if not targetPlayer or not targetPlayer.Character then
                MakeNotifi({Title = "خطأ", Text = "اللاعب غير متوفر في السيرفر", Time = 3})
                return
            end

            StopInvisibilityAndFollow(false)
            isInvisActive = true

            MakeNotifi({Title = "Troll", Text = "تم تفعيل الإخفاء والتتبع بنجاح!", Time = 3})

            local myRoot = myChar:WaitForChild("HumanoidRootPart")
            local myHumanoid = myChar:WaitForChild("Humanoid")

            local targetHumanoid = targetPlayer.Character:FindFirstChildOfClass("Humanoid")
            if targetHumanoid then
                targetDeathConnection = targetHumanoid.Died:Connect(function()
                    StopInvisibilityAndFollow(true)
                    MakeNotifi({Title = "تم القتل!! ", Text = "تم القتل والانتقال فوراً للمكان الآمن (284, 40, 118)", Time = 5})
                end)
            end

            invisConnection = RunService.Heartbeat:Connect(function()
                if not isInvisActive or not myChar or not myHumanoid or myHumanoid.Health <= 0 then
                    StopInvisibilityAndFollow(false)
                    return
                end

                if workspace.CurrentCamera.CameraSubject ~= myRoot then
                    workspace.CurrentCamera.CameraSubject = myRoot
                end

                myRoot.CFrame = myRoot.CFrame * CFrame.Angles(math.rad(90), 0, 0)
                myHumanoid.CameraOffset = Vector3.new(0, 1.44, 0)

                local anim = Instance.new("Animation")
                anim.AnimationId = "rbxassetid://215384594"
                local track = myHumanoid:LoadAnimation(anim)
                track.Priority = Enum.AnimationPriority.Action4
                track:Play()
                track:AdjustSpeed(0)
                track.TimePosition = 0.3
                RunService.RenderStepped:Wait()
                track:Stop()

                for _, v in pairs(myChar:GetDescendants()) do
                    if v:IsA("BasePart") and v.Transparency ~= 1 then
                        v.Transparency = 0.5
                    end
                end
            end)

            followConnection = RunService.RenderStepped:Connect(function()
                if isInvisActive and targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart") then
                    local targetRoot = targetPlayer.Character.HumanoidRootPart
                    myRoot.CFrame = targetRoot.CFrame * CFrame.new(0, 0, 1)
                end
            end)
        end)
    end
})

AddButton(TrollTab, {
    Name = " إيقاف الإخفاء والتتبع",
    Callback = function()
        StopInvisibilityAndFollow(false)
        MakeNotifi({Title = "Troll", Text = "تم إلغاء الإخفاء والتتبع", Time = 2})
    end
})

UpdatePlayerList()
Players.PlayerAdded:Connect(UpdatePlayerList)
Players.PlayerRemoved:Connect(UpdatePlayerList)

-- =============================================
-- RIGHTS TAB (FULLY RESTORED)
-- =============================================
AddPlayerProfile(RightsTab)

AddButton(RightsTab, {
    Name = " قناة Monetor X على التليجرام",
    Callback = function()
        pcall(function()
            if setclipboard then
                setclipboard('https://t.me/Speed_loder')
            end
        end)
        MakeNotifi({Title = "Monetor X", Text = "تم نسخ رابط قناة التليجرام", Time = 2})
    end
})

AddButton(RightsTab, {
    Name = " حساب Monetor X على التليجرام",
    Callback = function()
        pcall(function()
            if setclipboard then
                setclipboard('@LXEKE')
            end
        end)
        MakeNotifi({Title = "Monetor X", Text = "تم نسخ حساب @monetor_", Time = 2})
    end
})

AddButton(RightsTab, {
    Name = " حساب Monetor X على تيك توك",
    Callback = function()
        pcall(function()
            if setclipboard then
                setclipboard('@monetor_')
            end
        end)
        MakeNotifi({Title = "Monetor X", Text = "تم نسخ حساب @monetor_", Time = 2})
    end
})

AddButton(RightsTab, {
    Name = "️ حقوق Monetor X",
    Callback = function()
        MakeNotifi({
            Title = "حقوق Monetor X",
            Text = "تم صنع السكربت بواسطة Monetor X\nتيك توك: @monetor_\nتليجرام: @LXEKE\nقناة التليجرام: https://t.me/Speed_loder",
            Time = 6
        })
    end
})

-- =============================================
-- FINAL INITIALIZATION
-- =============================================
task.wait(1.5)
pcall(FullBypass)

task.wait(0.5)
MakeNotifi({
    Title = "Monetor X",
    Text = "تم تفعيل Monetor X بنجاح! استمتع",
    Time = 3
})

print(" Monetor X Loaded Successfully!")