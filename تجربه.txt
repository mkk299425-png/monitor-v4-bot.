-- =============================================
-- MONETOR X (FULL EDITION + AVATAR + USAGE + CREDITS)
-- Developed by Monetor
-- =============================================

local Players = game:GetService("Players")
local LP = Players.LocalPlayer
local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local RepStorage = game:GetService("ReplicatedStorage")
local LogService = game:GetService("LogService")
local MarketplaceService = game:GetService("MarketplaceService")
local HttpService = game:GetService("HttpService")

local Configs_HUB = {
    Cor_Hub = Color3.fromRGB(10, 10, 12),
    Cor_Options = Color3.fromRGB(19, 19, 23),
    Cor_Stroke = Color3.fromRGB(235, 35, 55),
    Cor_Text = Color3.fromRGB(245, 245, 248),
    Cor_DarkText = Color3.fromRGB(135, 135, 145),
    Corner_Radius = UDim.new(0, 11),
    Text_Font = Enum.Font.FredokaOne
}

-- =============================================
-- HELPER FUNCTIONS
-- =============================================
local function Create(instance, parent, props)
    local new = Instance.new(instance)
    if props then
        for prop, value in pairs(props) do new[prop] = value end
    end
    new.Parent = parent
    return new
end

local function SetProps(instance, props)
    if instance and props then
        for prop, value in pairs(props) do instance[prop] = value end
    end
    return instance
end

local function Corner(parent, props)
    local new = Create("UICorner", parent)
    new.CornerRadius = Configs_HUB.Corner_Radius
    if props then SetProps(new, props) end
    return new
end

local function Stroke(parent, props)
    local new = Create("UIStroke", parent)
    new.Color = Configs_HUB.Cor_Stroke
    new.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    new.Thickness = 2
    local gradient = Instance.new("UIGradient")
    gradient.Color = ColorSequence.new{
        ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 255, 255)),
        ColorSequenceKeypoint.new(0.25, Color3.fromRGB(180, 180, 180)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(15, 15, 15)),
        ColorSequenceKeypoint.new(0.75, Color3.fromRGB(90, 90, 90)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 255, 255))
    }
    gradient.Rotation = 0
    gradient.Parent = new
    coroutine.wrap(function()
        while new.Parent do
            gradient.Rotation = (gradient.Rotation + 2) % 360
            RunService.RenderStepped:Wait()
        end
    end)()
    if props then SetProps(new, props) end
    return new
end

local function CreateTween(instance, prop, value, time, tweenWait)
    local tween = TweenService:Create(instance,
    TweenInfo.new(time, Enum.EasingStyle.Linear),
    {[prop] = value})
    tween:Play()
    if tweenWait then tween.Completed:Wait() end
end

local function TextSetColor(instance)
    instance.MouseEnter:Connect(function()
        CreateTween(instance, "TextColor3", Color3.fromRGB(180, 180, 180), 0.4, false)
    end)
    instance.MouseLeave:Connect(function()
        CreateTween(instance, "TextColor3", Configs_HUB.Cor_Text, 0.4, false)
    end)
end

-- =============================================
-- SCREEN GUI
-- =============================================
local ScreenGui = Create("ScreenGui", CoreGui, {
    Name = "Monetor_Hub",
    IgnoreGuiInset = true,
    ResetOnSpawn = false,
    DisplayOrder = 9999,
    ZIndexBehavior = Enum.ZIndexBehavior.Global
})
-- =============================================
-- CREDITS PANEL (DEFINED EARLY TO AVOID LOCAL REGISTER LIMIT)
-- =============================================
local CreditsPanel = nil


local function CreateCreditsPanel()
    if CreditsPanel and CreditsPanel.Parent then
        CreditsPanel.Visible = not CreditsPanel.Visible
        return
    end

    CreditsPanel = Create("Frame", ScreenGui, {
        Name = "Monetor_CreditsPanel",
        Size = UDim2.new(0, 530, 0, 320),
        Position = UDim2.new(0.5, -265, 0.5, -160),
        BackgroundColor3 = Configs_HUB.Cor_Hub,
        BackgroundTransparency = 0,
        BorderSizePixel = 0,
        Active = true,
        Draggable = true,
        ClipsDescendants = true,
        ZIndex = 200
    })
    Corner(CreditsPanel, {CornerRadius = UDim.new(0, 15)})
    Stroke(CreditsPanel)

    local Header = Create("Frame", CreditsPanel, {
        Size = UDim2.new(1, -20, 0, 42),
        Position = UDim2.new(0, 10, 0, 8),
        BackgroundColor3 = Color3.fromRGB(18, 19, 24),
        BorderSizePixel = 0,
        ZIndex = 201
    })
    Corner(Header, {CornerRadius = UDim.new(0, 10)})

    Create("TextLabel", Header, {
        Size = UDim2.new(1, -55, 1, 0),
        Position = UDim2.new(0, 15, 0, 0),
        BackgroundTransparency = 1,
        Text = "حقوق MONETOR X",
        TextColor3 = Configs_HUB.Cor_Text,
        Font = Enum.Font.GothamBold,
        TextSize = 16,
        TextXAlignment = Enum.TextXAlignment.Left,
        ZIndex = 202
    })

    local CloseBtn = Create("TextButton", Header, {
        Size = UDim2.new(0, 30, 0, 30),
        Position = UDim2.new(1, -36, 0.5, -15),
        BackgroundColor3 = Color3.fromRGB(65, 18, 28),
        BorderSizePixel = 0,
        Text = "×",
        TextColor3 = Color3.fromRGB(255, 150, 160),
        Font = Enum.Font.GothamBold,
        TextSize = 18,
        AutoButtonColor = false,
        ZIndex = 203
    })
    Corner(CloseBtn, {CornerRadius = UDim.new(0, 8)})
    CloseBtn.Activated:Connect(function()
        CreditsPanel.Visible = false
    end)

    -- Avatar card: no "A" fallback; if the image cannot be resolved, it stays clean.
    local AvatarCard = Create("Frame", CreditsPanel, {
        Size = UDim2.new(0, 145, 0, 245),
        Position = UDim2.new(0, 12, 0, 58),
        BackgroundColor3 = Color3.fromRGB(20, 21, 26),
        BorderSizePixel = 0,
        ZIndex = 201
    })
    Corner(AvatarCard, {CornerRadius = UDim.new(0, 11)})
    Stroke(AvatarCard)

    local devAvatar = Create("ImageLabel", AvatarCard, {
        Size = UDim2.new(0, 92, 0, 92),
        Position = UDim2.new(0.5, -46, 0, 16),
        BackgroundColor3 = Color3.fromRGB(28, 29, 35),
        BackgroundTransparency = 0,
        Image = "",
        ScaleType = Enum.ScaleType.Crop,
        ClipsDescendants = true,
        ZIndex = 202
    })
    Corner(devAvatar, {CornerRadius = UDim.new(1, 0)})

    local devStroke = Instance.new("UIStroke")
    devStroke.Thickness = 3
    devStroke.Color = Color3.fromRGB(195, 20, 42)
    devStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    devStroke.Parent = devAvatar

    Create("TextLabel", AvatarCard, {
        Size = UDim2.new(1, -12, 0, 22),
        Position = UDim2.new(0, 6, 0, 116),
        BackgroundTransparency = 1,
        Text = "@sjbcjjs0",
        TextColor3 = Color3.fromRGB(255, 255, 255),
        Font = Enum.Font.GothamBold,
        TextSize = 14,
        TextXAlignment = Enum.TextXAlignment.Center,
        ZIndex = 202
    })

    Create("TextLabel", AvatarCard, {
        Size = UDim2.new(1, -12, 0, 18),
        Position = UDim2.new(0, 6, 0, 140),
        BackgroundTransparency = 1,
        Text = "ADMIN • الحساب الإداري",
        TextColor3 = Configs_HUB.Cor_DarkText,
        Font = Enum.Font.Gotham,
        TextSize = 9,
        TextXAlignment = Enum.TextXAlignment.Center,
        ZIndex = 202
    })

    Create("TextLabel", AvatarCard, {
        Size = UDim2.new(1, -16, 0, 42),
        Position = UDim2.new(0, 8, 1, -52),
        BackgroundTransparency = 1,
        Text = "Monetor X\nPersonal Control Panel",
        TextColor3 = Configs_HUB.Cor_DarkText,
        Font = Enum.Font.Gotham,
        TextSize = 9,
        TextXAlignment = Enum.TextXAlignment.Center,
        TextYAlignment = Enum.TextYAlignment.Center,
        ZIndex = 202
    })

    -- Resolve the requested account and load its current Roblox headshot.
    task.spawn(function()
        local adminUserId
        pcall(function()
            local p = Players:FindFirstChild("sjbcjjs0")
            if p then adminUserId = p.UserId end
        end)
        if not adminUserId then
            pcall(function()
                adminUserId = Players:GetUserIdFromNameAsync("sjbcjjs0")
            end)
        end

        if type(adminUserId) == "number" and devAvatar.Parent then
            pcall(function()
                local image, ready = Players:GetUserThumbnailAsync(
                    adminUserId,
                    Enum.ThumbnailType.AvatarHeadShot,
                    Enum.ThumbnailSize.Size150x150
                )
                if image and image ~= "" and devAvatar.Parent then
                    devAvatar.Image = image
                end
            end)
        end
    end)

    -- Scrollable credits/info area so nothing is cut off.
    local CreditsScroll = Create("ScrollingFrame", CreditsPanel, {
        Size = UDim2.new(1, -170, 0, 245),
        Position = UDim2.new(0, 158, 0, 58),
        BackgroundColor3 = Color3.fromRGB(16, 17, 21),
        BorderSizePixel = 0,
        BackgroundTransparency = 0,
        CanvasSize = UDim2.new(0, 0, 0, 0),
        AutomaticCanvasSize = Enum.AutomaticSize.Y,
        ScrollingDirection = Enum.ScrollingDirection.Y,
        ScrollBarThickness = 4,
        ScrollBarImageColor3 = Configs_HUB.Cor_Stroke,
        ZIndex = 201
    })
    Corner(CreditsScroll, {CornerRadius = UDim.new(0, 11)})
    Stroke(CreditsScroll)
    Create("UIPadding", CreditsScroll, {
        PaddingLeft = UDim.new(0, 12),
        PaddingRight = UDim.new(0, 12),
        PaddingTop = UDim.new(0, 12),
        PaddingBottom = UDim.new(0, 12)
    })
    Create("UIListLayout", CreditsScroll, {
        Padding = UDim.new(0, 8),
        SortOrder = Enum.SortOrder.LayoutOrder
    })

    local infoText = [[
MONETOR X
Version: 2.0 Full Edition
Platform: Roblox

الحساب الإداري: @sjbcjjs0
المطور: Monetor

الروابط:
Telegram: @LXEKE
TikTok: @monetor_
Channel: Speed_loder

جميع الحقوق محفوظة © 2026
]]
    Create("TextLabel", CreditsScroll, {
        Size = UDim2.new(1, 0, 0, 0),
        BackgroundTransparency = 1,
        Text = infoText,
        TextColor3 = Configs_HUB.Cor_Text,
        Font = Enum.Font.Gotham,
        TextSize = 12,
        TextXAlignment = Enum.TextXAlignment.Left,
        TextYAlignment = Enum.TextYAlignment.Top,
        TextWrapped = true,
        AutomaticSize = Enum.AutomaticSize.Y,
        ZIndex = 202
    })
end

local ClickSound = Instance.new("Sound", ScreenGui)
ClickSound.SoundId = "rbxassetid://88442833509532"
ClickSound.Volume = 0.5

local function PlaySound() pcall(function() ClickSound:Play() end) end

task.spawn(function()
    task.wait(2)
    for _, button in pairs(ScreenGui:GetDescendants()) do
        if button:IsA("TextButton") or button:IsA("ImageButton") then
            button.Activated:Connect(PlaySound)
        end
    end
    ScreenGui.DescendantAdded:Connect(function(newButton)
        if newButton:IsA("TextButton") or newButton:IsA("ImageButton") then
            task.wait(0.1)
            newButton.Activated:Connect(PlaySound)
        end
    end)
end)

local ScreenFind = CoreGui:FindFirstChild(ScreenGui.Name)
if ScreenFind and ScreenFind ~= ScreenGui then ScreenFind:Destroy() end

function DestroyScript() pcall(function() ScreenGui:Destroy() end) end

-- =============================================
-- NOTIFICATION SYSTEM
-- =============================================
local Menu_Notifi = Create("Frame", ScreenGui, {
    Size = UDim2.new(0, 300, 1, 0),
    Position = UDim2.new(1, 0, 0, 0),
    AnchorPoint = Vector2.new(1, 0),
    BackgroundTransparency = 1
})
Create("UIPadding", Menu_Notifi, { PaddingLeft = UDim.new(0, 25), PaddingTop = UDim.new(0, 25), PaddingBottom = UDim.new(0, 50) })
Create("UIListLayout", Menu_Notifi, { Padding = UDim.new(0, 15), VerticalAlignment = "Bottom" })

local Monetor_NotificationLimit = 3

function MakeNotifi(Configs)
    Configs = Configs or {}
    local Title = Configs.Title or "Monetor X"
    local text = Configs.Text or "تم التنفيذ"
    local timewait = Configs.Time or 5

    -- Keep only the newest 3 notifications.
    local active = {}
    for _, child in ipairs(Menu_Notifi:GetChildren()) do
        if child:IsA("Frame") and child.Name == "MonetorNotification" then
            table.insert(active, child)
        end
    end
    while #active >= Monetor_NotificationLimit do
        local oldest = table.remove(active, 1)
        if oldest then
            pcall(function() oldest:Destroy() end)
        end
    end

    local Frame1 = Create("Frame", Menu_Notifi, {
        Size = UDim2.new(2, 0, 0, 0),
        BackgroundTransparency = 1,
        AutomaticSize = "Y",
        Name = "MonetorNotification"
    })

    local Frame2 = Create("Frame", Frame1, {
        Size = UDim2.new(0, Menu_Notifi.Size.X.Offset - 50, 0, 0),
        BackgroundColor3 = Configs_HUB.Cor_Hub,
        Position = UDim2.new(0, Menu_Notifi.Size.X.Offset, 0, 0),
        AutomaticSize = "Y"
    })
    Corner(Frame2)
    Stroke(Frame2)

    Create("TextLabel", Frame2, {
        Size = UDim2.new(1, -55, 0, 25),
        Font = Configs_HUB.Text_Font,
        BackgroundTransparency = 1,
        Text = Title,
        TextSize = 18,
        Position = UDim2.new(0, 20, 0, 5),
        TextXAlignment = "Left",
        TextColor3 = Configs_HUB.Cor_Text
    })

    local CloseNotifi = Create("TextButton", Frame2, {
        Text = "×",
        Font = Configs_HUB.Text_Font,
        TextSize = 20,
        BackgroundTransparency = 1,
        TextColor3 = Color3.fromRGB(200, 200, 200),
        Position = UDim2.new(1, -5, 0, 5),
        AnchorPoint = Vector2.new(1, 0),
        Size = UDim2.new(0, 25, 0, 25),
        AutoButtonColor = false
    })

    Create("TextLabel", Frame2, {
        Size = UDim2.new(1, -30, 0, 0),
        Position = UDim2.new(0, 20, 0, CloseNotifi.Size.Y.Offset + 10),
        TextSize = 14,
        TextColor3 = Configs_HUB.Cor_DarkText,
        TextXAlignment = "Left",
        TextYAlignment = "Top",
        AutomaticSize = "Y",
        Text = tostring(text):gsub("^[%z\1-\127\194-\244][\128-\191]*%s*", ""),
        Font = Configs_HUB.Text_Font,
        BackgroundTransparency = 1,
        TextWrapped = true
    })

    local FrameSize = Create("Frame", Frame2, {
        Size = UDim2.new(1, 0, 0, 2),
        BackgroundColor3 = Configs_HUB.Cor_Stroke,
        Position = UDim2.new(0, 2, 0, 30),
        BorderSizePixel = 0
    })
    Corner(FrameSize)

    Create("Frame", Frame2, {
        Size = UDim2.new(0, 0, 0, 5),
        Position = UDim2.new(0, 0, 1, 5),
        BackgroundTransparency = 1
    })

    task.spawn(function()
        CreateTween(FrameSize, "Size", UDim2.new(0, 0, 0, 2), timewait, true)
    end)

    local closed = false
    local function CloseNotification()
        if closed then return end
        closed = true
        pcall(function()
            CreateTween(Frame2, "Position", UDim2.new(0, Menu_Notifi.Size.X.Offset, 0, 0), 0.22, true)
        end)
        pcall(function() Frame1:Destroy() end)
    end

    CloseNotifi.MouseButton1Click:Connect(CloseNotification)

    task.spawn(function()
        CreateTween(Frame2, "Position", UDim2.new(0, -18, 0, 0), 0.18, true)
        CreateTween(Frame2, "Position", UDim2.new(), 0.28, true)
        task.wait(timewait)
        if Frame1 and Frame1.Parent then
            CloseNotification()
        end
    end)
end

-- =============================================
-- BYPASS SYSTEM
-- =============================================
local BypassEnabled = false

local function SafeHookFunction(func, newfunc)
    if hookfunction then return hookfunction(func, newfunc) end
    return func
end

local function FindAllACERemotes()
    local remotes = {}
    local containers = {
        RepStorage, workspace,
        game:GetService("Lighting"), game:GetService("CoreGui"),
        game:GetService("StarterGui"),
        LP:FindFirstChild("PlayerGui"), LP:FindFirstChild("Backpack"),
        LP:FindFirstChild("PlayerScripts"),
        LP:FindFirstChild("Character") or workspace:FindFirstChild(LP.Name)
    }
    for _, service in ipairs(game:GetChildren()) do
        if service:IsA("Service") and not table.find(containers, service) then
            table.insert(containers, service)
        end
    end
    for _, container in ipairs(containers) do
        if not container then continue end
        local success, descendants = pcall(function() return container:GetDescendants() end)
        if not success then continue end
        for _, obj in ipairs(descendants) do
            if (obj:IsA("RemoteEvent") or obj:IsA("RemoteFunction")) then
                local name = obj.Name
                if string.find(name, "ACE") or string.find(name:lower(), "ace") then
                    table.insert(remotes, obj)
                end
            end
        end
    end
    return remotes
end

local function ApplyBypassToRemote(remote)
    if not remote then return end
    if remote:IsA("RemoteEvent") then
        local oldFire = remote.FireServer
        if oldFire then
            SafeHookFunction(oldFire, function(self, ...)
                local args = {...}
                local msg = tostring(args[1] or "")
                if msg == "HB" then return oldFire(self, unpack(args)) end
                return nil
            end)
        end
    elseif remote:IsA("RemoteFunction") then
        local oldInvoke = remote.InvokeServer
        if oldInvoke then
            SafeHookFunction(oldInvoke, function(self, ...)
                local args = {...}
                if tostring(args[1]) == "HB" then return "OK" end
                return true
            end)
        end
    end
end

local function DisableSprintMovementScans()
    local ps = LP:FindFirstChild("PlayerScripts")
    if not ps then return end
    if not getgc then return end
    local GC = getgc(true)
    for _, obj in ipairs(GC) do
        if type(obj) == "function" then
            local info = debug.getinfo(obj)
            if info and info.what == "Lua" and info.source and string.find(info.source, "SprintMovement") then
                if info.name == "scanForMovementObjects" or info.name == "scanForGuiObjects" then
                    SafeHookFunction(obj, function() return false, nil end)
                end
            end
        end
    end
end

local function DisableLogs()
    if not getconnections then return end
    local conns = getconnections(LogService.MessageOut)
    if conns then
        for _, c in ipairs(conns) do pcall(c.Disconnect, c) end
    end
    pcall(function() 
        if hookfunction then hookfunction(LogService.MessageOut.Connect, function() return nil end) end
    end)
end

local function FullBypass()
    if BypassEnabled then
        MakeNotifi({Title = "Monetor X", Text = "الحماية مكسورة مسبقاً", Time = 2})
        return
    end
    local remotes = FindAllACERemotes()
    if #remotes > 0 then
        for _, r in ipairs(remotes) do ApplyBypassToRemote(r) end
    end
    DisableSprintMovementScans()
    DisableLogs()
    BypassEnabled = true
    MakeNotifi({ Title = "Monetor X", Text = "تم فك الحماية بنجاح", Time = 2 })
end

local function MonitorNewRemotes()
    local containers = {
        RepStorage, workspace,
        game:GetService("Lighting"), game:GetService("CoreGui"),
        game:GetService("StarterGui"),
        LP:FindFirstChild("PlayerGui"), LP:FindFirstChild("Backpack"),
        LP:FindFirstChild("PlayerScripts")
    }
    for _, container in ipairs(containers) do
        if not container then continue end
        pcall(function()
            container.ChildAdded:Connect(function(child)
                if child:IsA("RemoteEvent") or child:IsA("RemoteFunction") then
                    if string.find(child.Name, "ACE") or string.find(child.Name:lower(), "ace") then
                        task.wait(0.5)
                        ApplyBypassToRemote(child)
                    end
                end
            end)
        end)
    end
end

LP.CharacterAdded:Connect(function()
    task.wait(1)
    local char = LP.Character
    if char then
        for _, obj in ipairs(char:GetDescendants()) do
            if (obj:IsA("RemoteEvent") or obj:IsA("RemoteFunction")) and (string.find(obj.Name, "ACE") or string.find(obj.Name:lower(), "ace")) then
                ApplyBypassToRemote(obj)
            end
        end
    end
    DisableSprintMovementScans()
end)

local ps_ref = LP:FindFirstChild("PlayerScripts")
if ps_ref then
    ps_ref.ChildAdded:Connect(function(child)
        if child:IsA("LocalScript") and child.Name == "SprintMovement" then
            task.wait(1)
            DisableSprintMovementScans()
        end
    end)
end

FullBypass()
MonitorNewRemotes()

-- =============================================
-- FLIGHT SYSTEM
-- =============================================
local MainFlyButton = nil
local Flying = false
local FlySpeed = 50
local bodyVelocity = nil
local bodyGyro = nil
local flightConnection = nil
local currentHumanoid = nil
local currentRoot = nil
local FlyPanelCreated = false

local function UpdateMainFlyButton()
    if MainFlyButton then
        if Flying then MainFlyButton.Text = " هبوط الطيران"
        else MainFlyButton.Text = "️ تشغيل وضع طيران" end
    end
end

local FlyPanel = Create("Frame", ScreenGui, {
    Name = "FlyControlPanel",
    Size = UDim2.new(0, 200, 0, 120),
    Position = UDim2.new(0.5, -100, 0.2, 0),
    BackgroundColor3 = Configs_HUB.Cor_Hub,
    BackgroundTransparency = 0.2, Active = true, Draggable = true,
    Visible = false, BorderSizePixel = 0, ZIndex = 50
})
Corner(FlyPanel); Stroke(FlyPanel)

Create("TextLabel", FlyPanel, {
    Size = UDim2.new(1, 0, 0, 25), BackgroundTransparency = 1,
    Text = "التحكم بالطيران", TextColor3 = Configs_HUB.Cor_Text,
    Font = Configs_HUB.Text_Font, TextSize = 18,
    TextXAlignment = Enum.TextXAlignment.Center, ZIndex = 51
})

local FlySpeedLabel = Create("TextLabel", FlyPanel, {
    Size = UDim2.new(1, 0, 0, 20), Position = UDim2.new(0, 0, 0, 30),
    BackgroundTransparency = 1, Text = "السرعة: " .. FlySpeed,
    TextColor3 = Configs_HUB.Cor_Text, Font = Configs_HUB.Text_Font,
    TextSize = 14, TextXAlignment = Enum.TextXAlignment.Center, ZIndex = 51
})

local FlyIncButton = Create("TextButton", FlyPanel, {
    Size = UDim2.new(0.45, 0, 0, 30), Position = UDim2.new(0.05, 0, 0, 55),
    BackgroundColor3 = Configs_HUB.Cor_Options, Text = "+10",
    TextColor3 = Configs_HUB.Cor_Text, Font = Configs_HUB.Text_Font,
    TextSize = 16, AutoButtonColor = false, ZIndex = 51
})
Corner(FlyIncButton); Stroke(FlyIncButton)

local FlyDecButton = Create("TextButton", FlyPanel, {
    Size = UDim2.new(0.45, 0, 0, 30), Position = UDim2.new(0.5, 0, 0, 55),
    BackgroundColor3 = Configs_HUB.Cor_Options, Text = "-10",
    TextColor3 = Configs_HUB.Cor_Text, Font = Configs_HUB.Text_Font,
    TextSize = 16, AutoButtonColor = false, ZIndex = 51
})
Corner(FlyDecButton); Stroke(FlyDecButton)

local FlyStopButton = Create("TextButton", FlyPanel, {
    Size = UDim2.new(1, -10, 0, 30), Position = UDim2.new(0, 5, 0, 90),
    BackgroundColor3 = Configs_HUB.Cor_Options, Text = " هبوط",
    TextColor3 = Color3.fromRGB(255, 100, 100), Font = Configs_HUB.Text_Font,
    TextSize = 14, AutoButtonColor = false, Active = true, ZIndex = 51
})
Corner(FlyStopButton); Stroke(FlyStopButton)

local function UpdateFlyPanel() FlySpeedLabel.Text = "السرعة: " .. FlySpeed end

FlyIncButton.Activated:Connect(function()
    FlySpeed = FlySpeed + 10
    UpdateFlyPanel()
    MakeNotifi({Title="Fly", Text="سرعة الطيران: "..FlySpeed, Time=1})
end)

FlyDecButton.Activated:Connect(function()
    if FlySpeed > 10 then FlySpeed = FlySpeed - 10 end
    UpdateFlyPanel()
    MakeNotifi({Title="Fly", Text="سرعة الطيران: "..FlySpeed, Time=1})
end)

local function stopFly()
    if not Flying then
        FlyPanel.Visible = FlyPanelCreated
        UpdateMainFlyButton()
        return
    end
    Flying = false
    if flightConnection then flightConnection:Disconnect() flightConnection = nil end
    if bodyVelocity then bodyVelocity.Velocity = Vector3.zero bodyVelocity:Destroy() bodyVelocity = nil end
    if bodyGyro then bodyGyro:Destroy() bodyGyro = nil end
    if currentHumanoid then
        currentHumanoid.PlatformStand = false
        currentHumanoid:ChangeState(Enum.HumanoidStateType.GettingUp)
        currentHumanoid = nil
    end
    currentRoot = nil
    FlyPanel.Visible = true
    FlyPanelCreated = true
    FlyStopButton.Text = "️ تشغيل الطيران"
    UpdateMainFlyButton()
    MakeNotifi({Title = "Fly", Text = "تم الهبوط", Time = 2})
end

local function startFly()
    if Flying then return end
    local char = LP.Character
    if not char then return end
    local root = char:FindFirstChild("HumanoidRootPart") or char:FindFirstChild("Torso")
    local humanoid = char:FindFirstChildOfClass("Humanoid")
    if not root or not humanoid then return end

    stopFly()
    currentRoot = root
    currentHumanoid = humanoid

    bodyGyro = Instance.new("BodyGyro", root)
    bodyGyro.MaxTorque = Vector3.new(9e9, 9e9, 9e9)
    bodyGyro.P = 9e4
    bodyGyro.CFrame = root.CFrame

    bodyVelocity = Instance.new("BodyVelocity", root)
    bodyVelocity.MaxForce = Vector3.new(9e9, 9e9, 9e9)
    bodyVelocity.Velocity = Vector3.zero

    humanoid.PlatformStand = true
    Flying = true
    FlyPanelCreated = true
    FlyPanel.Visible = true
    FlyStopButton.Text = " هبوط"
    UpdateMainFlyButton()
    UpdateFlyPanel()

    local camera = workspace.CurrentCamera
    flightConnection = RunService.Heartbeat:Connect(function()
        if not Flying or not currentRoot or not currentHumanoid or currentHumanoid.Health <= 0 then
            stopFly() return
        end
        bodyGyro.CFrame = camera.CFrame
        local moveDir = currentHumanoid.MoveDirection
        if moveDir.Magnitude > 0.05 then
            local camCFrame = camera.CFrame
            local forward = camCFrame.LookVector
            local right = camCFrame.RightVector
            local relativeMove = camCFrame:VectorToObjectSpace(moveDir)
            local finalVel = (forward * -relativeMove.Z) + (right * relativeMove.X)
            bodyVelocity.Velocity = finalVel * FlySpeed
        else
            bodyVelocity.Velocity = Vector3.zero
        end
    end)
    MakeNotifi({Title = "Fly", Text = "تم تشغيل الطيران", Time = 2})
end

FlyStopButton.Activated:Connect(function()
    if Flying then stopFly() else startFly() end
end)

LP.CharacterAdded:Connect(function() if Flying then stopFly() end end)

-- =============================================
-- GUI SCANNER REMOVER
-- =============================================
local PlayerGui = LP:WaitForChild("PlayerGui")
local SuspiciousKeywords = {"warning","anticheat","detected","banned","admin","notification","toast","popup","exploit"}

PlayerGui.ChildAdded:Connect(function(child)
    if child:IsA("ScreenGui") then
        local name = child.Name:lower()
        for _, kw in ipairs(SuspiciousKeywords) do
            if string.find(name, kw) then
                task.wait(0.1)
                pcall(child.Destroy, child)
                break
            end
        end
    end
end)

task.wait(0.5)
for _, child in ipairs(PlayerGui:GetChildren()) do
    if child:IsA("ScreenGui") then
        local name = child.Name:lower()
        for _, kw in ipairs(SuspiciousKeywords) do
            if string.find(name, kw) then pcall(child.Destroy, child) break end
        end
    end
end

-- =============================================
-- =============================================
-- =============================================
-- MONETOR X - PROFESSIONAL CUSTOM UI
-- =============================================
-- ملاحظة: الواجهة هنا مستقلة عن وظائف التبويبات.
-- كل عناصر الواجهة لها خلفية واضحة حتى لا تختفي فوق اللعبة.
local UI = {}

UI.Menu = Create("Frame", ScreenGui, {
    Size = UDim2.new(0, 530, 0, 320),
    Position = UDim2.new(0.5, -265, 0.5, -160),
    BackgroundColor3 = Color3.fromRGB(11, 12, 15),
    BackgroundTransparency = 0,
    BorderSizePixel = 0,
    Active = true,
    Draggable = true,
    ClipsDescendants = true,
    ZIndex = 50
})
Corner(UI.Menu, {CornerRadius = UDim.new(0, 15)})
Stroke(UI.Menu)

-- شريط علوي
UI.Header = Create("Frame", UI.Menu, {
    Size = UDim2.new(1, -20, 0, 50),
    Position = UDim2.new(0, 10, 0, 8),
    BackgroundColor3 = Color3.fromRGB(18, 19, 24),
    BackgroundTransparency = 0,
    BorderSizePixel = 0,
    ZIndex = 51
})
Corner(UI.Header, {CornerRadius = UDim.new(0, 11)})

UI.Logo = Create("Frame", UI.Header, {
    Size = UDim2.new(0, 34, 0, 34),
    Position = UDim2.new(0, 8, 0.5, -17),
    BackgroundColor3 = Color3.fromRGB(190, 20, 40),
    BorderSizePixel = 0,
    ZIndex = 52
})
Corner(UI.Logo, {CornerRadius = UDim.new(0, 9)})

Create("TextLabel", UI.Logo, {
    Size = UDim2.new(1, 0, 1, 0),
    BackgroundTransparency = 1,
    Text = "M",
    Font = Enum.Font.GothamBold,
    TextSize = 19,
    TextColor3 = Color3.fromRGB(255,255,255),
    ZIndex = 53
})

Create("TextLabel", UI.Header, {
    Size = UDim2.new(0, 190, 0, 23),
    Position = UDim2.new(0, 51, 0, 7),
    BackgroundTransparency = 1,
    Text = "MONETOR X",
    Font = Enum.Font.GothamBold,
    TextSize = 17,
    TextColor3 = Color3.fromRGB(248,248,250),
    TextXAlignment = Enum.TextXAlignment.Left,
    ZIndex = 52
})

Create("TextLabel", UI.Header, {
    Size = UDim2.new(0, 190, 0, 15),
    Position = UDim2.new(0, 51, 0, 28),
    BackgroundTransparency = 1,
    Text = "PERSONAL CONTROL PANEL",
    Font = Enum.Font.GothamMedium,
    TextSize = 8,
    TextColor3 = Color3.fromRGB(125,127,136),
    TextXAlignment = Enum.TextXAlignment.Left,
    ZIndex = 52
})

-- حالة الاتصال/التشغيل
UI.Status = Create("Frame", UI.Header, {
    Size = UDim2.new(0, 78, 0, 24),
    Position = UDim2.new(1, -145, 0.5, -12),
    BackgroundColor3 = Color3.fromRGB(20, 55, 39),
    BackgroundTransparency = 0,
    BorderSizePixel = 0,
    ZIndex = 52
})
Corner(UI.Status, {CornerRadius = UDim.new(0, 7)})

Create("Frame", UI.Status, {
    Size = UDim2.new(0, 6, 0, 6),
    Position = UDim2.new(0, 9, 0.5, -3),
    BackgroundColor3 = Color3.fromRGB(80, 220, 130),
    BorderSizePixel = 0,
    ZIndex = 53
})
Corner(UI.Status, {CornerRadius = UDim.new(1,0)})

Create("TextLabel", UI.Status, {
    Size = UDim2.new(1, -23, 1, 0),
    Position = UDim2.new(0, 20, 0, 0),
    BackgroundTransparency = 1,
    Text = "READY",
    Font = Enum.Font.GothamBold,
    TextSize = 8,
    TextColor3 = Color3.fromRGB(150,235,180),
    TextXAlignment = Enum.TextXAlignment.Left,
    ZIndex = 53
})

UI.Minimize = Create("TextButton", UI.Header, {
    Size = UDim2.new(0, 30, 0, 30),
    Position = UDim2.new(1, -67, 0.5, -15),
    BackgroundColor3 = Color3.fromRGB(27,28,34),
    BorderSizePixel = 0,
    Text = "−",
    Font = Enum.Font.GothamBold,
    TextSize = 17,
    TextColor3 = Color3.fromRGB(205,205,210),
    AutoButtonColor = false,
    ZIndex = 53
})
Corner(UI.Minimize, {CornerRadius = UDim.new(0,8)})

UI.Close = Create("TextButton", UI.Header, {
    Size = UDim2.new(0, 30, 0, 30),
    Position = UDim2.new(1, -33, 0.5, -15),
    BackgroundColor3 = Color3.fromRGB(70,18,28),
    BorderSizePixel = 0,
    Text = "×",
    Font = Enum.Font.GothamBold,
    TextSize = 18,
    TextColor3 = Color3.fromRGB(255,145,155),
    AutoButtonColor = false,
    ZIndex = 53
})
Corner(UI.Close, {CornerRadius = UDim.new(0,8)})

-- شريط التبويبات الأفقي: تصميم مختلف بالكامل
UI.Nav = Create("ScrollingFrame", UI.Menu, {
    Size = UDim2.new(1, -20, 0, 39),
    Position = UDim2.new(0, 10, 0, 65),
    BackgroundColor3 = Color3.fromRGB(16,17,21),
    BackgroundTransparency = 0,
    BorderSizePixel = 0,
    ScrollBarThickness = 0,
    ScrollingDirection = Enum.ScrollingDirection.X,
    AutomaticCanvasSize = Enum.AutomaticSize.X,
    CanvasSize = UDim2.new(),
    ZIndex = 51
})
Corner(UI.Nav, {CornerRadius = UDim.new(0,10)})
Create("UIPadding", UI.Nav, {
    PaddingLeft = UDim.new(0,7),
    PaddingRight = UDim.new(0,7),
    PaddingTop = UDim.new(0,5),
    PaddingBottom = UDim.new(0,5)
})
Create("UIListLayout", UI.Nav, {
    FillDirection = Enum.FillDirection.Horizontal,
    Padding = UDim.new(0,5),
    VerticalAlignment = Enum.VerticalAlignment.Center
})

-- مساحة المحتوى الواضحة
UI.Content = Create("Frame", UI.Menu, {
    Size = UDim2.new(1, -20, 1, -116),
    Position = UDim2.new(0, 10, 0, 110),
    BackgroundColor3 = Color3.fromRGB(16,17,21),
    BackgroundTransparency = 0,
    BorderSizePixel = 0,
    ClipsDescendants = true,
    ZIndex = 51
})
Corner(UI.Content, {CornerRadius = UDim.new(0,11)})
Stroke(UI.Content)

UI.FirstTab = true

-- تصغير/تكبير
UI.Minimized = false
UI.Minimize.MouseButton1Click:Connect(function()
    UI.Minimized = not UI.Minimized
    UI.Minimize.Text = UI.Minimized and "+" or "−"
    CreateTween(
        UI.Menu,
        "Size",
        UI.Minimized and UDim2.new(0,530,0,66) or UDim2.new(0,530,0,320),
        0.18,
        true
    )
end)

-- نافذة تأكيد الإغلاق
local function ShowCloseConfirm()
    if UI.CloseDialog and UI.CloseDialog.Parent then return end

    UI.CloseDialog = Create("Frame", UI.Menu, {
        Size = UDim2.new(0, 285, 0, 145),
        Position = UDim2.new(0.5, -142, 0.5, -70),
        BackgroundColor3 = Color3.fromRGB(18,19,24),
        BorderSizePixel = 0,
        ZIndex = 100
    })
    Corner(UI.CloseDialog, {CornerRadius = UDim.new(0,12)})
    Stroke(UI.CloseDialog)

    Create("TextLabel", UI.CloseDialog, {
        Size = UDim2.new(1,-24,0,26),
        Position = UDim2.new(0,12,0,14),
        BackgroundTransparency = 1,
        Text = "إغلاق Monetor X",
        Font = Enum.Font.GothamBold,
        TextSize = 17,
        TextColor3 = Color3.fromRGB(248,248,250),
        TextXAlignment = Enum.TextXAlignment.Center,
        ZIndex = 101
    })

    Create("TextLabel", UI.CloseDialog, {
        Size = UDim2.new(1,-24,0,30),
        Position = UDim2.new(0,12,0,43),
        BackgroundTransparency = 1,
        Text = "هل تريد إغلاق الواجهة؟",
        Font = Enum.Font.Gotham,
        TextSize = 11,
        TextColor3 = Color3.fromRGB(145,146,154),
        TextXAlignment = Enum.TextXAlignment.Center,
        ZIndex = 101
    })

    UI.ConfirmClose = Create("TextButton", UI.CloseDialog, {
        Size = UDim2.new(0.42,0,0,34),
        Position = UDim2.new(0.05,0,1,-45),
        BackgroundColor3 = Color3.fromRGB(165,20,40),
        BorderSizePixel = 0,
        Text = "إغلاق",
        Font = Enum.Font.GothamBold,
        TextSize = 11,
        TextColor3 = Color3.fromRGB(255,255,255),
        AutoButtonColor = false,
        ZIndex = 101
    })
    Corner(UI.ConfirmClose, {CornerRadius = UDim.new(0,8)})

    UI.CancelClose = Create("TextButton", UI.CloseDialog, {
        Size = UDim2.new(0.42,0,0,34),
        Position = UDim2.new(0.53,0,1,-45),
        BackgroundColor3 = Color3.fromRGB(30,31,37),
        BorderSizePixel = 0,
        Text = "إلغاء",
        Font = Enum.Font.GothamBold,
        TextSize = 11,
        TextColor3 = Color3.fromRGB(225,225,230),
        AutoButtonColor = false,
        ZIndex = 101
    })
    Corner(UI.CancelClose, {CornerRadius = UDim.new(0,8)})

    UI.CancelClose.MouseButton1Click:Connect(function()
        if UI.CloseDialog then UI.CloseDialog:Destroy(); UI.CloseDialog = nil end
    end)

    UI.ConfirmClose.MouseButton1Click:Connect(function()
        if UI.CloseDialog then UI.CloseDialog:Destroy(); UI.CloseDialog = nil end
        CreateTween(UI.Menu, "Size", UDim2.new(0,530,0,0), 0.2, true)
        task.delay(0.22, function()
            pcall(DestroyScript)
        end)
    end)
end

UI.Close.MouseButton1Click:Connect(ShowCloseConfirm)

-- =============================================
-- MAKETAB - PROFESSIONAL HORIZONTAL TABS
-- =============================================
function MakeTab(Configs)
    local TabName = Configs.Name or "Tab"

    local Chip = Create("TextButton", UI.Nav, {
        Size = UDim2.new(0, 66, 0, 29),
        BackgroundColor3 = Color3.fromRGB(25,26,31),
        BackgroundTransparency = 0,
        BorderSizePixel = 0,
        Text = TabName,
        Font = Enum.Font.GothamMedium,
        TextSize = 9,
        TextColor3 = Color3.fromRGB(175,176,184),
        AutoButtonColor = false,
        ZIndex = 53
    })
    Corner(Chip, {CornerRadius = UDim.new(0,7)})

    local Page = Create("ScrollingFrame", UI.Content, {
        Size = UDim2.new(1,0,1,0),
        Position = UDim2.new(0,0,0,0),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        ScrollBarThickness = 3,
        ScrollBarImageColor3 = Color3.fromRGB(175,20,40),
        ScrollingDirection = Enum.ScrollingDirection.Y,
        AutomaticCanvasSize = Enum.AutomaticSize.Y,
        CanvasSize = UDim2.new(0,0,0,0),
        Visible = UI.FirstTab,
        ClipsDescendants = true,
        ZIndex = 52
    })
    Create("UIPadding", Page, {
        PaddingLeft = UDim.new(0,12),
        PaddingRight = UDim.new(0,12),
        PaddingTop = UDim.new(0,12),
        PaddingBottom = UDim.new(0,12)
    })
    Create("UIListLayout", Page, {
        Padding = UDim.new(0,8),
        SortOrder = Enum.SortOrder.LayoutOrder
    })

    local function Select()
        for _, child in ipairs(UI.Content:GetChildren()) do
            if child:IsA("ScrollingFrame") then
                child.Visible = false
            end
        end

        for _, child in ipairs(UI.Nav:GetChildren()) do
            if child:IsA("TextButton") then
                child.BackgroundColor3 = Color3.fromRGB(25,26,31)
                child.TextColor3 = Color3.fromRGB(175,176,184)
            end
        end

        Page.Visible = true
        Page.CanvasPosition = Vector2.new(0,0)
        Chip.BackgroundColor3 = Color3.fromRGB(158,18,39)
        Chip.TextColor3 = Color3.fromRGB(255,255,255)
    end

    Chip.MouseButton1Click:Connect(Select)

    if UI.FirstTab then
        task.defer(Select)
        UI.FirstTab = false
    end

    return Page
end

-- =============================================
-- ADDBUTTON - واضح وعالي التباين
-- =============================================
function AddButton(parent, Configs)
    local ButtonName = Configs.Name or "Button"
    local Callback = Configs.Callback or function() end

    local Button = Create("TextButton", parent, {
        Size = UDim2.new(1,0,0,38),
        BackgroundColor3 = Color3.fromRGB(27,28,34),
        BackgroundTransparency = 0,
        BorderSizePixel = 0,
        Text = "",
        AutoButtonColor = false,
        ZIndex = 55,
        LayoutOrder = Configs.LayoutOrder or 0
    })
    Corner(Button, {CornerRadius = UDim.new(0,9)})
    Stroke(Button)

    local Accent = Create("Frame", Button, {
        Size = UDim2.new(0,4,0,22),
        Position = UDim2.new(0,10,0.5,-11),
        BackgroundColor3 = Color3.fromRGB(195,20,42),
        BorderSizePixel = 0,
        ZIndex = 56
    })
    Corner(Accent, {CornerRadius = UDim.new(0,2)})

    Create("TextLabel", Button, {
        Size = UDim2.new(1,-40,1,0),
        Position = UDim2.new(0,25,0,0),
        BackgroundTransparency = 1,
        Text = ButtonName:gsub("^%s+",""),
        Font = Enum.Font.GothamMedium,
        TextSize = 12,
        TextColor3 = Color3.fromRGB(242,242,245),
        TextXAlignment = Enum.TextXAlignment.Left,
        TextTruncate = Enum.TextTruncate.AtEnd,
        ZIndex = 56
    })

    Button.MouseEnter:Connect(function()
        Button.BackgroundColor3 = Color3.fromRGB(35,36,43)
        Accent.Size = UDim2.new(0,6,0,24)
    end)
    Button.MouseLeave:Connect(function()
        Button.BackgroundColor3 = Color3.fromRGB(27,28,34)
        Accent.Size = UDim2.new(0,4,0,22)
    end)
    Button.MouseButton1Click:Connect(function()
        pcall(Callback)
    end)

    return Button
end

-- =============================================
-- ADDTOGGLEBUTTON
-- =============================================
function AddToggleButton(parent, Configs)
    local ButtonName = Configs.Name or "Toggle"
    local OnText = Configs.OnText or (ButtonName .. " — تشغيل")
    local OffText = Configs.OffText or (ButtonName .. " — إيقاف")
    local State = Configs.StartState or false
    local OnEnable = Configs.OnEnable or function() end
    local OnDisable = Configs.OnDisable or function() end

    local TextButton = Create("TextButton", parent, {
        Size = UDim2.new(1,0,0,38),
        BackgroundColor3 = Color3.fromRGB(27,28,34),
        BackgroundTransparency = 0,
        BorderSizePixel = 0,
        Text = "",
        AutoButtonColor = false,
        ZIndex = 55
    })
    Corner(TextButton, {CornerRadius = UDim.new(0,9)})
    Stroke(TextButton)

    local Dot = Create("Frame", TextButton, {
        Size = UDim2.new(0,8,0,8),
        Position = UDim2.new(0,12,0.5,-4),
        BackgroundColor3 = State and Color3.fromRGB(75,220,125) or Color3.fromRGB(195,20,42),
        BorderSizePixel = 0,
        ZIndex = 56
    })
    Corner(Dot, {CornerRadius = UDim.new(1,0)})

    local TextLabel = Create("TextLabel", TextButton, {
        TextSize = 12,
        TextColor3 = Color3.fromRGB(242,242,245),
        Text = State and OnText or OffText,
        Size = UDim2.new(1,-100,1,0),
        Position = UDim2.new(0,28,0,0),
        BackgroundTransparency = 1,
        TextXAlignment = Enum.TextXAlignment.Left,
        Font = Enum.Font.GothamMedium,
        ZIndex = 56
    })

    local Status = Create("TextLabel", TextButton, {
        Size = UDim2.new(0,60,0,22),
        Position = UDim2.new(1,-70,0.5,-11),
        BackgroundColor3 = State and Color3.fromRGB(20,70,43) or Color3.fromRGB(65,20,29),
        BorderSizePixel = 0,
        Text = State and "ON" or "OFF",
        Font = Enum.Font.GothamBold,
        TextSize = 9,
        TextColor3 = State and Color3.fromRGB(145,245,175) or Color3.fromRGB(255,150,160),
        ZIndex = 56
    })
    Corner(Status, {CornerRadius = UDim.new(0,6)})

    local function ApplyVisual()
        Dot.BackgroundColor3 = State and Color3.fromRGB(75,220,125) or Color3.fromRGB(195,20,42)
        Status.BackgroundColor3 = State and Color3.fromRGB(20,70,43) or Color3.fromRGB(65,20,29)
        Status.TextColor3 = State and Color3.fromRGB(145,245,175) or Color3.fromRGB(255,150,160)
        Status.Text = State and "ON" or "OFF"
        TextLabel.Text = State and OnText or OffText
    end

    TextButton.MouseButton1Click:Connect(function()
        State = not State
        ApplyVisual()
        if State then pcall(OnEnable) else pcall(OnDisable) end
    end)

    return {
        Button = TextButton,
        TextLabel = TextLabel,
        GetState = function() return State end,
        SetState = function(newState)
            State = newState == true
            ApplyVisual()
        end
    }
end

-- =============================================
-- ADDTEXTLABEL
-- =============================================
function AddTextLabel(parent, text)
    local Label = Create("TextLabel", parent, {
        Size = UDim2.new(1,0,0,34),
        BackgroundColor3 = Color3.fromRGB(22,23,28),
        BackgroundTransparency = 0,
        BorderSizePixel = 0,
        Text = text or "Loading...",
        TextColor3 = Color3.fromRGB(175,176,184),
        TextSize = 10,
        Font = Enum.Font.Gotham,
        TextXAlignment = Enum.TextXAlignment.Left,
        TextYAlignment = Enum.TextYAlignment.Center,
        TextWrapped = true,
        TextScaled = false,
        ZIndex = 55
    })
    Corner(Label, {CornerRadius = UDim.new(0,8)})
    Stroke(Label)
    Create("UIPadding", Label, {
        PaddingLeft = UDim.new(0,12),
        PaddingRight = UDim.new(0,12)
    })
    return {
        Update = function(self, newText) Label.Text = newText end,
        Label = Label
    }
end

-- =============================================
-- ADDPLAYERPROFILE
-- =============================================
function AddPlayerProfile(parent)
    -- حاوية البروفايل
    local ProfileFrame = Create("Frame", parent, {
        Size = UDim2.new(1, 0, 0, 82),
        BackgroundColor3 = Configs_HUB.Cor_Options,
        BackgroundTransparency = 0,
        BorderSizePixel = 0,
        Name = "PlayerProfile"
    })
    Corner(ProfileFrame)
    Stroke(ProfileFrame)

    -- الأفتار
    local AvatarImage = Create("ImageLabel", ProfileFrame, {
        Size = UDim2.new(0, 55, 0, 55),
        Position = UDim2.new(0, 12, 0.5, -27.5),
        BackgroundTransparency = 1,
        BackgroundColor3 = Configs_HUB.Cor_Hub,
        Image = "",
        ZIndex = 5
    })
    Corner(AvatarImage, { CornerRadius = UDim.new(1, 0) })
    local avStroke = Instance.new("UIStroke")
    avStroke.Thickness = 2
    avStroke.Color = Color3.fromRGB(255, 255, 255)
    avStroke.Transparency = 0.1
    avStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    avStroke.Parent = AvatarImage
    task.spawn(function()
        local ok, content = pcall(function()
            local image = Players:GetUserThumbnailAsync(
                LP.UserId,
                Enum.ThumbnailType.AvatarHeadShot,
                Enum.ThumbnailSize.Size150x150
            )
            return image
        end)
        if ok and content and AvatarImage.Parent then
            AvatarImage.Image = content
        end
    end)

    -- الاسم
    local NameLabel = Create("TextLabel", ProfileFrame, {
        Size = UDim2.new(1, -80, 0, 22),
        Position = UDim2.new(0, 75, 0, 8),
        BackgroundTransparency = 1,
        Text = LP.Name,
        TextColor3 = Color3.fromRGB(255, 255, 255),
        Font = Configs_HUB.Text_Font,
        TextSize = 16,
        TextXAlignment = Enum.TextXAlignment.Left,
        ZIndex = 5
    })

    -- Display name
    local DisplayLabel = Create("TextLabel", ProfileFrame, {
        Size = UDim2.new(1, -80, 0, 16),
        Position = UDim2.new(0, 75, 0, 30),
        BackgroundTransparency = 1,
        Text = "Display: " .. LP.DisplayName,
        TextColor3 = Configs_HUB.Cor_DarkText,
        Font = Configs_HUB.Text_Font,
        TextSize = 12,
        TextXAlignment = Enum.TextXAlignment.Left,
        ZIndex = 5
    })

    -- معلومات عامة
    local placeName = "Unknown"
    pcall(function() placeName = MarketplaceService:GetProductInfo(game.PlaceId).Name end)
    
    local InfoLabel = Create("TextLabel", ProfileFrame, {
        Size = UDim2.new(1, -80, 0, 16),
        Position = UDim2.new(0, 75, 0, 48),
        BackgroundTransparency = 1,
        Text = tostring(placeName),
        TextColor3 = Configs_HUB.Cor_DarkText,
        Font = Configs_HUB.Text_Font,
        TextSize = 12,
        TextXAlignment = Enum.TextXAlignment.Left,
        TextTruncate = Enum.TextTruncate.AtEnd,
        ZIndex = 5
    })

    -- FPS + Ping (يتحدث)
    local StatsLabel = Create("TextLabel", ProfileFrame, {
        Size = UDim2.new(1, -80, 0, 16),
        Position = UDim2.new(0, 75, 0, 66),
        BackgroundTransparency = 1,
        Text = "FPS: -- | Ping: --",
        TextColor3 = Color3.fromRGB(120, 220, 120),
        Font = Configs_HUB.Text_Font,
        TextSize = 12,
        TextXAlignment = Enum.TextXAlignment.Left,
        ZIndex = 5
    })

    -- عداد FPS
    local fps = 0
    local frameCount = 0
    local lastTime = tick()
    RunService.Heartbeat:Connect(function()
        frameCount = frameCount + 1
        if tick() - lastTime >= 1 then
            fps = frameCount
            frameCount = 0
            lastTime = tick()
        end
    end)

    task.spawn(function()
        while StatsLabel and StatsLabel.Parent do
            task.wait(1)
            pcall(function()
                StatsLabel.Text = "FPS: " .. fps .. " | Ping: " .. math.floor(LP:GetNetworkPing() * 1000) .. "ms"
            end)
        end
    end)
end

-- =============================================
-- ADDDROPDOWN
-- =============================================
function AddDropdown(parent, Configs)
    local DropdownName = Configs.Name or "Dropdown"
    local Options = Configs.Options or {"1","2","3"}
    local Default = Configs.Default or Options[1]
    local Callback = Configs.Callback or function() end
    local Searchable = Configs.Searchable == true

    local MainFrame = Create("Frame", parent, {
        Size = UDim2.new(1,0,0,38),
        BackgroundTransparency = 1,
        Name = "DropdownContainer",
        ClipsDescendants = false,
        ZIndex = 60
    })

    local TextButton = Create("TextButton", MainFrame, {
        Size = UDim2.new(1,0,0,38),
        BackgroundColor3 = Color3.fromRGB(27,28,34),
        BorderSizePixel = 0,
        Text = "",
        AutoButtonColor = false,
        ZIndex = 61
    })
    Corner(TextButton, {CornerRadius = UDim.new(0,9)})
    Stroke(TextButton)

    local TextLabel = Create("TextLabel", TextButton, {
        Size = UDim2.new(1,-48,1,0),
        Position = UDim2.new(0,12,0,0),
        BackgroundTransparency = 1,
        Text = DropdownName,
        TextColor3 = Color3.fromRGB(240,240,244),
        TextSize = 11,
        Font = Enum.Font.GothamMedium,
        TextXAlignment = Enum.TextXAlignment.Left,
        TextTruncate = Enum.TextTruncate.AtEnd,
        ZIndex = 62
    })

    Create("TextLabel", TextButton, {
        Size = UDim2.new(0,24,1,0),
        Position = UDim2.new(1,-32,0,0),
        BackgroundTransparency = 1,
        Text = "⌄",
        TextColor3 = Color3.fromRGB(180,181,188),
        TextSize = 15,
        Font = Enum.Font.GothamBold,
        ZIndex = 62
    })

    local DropdownFrame = Create("ScrollingFrame", MainFrame, {
        Size = UDim2.new(1,0,0,0),
        Position = UDim2.new(0,0,0,42),
        BackgroundColor3 = Color3.fromRGB(18,19,23),
        BorderSizePixel = 0,
        ClipsDescendants = true,
        Visible = false,
        ScrollBarThickness = 3,
        ScrollBarImageColor3 = Color3.fromRGB(180,20,40),
        CanvasSize = UDim2.new(0,0,0,0),
        AutomaticCanvasSize = Enum.AutomaticSize.Y,
        ZIndex = 90
    })
    Corner(DropdownFrame, {CornerRadius = UDim.new(0,9)})
    Stroke(DropdownFrame)

    Create("UIListLayout", DropdownFrame, {
        Padding = UDim.new(0,4),
        SortOrder = Enum.SortOrder.LayoutOrder
    })
    Create("UIPadding", DropdownFrame, {
        PaddingLeft = UDim.new(0,6),
        PaddingRight = UDim.new(0,6),
        PaddingTop = UDim.new(0,6),
        PaddingBottom = UDim.new(0,6)
    })

    local SearchBox
    if Searchable then
        SearchBox = Create("TextBox", DropdownFrame, {
            Size = UDim2.new(1,0,0,28),
            BackgroundColor3 = Color3.fromRGB(28,29,35),
            BorderSizePixel = 0,
            Text = "",
            PlaceholderText = "بحث...",
            PlaceholderColor3 = Color3.fromRGB(120,121,130),
            TextColor3 = Color3.fromRGB(240,240,244),
            Font = Enum.Font.Gotham,
            TextSize = 11,
            TextXAlignment = Enum.TextXAlignment.Left,
            ClearTextOnFocus = false,
            ZIndex = 91
        })
        Corner(SearchBox, {CornerRadius = UDim.new(0,7)})
        SearchBox.LayoutOrder = -1
    end

    local Selected = Default
    local expanded = false
    local currentSearchText = ""

    local function GetTargetHeight()
        local count = math.max(1,#Options)
        local totalH = count * 30 + 18
        if Searchable then totalH = totalH + 34 end
        return math.min(totalH, 180)
    end

    local function UpdateButtons()
        for _, child in ipairs(DropdownFrame:GetChildren()) do
            if child:IsA("TextButton") then child:Destroy() end
        end

        local filtered = {}
        for _, option in ipairs(Options) do
            option = tostring(option)
            if currentSearchText == "" or string.find(option:lower(), currentSearchText:lower(),1,true) then
                table.insert(filtered, option)
            end
        end

        for _, option in ipairs(filtered) do
            local btn = Create("TextButton", DropdownFrame, {
                Size = UDim2.new(1,0,0,28),
                BackgroundColor3 = Color3.fromRGB(27,28,34),
                BorderSizePixel = 0,
                Text = option,
                TextColor3 = Color3.fromRGB(235,235,240),
                TextSize = 11,
                Font = Enum.Font.Gotham,
                AutoButtonColor = false,
                ZIndex = 92
            })
            Corner(btn, {CornerRadius = UDim.new(0,7)})
            btn.MouseButton1Click:Connect(function()
                Selected = option
                TextLabel.Text = DropdownName .. ": " .. option
                pcall(Callback, option)
                expanded = false
                currentSearchText = ""
                if SearchBox then SearchBox.Text = "" end
                DropdownFrame.Visible = false
                MainFrame.Size = UDim2.new(1,0,0,38)
            end)
        end

        if expanded then
            local h = GetTargetHeight()
            DropdownFrame.Size = UDim2.new(1,0,0,h)
            MainFrame.Size = UDim2.new(1,0,0,42+h)
        end
    end

    UpdateButtons()

    if SearchBox then
        SearchBox:GetPropertyChangedSignal("Text"):Connect(function()
            currentSearchText = SearchBox.Text
            UpdateButtons()
        end)
    end

    TextButton.MouseButton1Click:Connect(function()
        expanded = not expanded
        if expanded then
            UpdateButtons()
            DropdownFrame.Visible = true
            local h = GetTargetHeight()
            DropdownFrame.Size = UDim2.new(1,0,0,h)
            MainFrame.Size = UDim2.new(1,0,0,42+h)
        else
            DropdownFrame.Visible = false
            DropdownFrame.Size = UDim2.new(1,0,0,0)
            MainFrame.Size = UDim2.new(1,0,0,38)
        end
    end)

    return {
        Update = function(self,newOptions)
            Options = newOptions or {}
            UpdateButtons()
        end,
        GetSelected = function(self) return Selected end
    }
end

-- =============================================
-- TABS
-- =============================================
local MainTab = MakeTab({Name = "الرئيسية", TabTitle = true})
local LocalPlayerTab = MakeTab({Name = "شخصيتي ‍️", TabTitle = true})
local ESPTab = MakeTab({Name = "رادار ️", TabTitle = true})
local TeleportTab = MakeTab({Name = "الانتقال ", TabTitle = true})
local TrollTab = MakeTab({Name = "Troll", TabTitle = true, Image = "rbxassetid://6031075931"})
local RightsTab = MakeTab({Name = "حقوق", TabTitle = true, AvatarUsername = "sjbcjjs0"})

-- =============================================
-- COMMAND PANEL
-- =============================================
local Monetor_CommandPanel = nil
local Monetor_CommandInput = nil
local Monetor_CommandStatus = nil
local Monetor_ChatEnabled = false
local Monetor_CommandBusy = false

local function Monetor_SetCommandStatus(message, isError)
    if Monetor_CommandStatus then
        Monetor_CommandStatus.Text = message or ""
        Monetor_CommandStatus.TextColor3 = isError and Color3.fromRGB(255, 120, 120) or Color3.fromRGB(180, 255, 180)
    end
end

local function Monetor_ExecuteTitleCommand(text)
    local commandText = ";titler me " .. text
    local hdClient = RepStorage:WaitForChild("HDAdminHDClient", 5)
    if not hdClient then return false, "HDAdminHDClient غير موجود" end
    local signals = hdClient:WaitForChild("Signals", 5)
    if not signals then return false, "HDAdmin Signals غير موجود" end
    local requestCommand = signals:WaitForChild("RequestCommand", 5)
    if not requestCommand or not requestCommand:IsA("RemoteFunction") then
        return false, "HDAdmin RequestCommand غير متاح"
    end
    local ok, err = pcall(function() requestCommand:InvokeServer(commandText) end)
    if ok then return true, "HDAdmin" end
    return false, "فشل: " .. tostring(err)
end

local function Monetor_CreateCommandPanel()
    if Monetor_CommandPanel and Monetor_CommandPanel.Parent then return Monetor_CommandPanel end
    Monetor_CommandPanel = Create("Frame", ScreenGui, {
        Name = "Monetor_TitleCommandPanel",
        Size = UDim2.new(0, 245, 0, 142),
        Position = UDim2.new(0, 12, 0.5, 0), AnchorPoint = Vector2.new(0, 0.5),
        BackgroundColor3 = Configs_HUB.Cor_Hub, BackgroundTransparency = 0.08,
        BorderSizePixel = 0, Active = true, Draggable = true,
        Visible = false, ZIndex = 80
    })
    Corner(Monetor_CommandPanel); Stroke(Monetor_CommandPanel)
    Create("TextLabel", Monetor_CommandPanel, {
        Size = UDim2.new(1, -44, 0, 28), Position = UDim2.new(0, 12, 0, 7),
        BackgroundTransparency = 1, Text = "تنفيذ الأوامر",
        TextColor3 = Configs_HUB.Cor_Text, Font = Configs_HUB.Text_Font,
        TextSize = 18, TextXAlignment = Enum.TextXAlignment.Left, ZIndex = 81
    })
    local close = Create("TextButton", Monetor_CommandPanel, {
        Size = UDim2.new(0, 30, 0, 28), Position = UDim2.new(1, -8, 0, 7),
        AnchorPoint = Vector2.new(1, 0), BackgroundTransparency = 1,
        Text = "×", TextColor3 = Configs_HUB.Cor_Text,
        Font = Configs_HUB.Text_Font, TextSize = 24,
        AutoButtonColor = false, ZIndex = 82
    })
    close.Activated:Connect(function()
        Monetor_CommandPanel.Visible = false
        Monetor_ChatEnabled = false
        Monetor_SetCommandStatus("متوقف", false)
    end)
    Monetor_CommandInput = Create("TextBox", Monetor_CommandPanel, {
        Size = UDim2.new(1, -86, 0, 38), Position = UDim2.new(0, 12, 0, 58),
        BackgroundColor3 = Configs_HUB.Cor_Options, BackgroundTransparency = 0.1,
        BorderSizePixel = 0, Text = "", PlaceholderText = "اكتب النص هنا...",
        PlaceholderColor3 = Color3.fromRGB(150, 150, 150),
        TextColor3 = Configs_HUB.Cor_Text, Font = Configs_HUB.Text_Font,
        TextSize = 14, TextXAlignment = Enum.TextXAlignment.Right,
        ClearTextOnFocus = false, ZIndex = 82
    })
    Corner(Monetor_CommandInput); Stroke(Monetor_CommandInput)
    local execute = Create("TextButton", Monetor_CommandPanel, {
        Size = UDim2.new(0, 62, 0, 38), Position = UDim2.new(1, -12, 0, 58),
        AnchorPoint = Vector2.new(1, 0), BackgroundColor3 = Configs_HUB.Cor_Options,
        BorderSizePixel = 0, Text = "تنفيذ", TextColor3 = Configs_HUB.Cor_Text,
        Font = Configs_HUB.Text_Font, TextSize = 13, AutoButtonColor = false, ZIndex = 82
    })
    Corner(execute); Stroke(execute)
    Monetor_CommandStatus = Create("TextLabel", Monetor_CommandPanel, {
        Size = UDim2.new(1, -24, 0, 24), Position = UDim2.new(0, 12, 1, -28),
        BackgroundTransparency = 1, Text = "جاهز",
        TextColor3 = Color3.fromRGB(180, 255, 180),
        Font = Configs_HUB.Text_Font, TextSize = 11,
        TextXAlignment = Enum.TextXAlignment.Left, ZIndex = 81
    })
    local function SendTitleText()
        if Monetor_CommandBusy then return end
        local value = tostring(Monetor_CommandInput.Text or ""):gsub("[%c]", ""):match("^%s*(.-)%s*$") or ""
        if value == "" then Monetor_SetCommandStatus("اكتب نصًا أولاً", true) return end
        if #value > 80 then value = value:sub(1, 80) Monetor_CommandInput.Text = value end
        Monetor_CommandBusy = true
        Monetor_SetCommandStatus("جاري الارسال...", false)
        local success, route = Monetor_ExecuteTitleCommand(value)
        if success then
            Monetor_CommandInput.Text = ""
            Monetor_SetCommandStatus("تم الارسال بنجاح", false)
        else
            Monetor_SetCommandStatus(route or "تعذر الوصول", true)
        end
        task.delay(0.35, function() Monetor_CommandBusy = false end)
    end
    execute.Activated:Connect(SendTitleText)
    Monetor_CommandInput.FocusLost:Connect(function(enterPressed)
        if enterPressed then SendTitleText() end
    end)
    return Monetor_CommandPanel
end

local function Monetor_ToggleCommandPanel(forceState)
    Monetor_CreateCommandPanel()
    if forceState == nil then
        Monetor_CommandPanel.Visible = not Monetor_CommandPanel.Visible
    else
        Monetor_CommandPanel.Visible = forceState
    end
    Monetor_ChatEnabled = Monetor_CommandPanel.Visible
    Monetor_SetCommandStatus(Monetor_CommandPanel.Visible and "جاهز" or "متوقف", false)
end

AddButton(MainTab, { Name = "️ تنفيذ الأوامر", Callback = function() Monetor_ToggleCommandPanel() end })
AddButton(LocalPlayerTab, {
    Name = " الشات — تشغيل / إيقاف",
    Callback = function()
        Monetor_ToggleCommandPanel()
        MakeNotifi({
            Title = "الشات الخاص",
            Text = Monetor_ChatEnabled and "تم تشغيل لوحة الإدخال" or "تم إيقاف لوحة الإدخال",
            Time = 2
        })
    end
})

-- =============================================
-- MAIN TAB BUTTONS
-- =============================================
AddButton(MainTab, { Name = " فك حماية الطيران", Callback = function() FullBypass() end })

MainFlyButton = AddButton(MainTab, {
    Name = "️ تشغيل وضع طيران",
    Callback = function() if Flying then stopFly() else startFly() end end
})

AddButton(MainTab, {
    Name = "️ إظهار لوحة الطيران",
    Callback = function()
        FlyPanelCreated = true
        FlyPanel.Visible = true
        UpdateFlyPanel()
        MakeNotifi({Title="Fly", Text="تم إظهار اللوحة", Time=2})
    end
})

AddButton(MainTab, {
    Name = " زيادة سرعة الطيران (+50)",
    Callback = function()
        FlySpeed = FlySpeed + 50
        UpdateFlyPanel()
        MakeNotifi({Title="Fly", Text="السرعة: "..FlySpeed, Time=2})
    end
})

AddButton(MainTab, {
    Name = " تقليل سرعة الطيران (-50)",
    Callback = function()
        if FlySpeed > 50 then FlySpeed = FlySpeed - 50 end
        UpdateFlyPanel()
        MakeNotifi({Title="Fly", Text="السرعة: "..FlySpeed, Time=2})
    end
})

local RespawnKillDropdown = AddDropdown(MainTab, {
    Name = "اختر الأمر",
    Options = {"رفريش (Respawn)", "موت سريع (Kill)"},
    Default = "رفريش (Respawn)",
    Searchable = false
})

AddButton(MainTab, {
    Name = " تنفيذ الأمر المختار",
    Callback = function()
        local selected = RespawnKillDropdown.GetSelected()
        if selected == "رفريش (Respawn)" then
            pcall(function()
                local hdClient = RepStorage:FindFirstChild("HDAdminHDClient")
                if hdClient then
                    local signals = hdClient:FindFirstChild("Signals")
                    if signals then
                        local rc = signals:FindFirstChild("RequestCommand")
                        if rc then
                            rc:InvokeServer(";refresh")
                            MakeNotifi({Title="أمر", Text="تم ;refresh", Time=2})
                        end
                    end
                end
            end)
        elseif selected == "موت سريع (Kill)" then
            if LP.Character and LP.Character:FindFirstChild("Humanoid") then
                LP.Character.Humanoid.Health = 0
                MakeNotifi({Title="أمر", Text="تم القتل", Time=2})
            end
        end
    end
})

-- =============================================
-- LOCAL PLAYER TAB
-- =============================================
local SpeedEnabled = false
local JumpEnabled = false
local NoclipEnabled = false
local InfJumpEnabled = false

AddToggleButton(LocalPlayerTab, {
    Name = "سرعة عالية", StartState = false,
    OnEnable = function() SpeedEnabled = true MakeNotifi({Title="Player", Text="سرعة عالية On", Time=2}) end,
    OnDisable = function()
        SpeedEnabled = false
        if LP.Character and LP.Character:FindFirstChild("Humanoid") then LP.Character.Humanoid.WalkSpeed = 16 end
        MakeNotifi({Title="Player", Text="سرعة عادية", Time=2})
    end
})

AddToggleButton(LocalPlayerTab, {
    Name = "قفز عالي", StartState = false,
    OnEnable = function() JumpEnabled = true MakeNotifi({Title="Player", Text="قفز عالي On", Time=2}) end,
    OnDisable = function()
        JumpEnabled = false
        if LP.Character and LP.Character:FindFirstChild("Humanoid") then LP.Character.Humanoid.JumpPower = 50 end
        MakeNotifi({Title="Player", Text="قفز عادي", Time=2})
    end
})

AddToggleButton(LocalPlayerTab, {
    Name = "Noclip", StartState = false,
    OnEnable = function() NoclipEnabled = true MakeNotifi({Title="Player", Text="Noclip On", Time=2}) end,
    OnDisable = function() NoclipEnabled = false MakeNotifi({Title="Player", Text="Noclip Off", Time=2}) end
})

AddToggleButton(LocalPlayerTab, {
    Name = "قفز لا نهائي", StartState = false,
    OnEnable = function() InfJumpEnabled = true MakeNotifi({Title="Player", Text="Inf Jump On", Time=2}) end,
    OnDisable = function() InfJumpEnabled = false MakeNotifi({Title="Player", Text="Inf Jump Off", Time=2}) end
})

RunService.Stepped:Connect(function()
    if LP.Character and LP.Character:FindFirstChild("Humanoid") then
        if SpeedEnabled then LP.Character.Humanoid.WalkSpeed = 100 end
        if JumpEnabled then LP.Character.Humanoid.JumpPower = 150 end
    end
    if NoclipEnabled and LP.Character then
        for _, v in pairs(LP.Character:GetDescendants()) do
            if v:IsA("BasePart") then v.CanCollide = false end
        end
    end
end)

UserInputService.JumpRequest:Connect(function()
    if InfJumpEnabled and LP.Character and LP.Character:FindFirstChild("Humanoid") then
        LP.Character.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
    end
end)

-- =============================================
-- ESP TAB
-- =============================================
local ESP_Active = false
local ESP_ShowNames = true
local ESP_ShowBody = true
local ESP_ShowHealth = true
local ESP_ShowDistance = true
local ESP_MaxDistance = 250
local ESP_UpdateInterval = 0.08
local ESP_TARGET_DISTANCE = 1000000000
local ESP_FOLDER_NAME = "Monetor_ESP_RADAR"
local ESP_TargetUserId = nil
local ESP_TargetName = nil
local ESP_TargetOnline = false
local ESP_TargetDropdown = nil

local function GetESPColor(player)
    local ok, color = pcall(function()
        if player.Team and player.Team.TeamColor then return player.Team.TeamColor.Color end
        return Color3.fromRGB(0, 170, 255)
    end)
    return ok and color or Color3.fromRGB(0, 170, 255)
end

local function GetCharacterRoot(character)
    return character and (character:FindFirstChild("HumanoidRootPart")
        or character:FindFirstChild("UpperTorso")
        or character:FindFirstChild("Torso"))
end

local function IsTarget(player)
    return player and ESP_TargetUserId ~= nil and player.UserId == ESP_TargetUserId
end

local function GetOrCreateESPFolder(character)
    if not character then return nil end
    local folder = character:FindFirstChild(ESP_FOLDER_NAME)
    if folder and folder:IsA("Folder") then return folder end
    if folder then pcall(function() folder:Destroy() end) end
    folder = Instance.new("Folder")
    folder.Name = ESP_FOLDER_NAME
    folder.Parent = character
    return folder
end

local function RemoveESP(player)
    if not player or not player.Character then return end
    local folder = player.Character:FindFirstChild(ESP_FOLDER_NAME)
    if folder then pcall(function() folder:Destroy() end) end
end

local function ClearAllESP()
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LP then RemoveESP(player) end
    end
end

local function UpdateNameVisual(player, billboard, distance)
    if not player or not billboard then return end
    local card = billboard:FindFirstChild("Card")
    if not card then return end
    local nameLabel = card:FindFirstChild("PlayerName")
    local infoLabel = card:FindFirstChild("Info")
    local targetBadge = card:FindFirstChild("TargetBadge")
    local humanoid = player.Character and player.Character:FindFirstChildOfClass("Humanoid")
    local target = IsTarget(player)
    local baseColor = target and Color3.fromRGB(255, 55, 55) or GetESPColor(player)
    if nameLabel then
        nameLabel.Text = player.Name
        nameLabel.TextColor3 = target and Color3.fromRGB(255, 85, 85) or Color3.fromRGB(255, 255, 255)
        local accent = nameLabel:FindFirstChild("NameStroke")
        if accent and accent:IsA("UIStroke") then accent.Color = baseColor end
    end
    if targetBadge then
        targetBadge.Visible = target
        if target then targetBadge.Text = "TARGET" end
    end
    if infoLabel then
        local infoParts = {}
        if ESP_ShowHealth and humanoid then
            table.insert(infoParts, "HP " .. math.max(0, math.floor(humanoid.Health + 0.5)))
        end
        if ESP_ShowDistance and distance and distance < math.huge then
            table.insert(infoParts, math.floor(distance + 0.5) .. "m")
        end
        infoLabel.Text = table.concat(infoParts, "  •  ")
        infoLabel.Visible = (#infoParts > 0)
        infoLabel.TextColor3 = target and Color3.fromRGB(255, 180, 180) or Color3.fromRGB(215, 225, 235)
    end
end

local function CreateESPName(player)
    if not ESP_Active or not ESP_ShowNames or player == LP then return end
    local character = player.Character
    if not character then return end
    local head = character:FindFirstChild("Head")
    if not head then return end
    local folder = GetOrCreateESPFolder(character)
    if not folder then return end
    local billboard = folder:FindFirstChild("NameBillboard")
    if billboard and not billboard:IsA("BillboardGui") then
        pcall(function() billboard:Destroy() end)
        billboard = nil
    end
    if not billboard then
        billboard = Instance.new("BillboardGui")
        billboard.Name = "NameBillboard"
        billboard.Adornee = head
        billboard.Parent = folder
        billboard.Size = UDim2.new(0, 150, 0, 38)
        billboard.StudsOffsetWorldSpace = Vector3.new(0, 3.15, 0)
        billboard.AlwaysOnTop = true
        billboard.LightInfluence = 0
        billboard.MaxDistance = ESP_MaxDistance
        billboard.ResetOnSpawn = false
        billboard.Enabled = true
        billboard.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
        local card = Instance.new("Frame")
        card.Name = "Card"
        card.Size = UDim2.fromScale(1, 1)
        card.BackgroundTransparency = 1
        card.BorderSizePixel = 0
        card.Parent = billboard
        local nameLabel = Instance.new("TextLabel")
        nameLabel.Name = "PlayerName"
        nameLabel.Size = UDim2.new(1, -8, 0, 19)
        nameLabel.Position = UDim2.new(0, 4, 0, 0)
        nameLabel.BackgroundTransparency = 1
        nameLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        nameLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
        nameLabel.TextStrokeTransparency = 0.08
        nameLabel.Font = Enum.Font.GothamBold
        nameLabel.TextSize = 14
        nameLabel.TextXAlignment = Enum.TextXAlignment.Center
        nameLabel.TextYAlignment = Enum.TextYAlignment.Center
        nameLabel.Text = player.Name
        nameLabel.Parent = card
        local accent = Instance.new("UIStroke")
        accent.Name = "NameStroke"
        accent.Thickness = 0.9
        accent.Transparency = 0.10
        accent.Parent = nameLabel
        local infoLabel = Instance.new("TextLabel")
        infoLabel.Name = "Info"
        infoLabel.Size = UDim2.new(1, -8, 0, 15)
        infoLabel.Position = UDim2.new(0, 4, 0, 20)
        infoLabel.BackgroundTransparency = 1
        infoLabel.TextColor3 = Color3.fromRGB(215, 225, 235)
        infoLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
        infoLabel.TextStrokeTransparency = 0.25
        infoLabel.Font = Enum.Font.GothamMedium
        infoLabel.TextSize = 11
        infoLabel.TextXAlignment = Enum.TextXAlignment.Center
        infoLabel.TextYAlignment = Enum.TextYAlignment.Center
        infoLabel.Text = ""
        infoLabel.Parent = card
        local targetBadge = Instance.new("TextLabel")
        targetBadge.Name = "TargetBadge"
        targetBadge.Size = UDim2.new(0, 52, 0, 13)
        targetBadge.Position = UDim2.new(1, -54, 0, -7)
        targetBadge.BackgroundTransparency = 0.18
        targetBadge.BackgroundColor3 = Color3.fromRGB(170, 0, 0)
        targetBadge.BorderSizePixel = 0
        targetBadge.TextColor3 = Color3.fromRGB(255, 255, 255)
        targetBadge.TextStrokeTransparency = 0.35
        targetBadge.Font = Enum.Font.GothamBold
        targetBadge.TextSize = 8
        targetBadge.Text = "TARGET"
        targetBadge.Visible = false
        targetBadge.ZIndex = 3
        targetBadge.Parent = card
        local badgeCorner = Instance.new("UICorner")
        badgeCorner.CornerRadius = UDim.new(0, 4)
        badgeCorner.Parent = targetBadge
    end
    billboard.Adornee = head
    billboard.MaxDistance = IsTarget(player) and ESP_TARGET_DISTANCE or ESP_MaxDistance
    billboard.Enabled = ESP_ShowNames
    UpdateNameVisual(player, billboard)
end

local function CreateESPBody(player)
    if not ESP_Active or not ESP_ShowBody or player == LP then return end
    local character = player.Character
    if not character then return end
    local folder = GetOrCreateESPFolder(character)
    if not folder then return end
    local highlight = folder:FindFirstChild("BodyHighlight")
    if highlight and not highlight:IsA("Highlight") then
        pcall(function() highlight:Destroy() end)
        highlight = nil
    end
    if not highlight then
        highlight = Instance.new("Highlight")
        highlight.Name = "BodyHighlight"
        highlight.Adornee = character
        highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
        highlight.FillTransparency = 0.84
        highlight.OutlineTransparency = 0.03
        highlight.Parent = folder
    end
    local color = IsTarget(player) and Color3.fromRGB(255, 40, 40) or GetESPColor(player)
    highlight.FillColor = color
    highlight.OutlineColor = color
    highlight.Enabled = true
end

local function CreateESP(player)
    if player == LP or not ESP_Active then return end
    if not player.Character then return end
    CreateESPBody(player)
    CreateESPName(player)
end

local function RefreshTargetVisuals()
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LP and player.Character then
            local folder = player.Character:FindFirstChild(ESP_FOLDER_NAME)
            if folder then
                local billboard = folder:FindFirstChild("NameBillboard")
                if billboard and billboard:IsA("BillboardGui") then
                    billboard.MaxDistance = IsTarget(player) and ESP_TARGET_DISTANCE or ESP_MaxDistance
                    billboard.Enabled = ESP_Active and ESP_ShowNames
                    UpdateNameVisual(player, billboard)
                end
                local highlight = folder:FindFirstChild("BodyHighlight")
                if highlight and highlight:IsA("Highlight") then
                    local color = IsTarget(player) and Color3.fromRGB(255, 40, 40) or GetESPColor(player)
                    highlight.FillColor = color
                    highlight.OutlineColor = color
                    highlight.Enabled = ESP_Active and ESP_ShowBody
                end
            end
        end
    end
end

local function GetPlayersSortedByDistance()
    local list = {}
    local myCharacter = LP.Character
    local myRoot = myCharacter and myCharacter:FindFirstChild("HumanoidRootPart")
    local myPosition = myRoot and myRoot.Position
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LP then
            local distance = math.huge
            local character = player.Character
            local root = character and character:FindFirstChild("HumanoidRootPart")
            if myPosition and root then distance = (root.Position - myPosition).Magnitude end
            table.insert(list, {player = player, distance = distance})
        end
    end
    table.sort(list, function(a, b)
        if a.distance == b.distance then return a.player.Name:lower() < b.player.Name:lower() end
        return a.distance < b.distance
    end)
    return list
end

local function UpdateRadarTargetList()
    local options = {}
    for _, entry in ipairs(GetPlayersSortedByDistance()) do
        table.insert(options, entry.player.Name)
    end
    if #options == 0 then options = {"لا يوجد لاعبين"} end
    if ESP_TargetDropdown then
        pcall(function() ESP_TargetDropdown:Update(options) end)
    end
end

AddToggleButton(ESPTab, {
    Name = "الرادار المطور", StartState = false,
    OnEnable = function()
        ESP_Active = true
        for _, player in ipairs(Players:GetPlayers()) do
            if player ~= LP then CreateESP(player) end
        end
        RefreshTargetVisuals()
        MakeNotifi({Title="ESP", Text="الرادار On", Time=3})
    end,
    OnDisable = function()
        ESP_Active = false
        ClearAllESP()
        MakeNotifi({Title="ESP", Text="الرادار Off", Time=2})
    end
})

AddToggleButton(ESPTab, {
    Name = "الأسماء", StartState = true,
    OnEnable = function() ESP_ShowNames = true MakeNotifi({Title="ESP", Text="الأسماء On", Time=2}) end,
    OnDisable = function()
        ESP_ShowNames = false
        for _, player in ipairs(Players:GetPlayers()) do
            if player ~= LP and player.Character then
                local folder = player.Character:FindFirstChild(ESP_FOLDER_NAME)
                local billboard = folder and folder:FindFirstChild("NameBillboard")
                if billboard and billboard:IsA("BillboardGui") then billboard.Enabled = false end
            end
        end
        MakeNotifi({Title="ESP", Text="الأسماء Off", Time=2})
    end
})

AddToggleButton(ESPTab, {
    Name = "الجسم", StartState = true,
    OnEnable = function() ESP_ShowBody = true MakeNotifi({Title="ESP", Text="الجسم On", Time=2}) end,
    OnDisable = function() ESP_ShowBody = false MakeNotifi({Title="ESP", Text="الجسم Off", Time=2}) end
})

AddToggleButton(ESPTab, {
    Name = "الصحة", StartState = true,
    OnEnable = function() ESP_ShowHealth = true MakeNotifi({Title="ESP", Text="الصحة On", Time=2}) end,
    OnDisable = function() ESP_ShowHealth = false MakeNotifi({Title="ESP", Text="الصحة Off", Time=2}) end
})

AddToggleButton(ESPTab, {
    Name = "المسافة", StartState = true,
    OnEnable = function() ESP_ShowDistance = true MakeNotifi({Title="ESP", Text="المسافة On", Time=2}) end,
    OnDisable = function() ESP_ShowDistance = false MakeNotifi({Title="ESP", Text="المسافة Off", Time=2}) end
})

ESP_TargetDropdown = AddDropdown(ESPTab, {
    Name = " اختر المستهدف",
    Options = {"لا يوجد لاعبين"},
    Searchable = true,
    Callback = function(value)
        if value == "لا يوجد لاعبين" then return end
        local targetPlayer = Players:FindFirstChild(value)
        if not targetPlayer or targetPlayer == LP then
            MakeNotifi({Title="Target", Text="غير متوفر", Time=2})
            return
        end
        ESP_TargetUserId = targetPlayer.UserId
        ESP_TargetName = targetPlayer.Name
        ESP_TargetOnline = true
        if ESP_Active then CreateESP(targetPlayer) end
        RefreshTargetVisuals()
        MakeNotifi({Title="Target", Text="تم استهداف " .. targetPlayer.Name, Time=3})
    end
})

AddButton(ESPTab, {
    Name = " تثبيت المستهدف",
    Callback = function()
        RefreshTargetVisuals()
        UpdateRadarTargetList()
        MakeNotifi({Title="Target", Text=ESP_TargetName and ("الحالي: " .. ESP_TargetName) or "لم يتم اختيار", Time=2})
    end
})

AddButton(ESPTab, {
    Name = " إلغاء الاستهداف",
    Callback = function()
        ESP_TargetUserId = nil
        ESP_TargetName = nil
        ESP_TargetOnline = false
        RefreshTargetVisuals()
        MakeNotifi({Title="Target", Text="تم الإلغاء", Time=2})
    end
})

local ESP_DistanceBox = Create("TextBox", ESPTab, {
    Size = UDim2.new(1, 0, 0, 30),
    BackgroundColor3 = Configs_HUB.Cor_Options,
    BackgroundTransparency = 0.25, BorderSizePixel = 0,
    Text = tostring(ESP_MaxDistance),
    PlaceholderText = "مدى الرادار (متر)",
    TextColor3 = Configs_HUB.Cor_Text,
    PlaceholderColor3 = Color3.fromRGB(150, 150, 150),
    TextSize = 13, Font = Configs_HUB.Text_Font,
    ClearTextOnFocus = false, TextXAlignment = Enum.TextXAlignment.Center, ZIndex = 20
})
Corner(ESP_DistanceBox); Stroke(ESP_DistanceBox)

local function ApplyESPDistance(value)
    value = math.floor(tonumber(value) or 250)
    value = math.clamp(value, 10, 5000)
    ESP_MaxDistance = value
    ESP_DistanceBox.Text = tostring(value)
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LP and player.Character then
            local folder = player.Character:FindFirstChild(ESP_FOLDER_NAME)
            local billboard = folder and folder:FindFirstChild("NameBillboard")
            if billboard and billboard:IsA("BillboardGui") then
                billboard.MaxDistance = IsTarget(player) and ESP_TARGET_DISTANCE or ESP_MaxDistance
            end
        end
    end
    MakeNotifi({Title="ESP", Text="مدى الرادار: " .. ESP_MaxDistance .. "م", Time=2})
end

ESP_DistanceBox.FocusLost:Connect(function() ApplyESPDistance(ESP_DistanceBox.Text) end)

AddButton(ESPTab, { Name = " تطبيق مدى الرادار", Callback = function() ApplyESPDistance(ESP_DistanceBox.Text) end })

AddTextLabel(ESPTab, " LITE للموبايل • افتراضي 250 متر • تحديث تلقائي • المستهدف أحمر ودائم")

Players.PlayerAdded:Connect(function(player)
    UpdateRadarTargetList()
    if ESP_TargetUserId and player.UserId == ESP_TargetUserId then
        ESP_TargetName = player.Name
        ESP_TargetOnline = true
        MakeNotifi({Title="Target", Text="رجع المستهدف", Time=4})
    end
    player.CharacterAdded:Connect(function()
        task.wait(0.25)
        if ESP_Active then CreateESP(player) end
    end)
end)

Players.PlayerRemoving:Connect(function(player)
    if IsTarget(player) then
        ESP_TargetOnline = false
        MakeNotifi({Title="Target", Text="خرج المستهدف", Time=5})
    end
    RemoveESP(player)
    UpdateRadarTargetList()
end)

for _, player in ipairs(Players:GetPlayers()) do
    if player ~= LP then
        player.CharacterAdded:Connect(function()
            task.wait(0.25)
            if ESP_Active then CreateESP(player) end
        end)
    end
end

UpdateRadarTargetList()

local ESP_UpdateClock = 0
RunService.Heartbeat:Connect(function(dt)
    if not ESP_Active then return end
    ESP_UpdateClock += dt
    if ESP_UpdateClock < ESP_UpdateInterval then return end
    ESP_UpdateClock = 0
    local myCharacter = LP.Character
    local myRoot = GetCharacterRoot(myCharacter)
    if not myRoot then return end
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LP then
            local character = player.Character
            local folder = character and character:FindFirstChild(ESP_FOLDER_NAME)
            local head = character and character:FindFirstChild("Head")
            if character and head and folder then
                local billboard = folder:FindFirstChild("NameBillboard")
                local root = GetCharacterRoot(character)
                local distance = root and (root.Position - myRoot.Position).Magnitude or math.huge
                local target = IsTarget(player)
                if billboard and billboard:IsA("BillboardGui") then
                    billboard.Adornee = head
                    billboard.MaxDistance = target and ESP_TARGET_DISTANCE or ESP_MaxDistance
                    billboard.Enabled = ESP_ShowNames and (target or distance <= ESP_MaxDistance)
                    UpdateNameVisual(player, billboard, distance)
                end
                local highlight = folder:FindFirstChild("BodyHighlight")
                if highlight and highlight:IsA("Highlight") then
                    local color = target and Color3.fromRGB(255, 40, 40) or GetESPColor(player)
                    highlight.FillColor = color
                    highlight.OutlineColor = color
                    highlight.Enabled = ESP_ShowBody and (target or distance <= ESP_MaxDistance)
                end
            end
        end
    end
end)

-- =============================================
-- COMBAT TAB
-- =============================================
local CombatTab = MakeTab({Name = "قتال ️", TabTitle = true})

local GodMode = false
local ShieldMode = false
local SuperDamage = false
local CombatSpeed = false
local CombatJump = false
local godConnection = nil

local AutoRefreshEnabled = false
local AutoRefreshConnection = nil
local AutoRefreshCharacterConnection = nil
local AutoRefreshHealthConnection = nil
local REFRESH_THRESHOLD = 50
local AutoRefreshTriggered = false
local LastRefreshTime = 0
local REFRESH_COOLDOWN = 0.15

local AutoRefreshOnDeathEnabled = false
local AutoRefreshOnDeathConnection = nil
local AutoRefreshOnDeathCharConnection = nil
local RefreshBusy = false
local DeathRefreshBusy = false

local function GetAutoRefreshHumanoid()
    local character = LP.Character
    if not character then return nil end
    return character:FindFirstChildOfClass("Humanoid")
end

local function SendRefreshCommand()
    if RefreshBusy then return false, "قيد التنفيذ" end
    RefreshBusy = true
    local ok, success, message = pcall(function()
        local hdClient = RepStorage:FindFirstChild("HDAdminHDClient")
        if not hdClient then return false, "HDAdminHDClient غير موجود" end
        local signals = hdClient:FindFirstChild("Signals")
        if not signals then return false, "Signals غير موجود" end
        local rc = signals:FindFirstChild("RequestCommand")
        if not rc or not rc:IsA("RemoteFunction") then return false, "RequestCommand غير موجود" end
        rc:InvokeServer(";refresh")
        return true, "تم ;refresh"
    end)
    RefreshBusy = false
    if not ok then return false, tostring(success) end
    return success == true, message
end

local function CheckHealthAndRefresh()
    if not AutoRefreshEnabled then return end
    local humanoid = GetAutoRefreshHumanoid()
    if not humanoid or humanoid.MaxHealth <= 0 or humanoid.Health <= 0 then
        AutoRefreshTriggered = false
        return
    end
    local healthPercent = (humanoid.Health / humanoid.MaxHealth) * 100
    if healthPercent <= REFRESH_THRESHOLD then
        if not AutoRefreshTriggered and (os.clock() - LastRefreshTime) >= REFRESH_COOLDOWN then
            AutoRefreshTriggered = true
            LastRefreshTime = os.clock()
            local success, message = SendRefreshCommand()
            MakeNotifi({
                Title = success and "Auto Refresh" or "خطأ",
                Text = success and ("تم ;refresh — الدم: " .. math.floor(healthPercent) .. "%") or tostring(message),
                Time = 2
            })
        end
    else
        AutoRefreshTriggered = false
    end
end

local function DisconnectAutoRefreshConnections()
    if AutoRefreshConnection then AutoRefreshConnection:Disconnect() AutoRefreshConnection = nil end
    if AutoRefreshCharacterConnection then AutoRefreshCharacterConnection:Disconnect() AutoRefreshCharacterConnection = nil end
    if AutoRefreshHealthConnection then AutoRefreshHealthConnection:Disconnect() AutoRefreshHealthConnection = nil end
end

local function StartAutoRefreshMonitor()
    DisconnectAutoRefreshConnections()
    AutoRefreshTriggered = false
    LastRefreshTime = 0
    AutoRefreshConnection = RunService.RenderStepped:Connect(CheckHealthAndRefresh)
    AutoRefreshCharacterConnection = LP.CharacterAdded:Connect(function(character)
        AutoRefreshTriggered = false
        local humanoid = character:WaitForChild("Humanoid", 5)
        if humanoid and AutoRefreshEnabled then
            if AutoRefreshHealthConnection then AutoRefreshHealthConnection:Disconnect() end
            AutoRefreshHealthConnection = humanoid.HealthChanged:Connect(CheckHealthAndRefresh)
        end
    end)
    local humanoid = GetAutoRefreshHumanoid()
    if humanoid then
        AutoRefreshHealthConnection = humanoid.HealthChanged:Connect(CheckHealthAndRefresh)
    end
end

local function DisconnectAutoRefreshOnDeathConnections()
    if AutoRefreshOnDeathConnection then AutoRefreshOnDeathConnection:Disconnect() AutoRefreshOnDeathConnection = nil end
    if AutoRefreshOnDeathCharConnection then AutoRefreshOnDeathCharConnection:Disconnect() AutoRefreshOnDeathCharConnection = nil end
end

local function HookAutoRefreshOnDeathToHumanoid(humanoid)
    if AutoRefreshOnDeathConnection then AutoRefreshOnDeathConnection:Disconnect() AutoRefreshOnDeathConnection = nil end
    if not humanoid then return end
    AutoRefreshOnDeathConnection = humanoid.Died:Connect(function()
        if not AutoRefreshOnDeathEnabled then return end
        if DeathRefreshBusy or RefreshBusy then return end
        DeathRefreshBusy = true
        task.spawn(function()
            local success, message = SendRefreshCommand()
            MakeNotifi({
                Title = success and "Death Refresh" or "خطأ",
                Text = success and "تم ;refresh عند الموت" or tostring(message),
                Time = 2
            })
            DeathRefreshBusy = false
        end)
    end)
end

local function StartAutoRefreshOnDeathMonitor()
    DisconnectAutoRefreshOnDeathConnections()
    DeathRefreshBusy = false
    local humanoid = GetAutoRefreshHumanoid()
    if humanoid then HookAutoRefreshOnDeathToHumanoid(humanoid) end
    AutoRefreshOnDeathCharConnection = LP.CharacterAdded:Connect(function(character)
        DeathRefreshBusy = false
        local newHumanoid = character:WaitForChild("Humanoid", 5)
        if newHumanoid and AutoRefreshOnDeathEnabled then HookAutoRefreshOnDeathToHumanoid(newHumanoid) end
    end)
end

AddToggleButton(CombatTab, {
    Name = "رفريش عند الموت", StartState = false,
    OnEnable = function()
        AutoRefreshOnDeathEnabled = true
        StartAutoRefreshOnDeathMonitor()
        MakeNotifi({Title="Death Refresh", Text="On", Time=3})
    end,
    OnDisable = function()
        AutoRefreshOnDeathEnabled = false
        DisconnectAutoRefreshOnDeathConnections()
        MakeNotifi({Title="Death Refresh", Text="Off", Time=2})
    end
})

AddToggleButton(CombatTab, {
    Name = "رفريش تلقائي (عند الدم)", StartState = false,
    OnEnable = function()
        AutoRefreshEnabled = true
        StartAutoRefreshMonitor()
        MakeNotifi({Title="Auto Refresh", Text="On", Time=3})
        CheckHealthAndRefresh()
    end,
    OnDisable = function()
        AutoRefreshEnabled = false
        DisconnectAutoRefreshConnections()
        AutoRefreshTriggered = false
        MakeNotifi({Title="Auto Refresh", Text="Off", Time=2})
    end
})

local function CreateShield()
    if LP.Character then
        local shield = LP.Character:FindFirstChild("Monetor_Shield")
        if not shield then
            shield = Instance.new("ForceField")
            shield.Name = "Monetor_Shield"
            shield.Visible = true
            shield.Parent = LP.Character
        end
    end
end

local function RemoveShield()
    if LP.Character then
        local shield = LP.Character:FindFirstChild("Monetor_Shield")
        if shield then shield:Destroy() end
    end
end

AddToggleButton(CombatTab, {
    Name = "دم حديد", StartState = false,
    OnEnable = function()
        GodMode = true
        if godConnection then godConnection:Disconnect() end
        godConnection = RunService.Heartbeat:Connect(function()
            if LP.Character and LP.Character:FindFirstChild("Humanoid") then
                if LP.Character.Humanoid.Health < LP.Character.Humanoid.MaxHealth then
                    LP.Character.Humanoid.Health = LP.Character.Humanoid.MaxHealth
                end
            end
        end)
        MakeNotifi({Title="قتال", Text="دم حديد On", Time=3})
    end,
    OnDisable = function()
        GodMode = false
        if godConnection then godConnection:Disconnect() godConnection = nil end
        MakeNotifi({Title="قتال", Text="دم حديد Off", Time=2})
    end
})

AddToggleButton(CombatTab, {
    Name = "درع قوي", StartState = false,
    OnEnable = function()
        ShieldMode = true
        CreateShield()
        MakeNotifi({Title="قتال", Text="درع On", Time=3})
    end,
    OnDisable = function()
        ShieldMode = false
        RemoveShield()
        MakeNotifi({Title="قتال", Text="درع Off", Time=2})
    end
})

AddToggleButton(CombatTab, {
    Name = "قوة ضربة سوبر", StartState = false,
    OnEnable = function()
        SuperDamage = true
        if LP.Character then
            for _, tool in ipairs(LP.Character:GetChildren()) do
                if tool:IsA("Tool") and tool:FindFirstChild("Handle") then
                    local handle = tool.Handle
                    if handle:IsA("BasePart") then
                        handle.Touched:Connect(function(hit)
                            if SuperDamage then
                                local humanoid = hit.Parent and hit.Parent:FindFirstChildOfClass("Humanoid")
                                if humanoid and humanoid ~= LP.Character.Humanoid then humanoid.Health = 0 end
                            end
                        end)
                    end
                end
            end
        end
        MakeNotifi({Title="قتال", Text="قوة ضربة On", Time=3})
    end,
    OnDisable = function() SuperDamage = false MakeNotifi({Title="قتال", Text="قوة ضربة Off", Time=2}) end
})

AddToggleButton(CombatTab, {
    Name = "سرعة قتالية", StartState = false,
    OnEnable = function()
        CombatSpeed = true
        if LP.Character and LP.Character:FindFirstChild("Humanoid") then LP.Character.Humanoid.WalkSpeed = 150 end
        MakeNotifi({Title="قتال", Text="سرعة 150", Time=3})
    end,
    OnDisable = function()
        CombatSpeed = false
        if LP.Character and LP.Character:FindFirstChild("Humanoid") then LP.Character.Humanoid.WalkSpeed = 16 end
        MakeNotifi({Title="قتال", Text="سرعة عادية", Time=2})
    end
})

AddToggleButton(CombatTab, {
    Name = "قفز خارق", StartState = false,
    OnEnable = function()
        CombatJump = true
        if LP.Character and LP.Character:FindFirstChild("Humanoid") then LP.Character.Humanoid.JumpPower = 300 end
        MakeNotifi({Title="قتال", Text="قفز خارق On", Time=3})
    end,
    OnDisable = function()
        CombatJump = false
        if LP.Character and LP.Character:FindFirstChild("Humanoid") then LP.Character.Humanoid.JumpPower = 50 end
        MakeNotifi({Title="قتال", Text="قفز عادي", Time=2})
    end
})

RunService.Stepped:Connect(function()
    if LP.Character and LP.Character:FindFirstChild("Humanoid") then
        if CombatSpeed then LP.Character.Humanoid.WalkSpeed = 150 end
        if CombatJump then LP.Character.Humanoid.JumpPower = 300 end
    end
end)

LP.CharacterAdded:Connect(function()
    task.wait(0.5)
    if ShieldMode then CreateShield() end
end)

-- =============================================
-- تطوير (TATWIR)
-- =============================================
local TATWIR_Active = false
local TATWIR_SpinSpeed = 200
local TATWIR_SpeedBox = nil
local TATWIR_Toggle = nil
local TATWIR_SpinConnection = nil
local TATWIR_TouchConnection = nil
local TATWIR_BodyAngular = nil

local function TATWIR_Cleanup()
    TATWIR_Active = false
    if TATWIR_SpinConnection then TATWIR_SpinConnection:Disconnect() TATWIR_SpinConnection = nil end
    if TATWIR_TouchConnection then TATWIR_TouchConnection:Disconnect() TATWIR_TouchConnection = nil end
    if TATWIR_BodyAngular then pcall(function() TATWIR_BodyAngular:Destroy() end) TATWIR_BodyAngular = nil end
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= LP and plr.Character then
            for _, part in ipairs(plr.Character:GetDescendants()) do
                if part:IsA("BasePart") then
                    pcall(function()
                        if part.Name == "HumanoidRootPart" then part.CanCollide = false
                        else part.CanCollide = true end
                    end)
                end
            end
        end
    end
    if LP.Character then
        local myRoot = LP.Character:FindFirstChild("HumanoidRootPart")
        if myRoot then
            local oldBV = myRoot:FindFirstChild("Tatwir_Spin")
            if oldBV then pcall(function() oldBV:Destroy() end) end
        end
    end
end

local function TATWIR_LaunchPlayer(targetChar)
    if not targetChar or targetChar == LP.Character then return end
    local targetRoot = targetChar:FindFirstChild("HumanoidRootPart") or targetChar:FindFirstChild("Torso")
    if not targetRoot then return end
    local oldBV = targetRoot:FindFirstChild("Tatwir_Launch")
    if oldBV then pcall(function() oldBV:Destroy() end) end
    local bv = Instance.new("BodyVelocity")
    bv.Name = "Tatwir_Launch"
    bv.MaxForce = Vector3.new(9e9, 9e9, 9e9)
    bv.Velocity = Vector3.new(0, 250, 0) + Vector3.new(math.random(-50, 50), 0, math.random(-50, 50))
    bv.Parent = targetRoot
    task.delay(0.35, function() if bv and bv.Parent then bv:Destroy() end end)
end

local function TATWIR_Start()
    if TATWIR_Active then return end
    local char = LP.Character
    if not char then return end
    local myRoot = char:FindFirstChild("HumanoidRootPart")
    local myHum = char:FindFirstChildOfClass("Humanoid")
    if not myRoot or not myHum then return end
    TATWIR_Active = true
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= LP and plr.Character then
            for _, part in ipairs(plr.Character:GetDescendants()) do
                if part:IsA("BasePart") then pcall(function() part.CanCollide = true end) end
            end
        end
    end
    local oldSpin = myRoot:FindFirstChild("Tatwir_Spin")
    if oldSpin then pcall(function() oldSpin:Destroy() end) end
    TATWIR_BodyAngular = Instance.new("BodyAngularVelocity")
    TATWIR_BodyAngular.Name = "Tatwir_Spin"
    TATWIR_BodyAngular.MaxTorque = Vector3.new(9e9, 9e9, 9e9)
    TATWIR_BodyAngular.AngularVelocity = Vector3.new(0, math.rad(TATWIR_SpinSpeed), 0)
    TATWIR_BodyAngular.P = 9e9
    TATWIR_BodyAngular.Parent = myRoot
    TATWIR_TouchConnection = myRoot.Touched:Connect(function(hit)
        if not TATWIR_Active then return end
        local model = hit:FindFirstAncestorOfClass("Model")
        if not model or model == char then return end
        if not model:FindFirstChildOfClass("Humanoid") then return end
        local plr = Players:GetPlayerFromCharacter(model)
        if not plr or plr == LP then return end
        TATWIR_LaunchPlayer(model)
    end)
    TATWIR_SpinConnection = RunService.Heartbeat:Connect(function()
        if not TATWIR_Active then return end
        if TATWIR_BodyAngular and TATWIR_BodyAngular.Parent then
            TATWIR_BodyAngular.AngularVelocity = Vector3.new(0, math.rad(TATWIR_SpinSpeed), 0)
        end
    end)
    MakeNotifi({Title = "تطوير", Text = "On", Time = 3})
end

local function TATWIR_Stop()
    if not TATWIR_Active then return end
    TATWIR_Cleanup()
    MakeNotifi({Title="تطوير", Text="Off", Time=2})
end

TATWIR_Toggle = AddToggleButton(CombatTab, {
    Name = "تطوير", StartState = false,
    OnEnable = function() TATWIR_Start() end,
    OnDisable = function() TATWIR_Stop() end
})

TATWIR_SpeedBox = Create("TextBox", CombatTab, {
    Size = UDim2.new(1, 0, 0, 30), BackgroundColor3 = Configs_HUB.Cor_Options,
    BackgroundTransparency = 0.25, BorderSizePixel = 0,
    Text = tostring(TATWIR_SpinSpeed),
    PlaceholderText = "سرعة الدوران",
    TextColor3 = Configs_HUB.Cor_Text,
    PlaceholderColor3 = Color3.fromRGB(150, 150, 150),
    TextSize = 13, Font = Configs_HUB.Text_Font,
    ClearTextOnFocus = false, TextXAlignment = Enum.TextXAlignment.Center, ZIndex = 20
})
Corner(TATWIR_SpeedBox); Stroke(TATWIR_SpeedBox)

local function TATWIR_ApplySpeed()
    local v = tonumber(TATWIR_SpeedBox.Text) or TATWIR_SpinSpeed
    v = math.clamp(math.floor(v), 0, 100000)
    TATWIR_SpinSpeed = v
    TATWIR_SpeedBox.Text = tostring(v)
    if TATWIR_BodyAngular then
        TATWIR_BodyAngular.AngularVelocity = Vector3.new(0, math.rad(v), 0)
    end
    MakeNotifi({Title="تطوير", Text="السرعة: " .. v, Time=2})
end

TATWIR_SpeedBox.FocusLost:Connect(function() TATWIR_ApplySpeed() end)
AddButton(CombatTab, { Name = " تطبيق سرعة الدوران", Callback = function() TATWIR_ApplySpeed() end })
AddButton(CombatTab, {
    Name = " زيادة سرعة (+50)",
    Callback = function()
        TATWIR_SpinSpeed = TATWIR_SpinSpeed + 50
        TATWIR_SpeedBox.Text = tostring(TATWIR_SpinSpeed)
        if TATWIR_BodyAngular then TATWIR_BodyAngular.AngularVelocity = Vector3.new(0, math.rad(TATWIR_SpinSpeed), 0) end
        MakeNotifi({Title="تطوير", Text="السرعة: " .. TATWIR_SpinSpeed, Time=1})
    end
})
AddButton(CombatTab, {
    Name = " تقليل سرعة (-50)",
    Callback = function()
        TATWIR_SpinSpeed = math.max(0, TATWIR_SpinSpeed - 50)
        TATWIR_SpeedBox.Text = tostring(TATWIR_SpinSpeed)
        if TATWIR_BodyAngular then TATWIR_BodyAngular.AngularVelocity = Vector3.new(0, math.rad(TATWIR_SpinSpeed), 0) end
        MakeNotifi({Title="تطوير", Text="السرعة: " .. TATWIR_SpinSpeed, Time=1})
    end
})

LP.CharacterAdded:Connect(function()
    if TATWIR_Active then
        TATWIR_Stop()
        if TATWIR_Toggle and TATWIR_Toggle.SetState then TATWIR_Toggle.SetState(false) end
    end
end)

-- =============================================
-- TARGET STICK (BanHammer)
-- =============================================
local Target_Enabled = false
local Target_UserId = nil
local Target_Name = nil
local Target_Offset = 3
local Target_Connection = nil
local Target_Dropdown = nil
local Target_OffsetBox = nil
local Target_ToggleObj = nil
local Target_Attacking = false
local TOOL_NAME = "BanHammer"

local function HasBanHammer()
    local backpack = LP:FindFirstChild("Backpack")
    if backpack and backpack:FindFirstChild(TOOL_NAME) then
        return true, backpack:FindFirstChild(TOOL_NAME), "Backpack"
    end
    local character = LP.Character
    if character then
        local tool = character:FindFirstChild(TOOL_NAME)
        if tool and tool:IsA("Tool") then return true, tool, "Character" end
    end
    return false, nil, nil
end

local function EquipTool(tool)
    if not tool then return false end
    local character = LP.Character
    if not character then return false end
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if not humanoid then return false end
    if tool.Parent == character then return true end
    pcall(function() humanoid:EquipTool(tool) end)
    return true
end

local function GetTargetPlayer()
    if not Target_UserId then return nil end
    for _, p in ipairs(Players:GetPlayers()) do
        if p.UserId == Target_UserId then return p end
    end
    return nil
end

local function StopTarget()
    Target_Enabled = false
    Target_Attacking = false
    if Target_Connection then Target_Connection:Disconnect() Target_Connection = nil end
    local ok, tool = HasBanHammer()
    if ok and tool then
        pcall(function()
            if tool:FindFirstChild("Deactivate") then tool.Deactivate(tool) end
        end)
    end
    if LP.Character then
        local hum = LP.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum.WalkSpeed = 16 end
    end
end

local function AttackTwice(tool)
    if not tool then return end
    if Target_Attacking then return end
    Target_Attacking = true
    task.spawn(function()
        pcall(function() if tool:FindFirstChild("Activate") then tool:Activate() end end)
        task.wait(1)
        pcall(function() if tool:FindFirstChild("Activate") then tool:Activate() end end)
        Target_Attacking = false
    end)
end

local function StartTargetLoop()
    if Target_Connection then Target_Connection:Disconnect() end
    Target_Connection = RunService.Heartbeat:Connect(function()
        if not Target_Enabled then return end
        local target = GetTargetPlayer()
        if not target or not target.Character then return end
        local targetRoot = target.Character:FindFirstChild("HumanoidRootPart")
            or target.Character:FindFirstChild("UpperTorso")
            or target.Character:FindFirstChild("Torso")
        if not targetRoot then return end
        local targetHum = target.Character:FindFirstChildOfClass("Humanoid")
        if not targetHum or targetHum.Health <= 0 then
            StopTarget()
            MakeNotifi({Title="Target", Text="مات المستهدف", Time=3})
            if Target_ToggleObj and Target_ToggleObj.SetState then Target_ToggleObj.SetState(false) end
            return
        end
        local myChar = LP.Character
        if not myChar then return end
        local myRoot = myChar:FindFirstChild("HumanoidRootPart")
        local myHum = myChar:FindFirstChildOfClass("Humanoid")
        if not myRoot or not myHum or myHum.Health <= 0 then return end
        local targetLook = targetRoot.CFrame.LookVector
        myRoot.CFrame = CFrame.new(targetRoot.Position + targetLook * Target_Offset, targetRoot.Position)
        local hasTool, tool = HasBanHammer()
        if hasTool and tool and tool.Parent ~= myChar then EquipTool(tool) end
        if hasTool and tool and not Target_Attacking then AttackTwice(tool) end
    end)
end

local function UpdateTargetDropdown()
    local options = {}
    for _, entry in ipairs(GetPlayersSortedByDistance()) do
        table.insert(options, entry.player.Name)
    end
    if #options == 0 then options = {"لا يوجد لاعبين"} end
    if Target_Dropdown then
        pcall(function() Target_Dropdown:Update(options) end)
    end
end

Target_ToggleObj = AddToggleButton(CombatTab, {
    Name = " استهداف الشخص (تصاق + ضرب)", StartState = false,
    OnEnable = function()
        local hasTool = HasBanHammer()
        if not hasTool then
            MakeNotifi({Title = "Target", Text = "لازم BanHammer بحقيبتك!", Time = 4})
            if Target_ToggleObj and Target_ToggleObj.SetState then Target_ToggleObj.SetState(false) end
            return
        end
        if not Target_UserId then
            MakeNotifi({Title="Target", Text="اختر لاعب أولاً", Time=3})
            if Target_ToggleObj and Target_ToggleObj.SetState then Target_ToggleObj.SetState(false) end
            return
        end
        local target = GetTargetPlayer()
        if not target or not target.Character then
            MakeNotifi({Title="Target", Text="المستهدف غير متوفر", Time=3})
            if Target_ToggleObj and Target_ToggleObj.SetState then Target_ToggleObj.SetState(false) end
            return
        end
        local _, tool = HasBanHammer()
        if tool then EquipTool(tool) end
        Target_Enabled = true
        StartTargetLoop()
        MakeNotifi({Title="Target", Text="On: " .. (Target_Name or "?"), Time=4})
    end,
    OnDisable = function()
        StopTarget()
        MakeNotifi({Title="Target", Text="Off", Time=2})
    end
})

Target_Dropdown = AddDropdown(CombatTab, {
    Name = " اختر لاعب للاستهداف",
    Options = {"لا يوجد لاعبين"},
    Searchable = true,
    Callback = function(value)
        if value == "لا يوجد لاعبين" then return end
        local targetPlayer = Players:FindFirstChild(value)
        if not targetPlayer or targetPlayer == LP then
            MakeNotifi({Title="Target", Text="غير متوفر", Time=2})
            return
        end
        Target_UserId = targetPlayer.UserId
        Target_Name = targetPlayer.Name
        MakeNotifi({Title="Target", Text="تم اختيار: " .. targetPlayer.Name, Time=3})
    end
})

Target_OffsetBox = Create("TextBox", CombatTab, {
    Size = UDim2.new(1, 0, 0, 30), BackgroundColor3 = Configs_HUB.Cor_Options,
    BackgroundTransparency = 0.25, BorderSizePixel = 0,
    Text = tostring(Target_Offset), PlaceholderText = "الفاصل (افتراضي 3)",
    TextColor3 = Configs_HUB.Cor_Text,
    PlaceholderColor3 = Color3.fromRGB(150, 150, 150),
    TextSize = 13, Font = Configs_HUB.Text_Font,
    ClearTextOnFocus = false, TextXAlignment = Enum.TextXAlignment.Center, ZIndex = 20
})
Corner(Target_OffsetBox); Stroke(Target_OffsetBox)

local function ApplyTargetOffset()
    local v = tonumber(Target_OffsetBox.Text) or Target_Offset
    v = math.clamp(v, 0.5, 50)
    Target_Offset = v
    Target_OffsetBox.Text = tostring(v)
    MakeNotifi({Title="Target", Text="الفاصل: " .. v, Time=2})
end

Target_OffsetBox.FocusLost:Connect(function() ApplyTargetOffset() end)
AddButton(CombatTab, { Name = " تطبيق الفاصل", Callback = function() ApplyTargetOffset() end })
AddButton(CombatTab, {
    Name = " زيادة الفاصل (+1)",
    Callback = function()
        Target_Offset = Target_Offset + 1
        Target_OffsetBox.Text = tostring(Target_Offset)
        MakeNotifi({Title="Target", Text="الفاصل: " .. Target_Offset, Time=1})
    end
})
AddButton(CombatTab, {
    Name = " تقليل الفاصل (-1)",
    Callback = function()
        Target_Offset = math.max(0.5, Target_Offset - 1)
        Target_OffsetBox.Text = tostring(Target_Offset)
        MakeNotifi({Title="Target", Text="الفاصل: " .. Target_Offset, Time=1})
    end
})
AddButton(CombatTab, {
    Name = " فحص BanHammer",
    Callback = function()
        local has, tool, loc = HasBanHammer()
        if has then MakeNotifi({Title="BanHammer", Text="موجود في: " .. tostring(loc), Time=3})
        else MakeNotifi({Title="BanHammer", Text="❌ غير موجود", Time=4}) end
    end
})

AddTextLabel(CombatTab, "الاستخدام: 1) اختر لاعب 2) تأكد من BanHammer 3) فعّل الزر")

Players.PlayerAdded:Connect(function()
    task.wait(0.5)
    UpdateTargetDropdown()
end)

Players.PlayerRemoving:Connect(function(player)
    if player.UserId == Target_UserId then
        StopTarget()
        if Target_ToggleObj and Target_ToggleObj.SetState then Target_ToggleObj.SetState(false) end
    end
    task.wait(0.3)
    UpdateTargetDropdown()
end)

UpdateTargetDropdown()

-- =============================================
-- TELEPORT TAB
-- =============================================
local TPSelectedPlayer = ""

local TPDropdown = AddDropdown(TeleportTab, {
    Name = "اختر لاعب للانتقال",
    Options = {},
    Searchable = true,
    Callback = function(value) TPSelectedPlayer = value end
})

AddButton(TeleportTab, {
    Name = " انتقال للاعب المختار",
    Callback = function()
        if TPSelectedPlayer == "" or TPSelectedPlayer == "لا يوجد لاعبين" then
            MakeNotifi({Title="تنبيه", Text="اختر لاعب", Time=2})
            return
        end
        local targetPlayer = Players:FindFirstChild(TPSelectedPlayer)
        if targetPlayer and targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart") then
            if LP.Character and LP.Character:FindFirstChild("HumanoidRootPart") then
                LP.Character.HumanoidRootPart.CFrame = targetPlayer.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, 3)
                MakeNotifi({Title="Teleport", Text="تم الانتقال", Time=2})
            end
        else
            MakeNotifi({Title="خطأ", Text="غير متوفر", Time=2})
        end
    end
})

-- =============================================
-- TROLL TAB
-- =============================================
local selectedPlayerName = ""
local isInvisActive = false
local invisConnection = nil
local followConnection = nil
local targetDeathConnection = nil
local EscapePosition = Vector3.new(284, 40, 118)

local function StopInvisibilityAndFollow(teleportToSafeSpot)
    isInvisActive = false
    if invisConnection then invisConnection:Disconnect() invisConnection = nil end
    if followConnection then followConnection:Disconnect() followConnection = nil end
    if targetDeathConnection then targetDeathConnection:Disconnect() targetDeathConnection = nil end
    local char = LP.Character
    if char then
        local root = char:FindFirstChild("HumanoidRootPart") or char:FindFirstChild("Torso")
        if teleportToSafeSpot and root then root.CFrame = CFrame.new(EscapePosition) end
        local humanoid = char:FindFirstChildOfClass("Humanoid")
        if humanoid then humanoid.CameraOffset = Vector3.zero end
        workspace.CurrentCamera.CameraSubject = humanoid or root
        for _, v in pairs(char:GetDescendants()) do
            if v:IsA("BasePart") and v.Transparency == 0.5 then v.Transparency = 0 end
        end
    end
end

local function UpdatePlayerList()
    local names = {}
    for _, entry in ipairs(GetPlayersSortedByDistance()) do
        table.insert(names, entry.player.Name)
    end
    if #names == 0 then table.insert(names, "لا يوجد لاعبين") end
    if playerDropdown then pcall(function() playerDropdown:Update(names) end) end
    if TPDropdown then pcall(function() TPDropdown:Update(names) end) end
end

playerDropdown = AddDropdown(TrollTab, {
    Name = "اختر لاعب",
    Options = {},
    Searchable = true,
    Callback = function(value) selectedPlayerName = value end
})

AddButton(TrollTab, {
    Name = "️ اقتل (تتبع + إخفاء)",
    Callback = function()
        if selectedPlayerName == "" or selectedPlayerName == "لا يوجد لاعبين" then
            MakeNotifi({Title="تنبيه", Text="اختر لاعب", Time=3})
            return
        end
        pcall(function()
            local signal = RepStorage:FindFirstChild("HDAdminHDClient")
            if signal then
                signal = signal:FindFirstChild("Signals")
                if signal then
                    signal = signal:FindFirstChild("RequestCommand")
                    if signal then signal:InvokeServer(";r6") end
                end
            end
        end)
        MakeNotifi({Title="Troll", Text="التحول R6...", Time=4})
        task.spawn(function()
            task.wait(3)
            local myChar = LP.Character
            if not myChar or not myChar:FindFirstChild("Torso") then return end
            local targetPlayer = Players:FindFirstChild(selectedPlayerName)
            if not targetPlayer or not targetPlayer.Character then return end
            StopInvisibilityAndFollow(false)
            isInvisActive = true
            local myRoot = myChar:WaitForChild("HumanoidRootPart")
            local myHumanoid = myChar:WaitForChild("Humanoid")
            local targetHumanoid = targetPlayer.Character:FindFirstChildOfClass("Humanoid")
            if targetHumanoid then
                targetDeathConnection = targetHumanoid.Died:Connect(function()
                    StopInvisibilityAndFollow(true)
                    MakeNotifi({Title="تم القتل", Text="انتقل لمكان آمن", Time=5})
                end)
            end
            invisConnection = RunService.Heartbeat:Connect(function()
                if not isInvisActive or not myChar or not myHumanoid or myHumanoid.Health <= 0 then
                    StopInvisibilityAndFollow(false)
                    return
                end
                if workspace.CurrentCamera.CameraSubject ~= myRoot then
                    workspace.CurrentCamera.CameraSubject = myRoot
                end
                myRoot.CFrame = myRoot.CFrame * CFrame.Angles(math.rad(90), 0, 0)
                myHumanoid.CameraOffset = Vector3.new(0, 1.44, 0)
                for _, v in pairs(myChar:GetDescendants()) do
                    if v:IsA("BasePart") and v.Transparency ~= 1 then v.Transparency = 0.5 end
                end
            end)
            followConnection = RunService.RenderStepped:Connect(function()
                if isInvisActive and targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart") then
                    myRoot.CFrame = targetPlayer.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, 1)
                end
            end)
        end)
    end
})

AddButton(TrollTab, {
    Name = " إيقاف",
    Callback = function()
        StopInvisibilityAndFollow(false)
        MakeNotifi({Title="Troll", Text="Off", Time=2})
    end
})

UpdatePlayerList()
Players.PlayerAdded:Connect(UpdatePlayerList)
Players.PlayerRemoving:Connect(UpdatePlayerList)

-- =============================================
-- RIGHTS TAB (مرتب وممتلئ بدون فراغات)
-- =============================================

local RightsHero = Create("Frame", RightsTab, {
    Size = UDim2.new(1, 0, 0, 88),
    BackgroundColor3 = Color3.fromRGB(22, 23, 29),
    BackgroundTransparency = 0,
    BorderSizePixel = 0,
    LayoutOrder = 1
})
Corner(RightsHero, {CornerRadius = UDim.new(0, 10)})
Stroke(RightsHero)

Create("Frame", RightsHero, {
    Size = UDim2.new(0, 5, 0, 58),
    Position = UDim2.new(0, 12, 0.5, -29),
    BackgroundColor3 = Color3.fromRGB(195, 20, 42),
    BorderSizePixel = 0
})
Corner(RightsHero, {CornerRadius = UDim.new(0, 10)})

Create("TextLabel", RightsHero, {
    Size = UDim2.new(1, -42, 0, 24),
    Position = UDim2.new(0, 28, 0, 12),
    BackgroundTransparency = 1,
    Text = "MONETOR X • الحقوق والمعلومات",
    TextColor3 = Color3.fromRGB(255, 255, 255),
    Font = Enum.Font.GothamBold,
    TextSize = 15,
    TextXAlignment = Enum.TextXAlignment.Left
})

Create("TextLabel", RightsHero, {
    Size = UDim2.new(1, -42, 0, 38),
    Position = UDim2.new(0, 28, 0, 38),
    BackgroundTransparency = 1,
    Text = "الحساب الإداري: @sjbcjjs0   •   المطور: Monetor\nVersion 2.0 Full Edition   •   جميع الحقوق محفوظة © 2026",
    TextColor3 = Configs_HUB.Cor_DarkText,
    Font = Enum.Font.Gotham,
    TextSize = 10,
    TextWrapped = true,
    TextXAlignment = Enum.TextXAlignment.Left,
    TextYAlignment = Enum.TextYAlignment.Top
})

AddButton(RightsTab, {
    Name = "طريقة الاستخدام",
    LayoutOrder = 2,
    Callback = function()
        CreateUsagePanel()
    end
})

AddButton(RightsTab, {
    Name = "حقوق المطور والنسخة",
    LayoutOrder = 3,
    Callback = function()
        CreateCreditsPanel()
    end
})

AddButton(RightsTab, {
    Name = "قناة التليجرام",
    LayoutOrder = 4,
    Callback = function()
        pcall(function() if setclipboard then setclipboard("https://t.me/Speed_loder") end end)
        MakeNotifi({Title = "Monetor X", Text = "تم نسخ رابط القناة", Time = 2})
    end
})

AddButton(RightsTab, {
    Name = "حساب التليجرام",
    LayoutOrder = 5,
    Callback = function()
        pcall(function() if setclipboard then setclipboard("@LXEKE") end end)
        MakeNotifi({Title = "Monetor X", Text = "تم نسخ الحساب", Time = 2})
    end
})

AddButton(RightsTab, {
    Name = "حساب تيك توك",
    LayoutOrder = 6,
    Callback = function()
        pcall(function() if setclipboard then setclipboard("@monetor_") end end)
        MakeNotifi({Title = "Monetor X", Text = "تم نسخ الحساب", Time = 2})
    end
})

AddTextLabel(RightsTab,
    "MONETOR X\nPersonal Control Panel\nصنع بواسطة Monetor"
)

-- =============================================
-- FINAL INIT
-- =============================================
task.wait(1.5)
pcall(FullBypass)

task.wait(0.5)
MakeNotifi({
    Title = "Monetor X",
    Text = "تم تفعيل Monetor X بنجاح! استمتع",
    Time = 3
})

print("✅ Monetor X Loaded Successfully! by Monetor")
