-- =============================================
-- MONETOR X (FULL EDITION + AVATAR + USAGE + CREDITS) — FIXED
-- Developed by @sjbcjjs0
-- Fixed by: AI Assistant
-- =============================================

local Players = game:GetService("Players")
local LP = Players.LocalPlayer
local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local RepStorage = game:GetService("ReplicatedStorage")
local LogService = game:GetService("LogService")
local MarketplaceService = game:GetService("MarketplaceService")
local HttpService = game:GetService("HttpService")

local Configs_HUB = {
    Cor_Hub = Color3.fromRGB(15, 15, 15),
    Cor_Options = Color3.fromRGB(30, 30, 30),
    Cor_Stroke = Color3.fromRGB(255, 255, 255),
    Cor_Text = Color3.fromRGB(255, 255, 255),
    Cor_DarkText = Color3.fromRGB(180, 180, 180),
    Corner_Radius = UDim.new(0, 15),
    Text_Font = Enum.Font.FredokaOne
}

-- =============================================
-- HELPER FUNCTIONS
-- =============================================
local function Create(instance, parent, props)
    local new = Instance.new(instance)
    if props then
        for prop, value in pairs(props) do 
            pcall(function() new[prop] = value end)
        end
    end
    new.Parent = parent
    return new
end

local function SetProps(instance, props)
    if instance and props then
        for prop, value in pairs(props) do 
            pcall(function() instance[prop] = value end)
        end
    end
    return instance
end

local function Corner(parent, props)
    local new = Create("UICorner", parent)
    new.CornerRadius = Configs_HUB.Corner_Radius
    if props then SetProps(new, props) end
    return new
end

local function Stroke(parent, props)
    local new = Create("UIStroke", parent)
    new.Color = Configs_HUB.Cor_Stroke
    new.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    new.Thickness = 2
    local gradient = Instance.new("UIGradient")
    gradient.Color = ColorSequence.new{
        ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 255, 255)),
        ColorSequenceKeypoint.new(0.25, Color3.fromRGB(180, 180, 180)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(15, 15, 15)),
        ColorSequenceKeypoint.new(0.75, Color3.fromRGB(90, 90, 90)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 255, 255))
    }
    gradient.Rotation = 0
    gradient.Parent = new
    coroutine.wrap(function()
        while new.Parent do
            pcall(function()
                gradient.Rotation = (gradient.Rotation + 2) % 360
            end)
            RunService.RenderStepped:Wait()
        end
    end)()
    if props then SetProps(new, props) end
    return new
end

local function CreateTween(instance, prop, value, time, tweenWait)
    if not instance or not instance.Parent then return end
    local tween = TweenService:Create(instance,
    TweenInfo.new(time, Enum.EasingStyle.Linear),
    {[prop] = value})
    tween:Play()
    if tweenWait then tween.Completed:Wait() end
end

local function TextSetColor(instance)
    instance.MouseEnter:Connect(function()
        CreateTween(instance, "TextColor3", Color3.fromRGB(180, 180, 180), 0.4, false)
    end)
    instance.MouseLeave:Connect(function()
        CreateTween(instance, "TextColor3", Configs_HUB.Cor_Text, 0.4, false)
    end)
end

-- =============================================
-- SCREEN GUI
-- =============================================
local existing = CoreGui:FindFirstChild("Monetor_Hub")
if existing then existing:Destroy() end

local ScreenGui = Create("ScreenGui", CoreGui, { Name = "Monetor_Hub" })

local ClickSound = Instance.new("Sound", ScreenGui)
ClickSound.SoundId = "rbxassetid://88442833509532"
ClickSound.Volume = 0.5

local function PlaySound() 
    pcall(function() ClickSound:Play() end) 
end

task.spawn(function()
    task.wait(2)
    for _, button in pairs(ScreenGui:GetDescendants()) do
        if button:IsA("TextButton") or button:IsA("ImageButton") then
            pcall(function() button.Activated:Connect(PlaySound) end)
        end
    end
    ScreenGui.DescendantAdded:Connect(function(newButton)
        if newButton:IsA("TextButton") or newButton:IsA("ImageButton") then
            task.wait(0.1)
            pcall(function() newButton.Activated:Connect(PlaySound) end)
        end
    end)
end)

function DestroyScript() 
    pcall(function() ScreenGui:Destroy() end) 
end

-- =============================================
-- NOTIFICATION SYSTEM
-- =============================================
local Menu_Notifi = Create("Frame", ScreenGui, {
    Size = UDim2.new(0, 300, 1, 0),
    Position = UDim2.new(1, 0, 0, 0),
    AnchorPoint = Vector2.new(1, 0),
    BackgroundTransparency = 1
})
Create("UIPadding", Menu_Notifi, { PaddingLeft = UDim.new(0, 25), PaddingTop = UDim.new(0, 25), PaddingBottom = UDim.new(0, 50) })
Create("UIListLayout", Menu_Notifi, { Padding = UDim.new(0, 15), VerticalAlignment = "Bottom" })

function MakeNotifi(Configs)
    local Title = Configs.Title or "Monetor X"
    local text = Configs.Text or "تم التنفيذ"
    local timewait = Configs.Time or 5
    
    local Frame1 = Create("Frame", Menu_Notifi, {
        Size = UDim2.new(2, 0, 0, 0),
        BackgroundTransparency = 1,
        AutomaticSize = "Y",
        Name = "Title"
    })
    local Frame2 = Create("Frame", Frame1, {
        Size = UDim2.new(0, Menu_Notifi.Size.X.Offset - 50, 0, 0),
        BackgroundColor3 = Configs_HUB.Cor_Hub,
        Position = UDim2.new(0, Menu_Notifi.Size.X.Offset, 0, 0),
        AutomaticSize = "Y"
    }) 
    Corner(Frame2); Stroke(Frame2)
    
    Create("TextLabel", Frame2, {
        Size = UDim2.new(1, 0, 0, 25),
        Font = Configs_HUB.Text_Font, BackgroundTransparency = 1,
        Text = Title, TextSize = 20,
        Position = UDim2.new(0, 20, 0, 5),
        TextXAlignment = "Left", TextColor3 = Configs_HUB.Cor_Text
    })
    
    local CloseNotifi = Create("TextButton", Frame2, {
        Text = "X", Font = Configs_HUB.Text_Font, TextSize = 20,
        BackgroundTransparency = 1, TextColor3 = Color3.fromRGB(200, 200, 200),
        Position = UDim2.new(1, -5, 0, 5),
        AnchorPoint = Vector2.new(1, 0), Size = UDim2.new(0, 25, 0, 25)
    })
    
    Create("TextLabel", Frame2, {
        Size = UDim2.new(1, -30, 0, 0),
        Position = UDim2.new(0, 20, 0, CloseNotifi.Size.Y.Offset + 10),
        TextSize = 15, TextColor3 = Configs_HUB.Cor_DarkText,
        TextXAlignment = "Left", TextYAlignment = "Top",
        AutomaticSize = "Y", Text = text,
        Font = Configs_HUB.Text_Font, BackgroundTransparency = 1, TextWrapped = true
    })
    
    local FrameSize = Create("Frame", Frame2, {
        Size = UDim2.new(1, 0, 0, 2), BackgroundColor3 = Configs_HUB.Cor_Stroke,
        Position = UDim2.new(0, 2, 0, 30), BorderSizePixel = 0
    }) 
    Corner(FrameSize)
    Create("Frame", Frame2, { Size = UDim2.new(0, 0, 0, 5), Position = UDim2.new(0, 0, 1, 5), BackgroundTransparency = 1 })
    
    task.spawn(function() CreateTween(FrameSize, "Size", UDim2.new(0, 0, 0, 2), timewait, true) end)
    
    CloseNotifi.MouseButton1Click:Connect(function()
        CreateTween(Frame2, "Position", UDim2.new(0, -20, 0, 0), 0.1, true)
        CreateTween(Frame2, "Position", UDim2.new(0, Menu_Notifi.Size.X.Offset, 0, 0), 0.5, true)
        Frame1:Destroy()
    end)
    
    task.spawn(function()
        CreateTween(Frame2, "Position", UDim2.new(0, -20, 0, 0), 0.5, true)
        CreateTween(Frame2, "Position", UDim2.new(), 0.1, true)
        task.wait(timewait)
        if Frame2 and Frame2.Parent then
            CreateTween(Frame2, "Position", UDim2.new(0, -20, 0, 0), 0.1, true)
            CreateTween(Frame2, "Position", UDim2.new(0, Menu_Notifi.Size.X.Offset, 0, 0), 0.5, true)
            Frame1:Destroy()
        end
    end)
end

-- =============================================
-- BYPASS SYSTEM (FIXED)
-- =============================================
local BypassEnabled = false

-- ✅ إصلاح: pcall حول hookfunction
local function SafeHookFunction(func, newfunc)
    if not func or not newfunc then return func end
    if type(hookfunction) == "function" then
        local ok, result = pcall(hookfunction, func, newfunc)
        if ok and result then return result end
    end
    return func
end

local function FindAllACERemotes()
    local remotes = {}
    local containers = {
        RepStorage, workspace,
        game:GetService("Lighting"), CoreGui,
        game:GetService("StarterGui"),
        LP:FindFirstChild("PlayerGui"), LP:FindFirstChild("Backpack"),
        LP:FindFirstChild("PlayerScripts"),
        LP:FindFirstChild("Character") or workspace:FindFirstChild(LP.Name)
    }
    for _, service in ipairs(game:GetChildren()) do
        if service:IsA("Service") and not table.find(containers, service) then
            table.insert(containers, service)
        end
    end
    for _, container in ipairs(containers) do
        if not container then continue end
        local success, descendants = pcall(function() return container:GetDescendants() end)
        if not success then continue end
        for _, obj in ipairs(descendants) do
            if (obj:IsA("RemoteEvent") or obj:IsA("RemoteFunction")) then
                local name = obj.Name
                if string.find(name, "ACE") or string.find(name:lower(), "ace") then
                    table.insert(remotes, obj)
                end
            end
        end
    end
    return remotes
end

local function ApplyBypassToRemote(remote)
    if not remote then return end
    if remote:IsA("RemoteEvent") then
        local oldFire = remote.FireServer
        if oldFire then
            SafeHookFunction(oldFire, function(self, ...)
                local args = {...}
                local msg = tostring(args[1] or "")
                if msg == "HB" then return oldFire(self, unpack(args)) end
                return nil
            end)
        end
    elseif remote:IsA("RemoteFunction") then
        local oldInvoke = remote.InvokeServer
        if oldInvoke then
            SafeHookFunction(oldInvoke, function(self, ...)
                local args = {...}
                if tostring(args[1]) == "HB" then return "OK" end
                return true
            end)
        end
    end
end

-- ✅ إصلاح: pcall حول debug.getinfo
local function DisableSprintMovementScans()
    local ps = LP:FindFirstChild("PlayerScripts")
    if not ps then return end
    if type(getgc) ~= "function" then return end
    
    local ok, GC = pcall(getgc, true)
    if not ok or not GC then return end
    
    for _, obj in ipairs(GC) do
        if type(obj) == "function" then
            local ok2, info = pcall(debug.getinfo, obj)
            if ok2 and info and info.what == "Lua" and info.source then
                if string.find(info.source, "SprintMovement") then
                    if info.name == "scanForMovementObjects" or info.name == "scanForGuiObjects" then
                        SafeHookFunction(obj, function() return false, nil end)
                    end
                end
            end
        end
    end
end

local function DisableLogs()
    if type(getconnections) ~= "function" then return end
    pcall(function()
        local conns = getconnections(LogService.MessageOut)
        if conns then
            for _, c in ipairs(conns) do 
                pcall(function() c:Disconnect() end)
            end
        end
    end)
end

local function FullBypass()
    if BypassEnabled then
        MakeNotifi({Title = "Monetor X", Text = "الحماية مكسورة مسبقاً", Time = 2})
        return
    end
    local remotes = FindAllACERemotes()
    if #remotes > 0 then
        for _, r in ipairs(remotes) do ApplyBypassToRemote(r) end
    end
    DisableSprintMovementScans()
    DisableLogs()
    BypassEnabled = true
    MakeNotifi({ Title = "Monetor X", Text = "تم فك الحماية بنجاح (" .. #remotes .. " remote)", Time = 2 })
end

local function MonitorNewRemotes()
    local containers = {
        RepStorage, workspace,
        game:GetService("Lighting"), CoreGui,
        game:GetService("StarterGui"),
        LP:FindFirstChild("PlayerGui"), LP:FindFirstChild("Backpack"),
        LP:FindFirstChild("PlayerScripts")
    }
    for _, container in ipairs(containers) do
        if not container then continue end
        pcall(function()
            container.ChildAdded:Connect(function(child)
                if child:IsA("RemoteEvent") or child:IsA("RemoteFunction") then
                    if string.find(child.Name, "ACE") or string.find(child.Name:lower(), "ace") then
                        task.wait(0.5)
                        ApplyBypassToRemote(child)
                    end
                end
            end)
        end)
    end
end

LP.CharacterAdded:Connect(function()
    task.wait(1)
    local char = LP.Character
    if char then
        for _, obj in ipairs(char:GetDescendants()) do
            if (obj:IsA("RemoteEvent") or obj:IsA("RemoteFunction")) and (string.find(obj.Name, "ACE") or string.find(obj.Name:lower(), "ace")) then
                ApplyBypassToRemote(obj)
            end
        end
    end
    DisableSprintMovementScans()
end)

local ps_ref = LP:FindFirstChild("PlayerScripts")
if ps_ref then
    ps_ref.ChildAdded:Connect(function(child)
        if child:IsA("LocalScript") and child.Name == "SprintMovement" then
            task.wait(1)
            DisableSprintMovementScans()
        end
    end)
end

pcall(FullBypass)
pcall(MonitorNewRemotes)

-- =============================================
-- FLIGHT SYSTEM (FIXED)
-- =============================================
local MainFlyButton = nil
local Flying = false
local FlySpeed = 50
local bodyVelocity = nil
local bodyGyro = nil
local flightConnection = nil
local currentHumanoid = nil
local currentRoot = nil
local FlyPanelCreated = false

-- ✅ إصلاح: نتحقق من AddButton ترجع TextButton
local function UpdateMainFlyButton()
    if MainFlyButton and MainFlyButton.Parent then
        pcall(function()
            if Flying then 
                MainFlyButton.Text = "🛬 هبوط الطيران"
            else 
                MainFlyButton.Text = "✈️ تشغيل وضع طيران" 
            end
        end)
    end
end

local FlyPanel = Create("Frame", ScreenGui, {
    Name = "FlyControlPanel",
    Size = UDim2.new(0, 200, 0, 120),
    Position = UDim2.new(0.5, -100, 0.2, 0),
    BackgroundColor3 = Configs_HUB.Cor_Hub,
    BackgroundTransparency = 0.2, Active = true, Draggable = true,
    Visible = false, BorderSizePixel = 0, ZIndex = 50
})
Corner(FlyPanel); Stroke(FlyPanel)

Create("TextLabel", FlyPanel, {
    Size = UDim2.new(1, 0, 0, 25), BackgroundTransparency = 1,
    Text = "التحكم بالطيران", TextColor3 = Configs_HUB.Cor_Text,
    Font = Configs_HUB.Text_Font, TextSize = 18,
    TextXAlignment = Enum.TextXAlignment.Center, ZIndex = 51
})

local FlySpeedLabel = Create("TextLabel", FlyPanel, {
    Size = UDim2.new(1, 0, 0, 20), Position = UDim2.new(0, 0, 0, 30),
    BackgroundTransparency = 1, Text = "السرعة: " .. FlySpeed,
    TextColor3 = Configs_HUB.Cor_Text, Font = Configs_HUB.Text_Font,
    TextSize = 14, TextXAlignment = Enum.TextXAlignment.Center, ZIndex = 51
})

local FlyIncButton = Create("TextButton", FlyPanel, {
    Size = UDim2.new(0.45, 0, 0, 30), Position = UDim2.new(0.05, 0, 0, 55),
    BackgroundColor3 = Configs_HUB.Cor_Options, Text = "+10",
    TextColor3 = Configs_HUB.Cor_Text, Font = Configs_HUB.Text_Font,
    TextSize = 16, AutoButtonColor = false, ZIndex = 51
})
Corner(FlyIncButton); Stroke(FlyIncButton)

local FlyDecButton = Create("TextButton", FlyPanel, {
    Size = UDim2.new(0.45, 0, 0, 30), Position = UDim2.new(0.5, 0, 0, 55),
    BackgroundColor3 = Configs_HUB.Cor_Options, Text = "-10",
    TextColor3 = Configs_HUB.Cor_Text, Font = Configs_HUB.Text_Font,
    TextSize = 16, AutoButtonColor = false, ZIndex = 51
})
Corner(FlyDecButton); Stroke(FlyDecButton)

local FlyStopButton = Create("TextButton", FlyPanel, {
    Size = UDim2.new(1, -10, 0, 30), Position = UDim2.new(0, 5, 0, 90),
    BackgroundColor3 = Configs_HUB.Cor_Options, Text = "🛬 هبوط",
    TextColor3 = Color3.fromRGB(255, 100, 100), Font = Configs_HUB.Text_Font,
    TextSize = 14, AutoButtonColor = false, Active = true, ZIndex = 51
})
Corner(FlyStopButton); Stroke(FlyStopButton)

local function UpdateFlyPanel() 
    if FlySpeedLabel and FlySpeedLabel.Parent then
        FlySpeedLabel.Text = "السرعة: " .. FlySpeed 
    end
end

FlyIncButton.Activated:Connect(function()
    FlySpeed = FlySpeed + 10
    UpdateFlyPanel()
    MakeNotifi({Title="Fly", Text="سرعة الطيران: "..FlySpeed, Time=1})
end)

FlyDecButton.Activated:Connect(function()
    if FlySpeed > 10 then FlySpeed = FlySpeed - 10 end
    UpdateFlyPanel()
    MakeNotifi({Title="Fly", Text="سرعة الطيران: "..FlySpeed, Time=1})
end)

local function stopFly()
    if not Flying then
        FlyPanel.Visible = FlyPanelCreated
        UpdateMainFlyButton()
        return
    end
    Flying = false
    if flightConnection then 
        pcall(function() flightConnection:Disconnect() end)
        flightConnection = nil 
    end
    if bodyVelocity then 
        pcall(function() 
            bodyVelocity.Velocity = Vector3.zero 
            bodyVelocity:Destroy() 
        end)
        bodyVelocity = nil 
    end
    if bodyGyro then 
        pcall(function() bodyGyro:Destroy() end)
        bodyGyro = nil 
    end
    if currentHumanoid and currentHumanoid.Parent then
        pcall(function()
            currentHumanoid.PlatformStand = false
            currentHumanoid:ChangeState(Enum.HumanoidStateType.GettingUp)
        end)
        currentHumanoid = nil
    end
    currentRoot = nil
    FlyPanel.Visible = true
    FlyPanelCreated = true
    FlyStopButton.Text = "✈️ تشغيل الطيران"
    UpdateMainFlyButton()
    MakeNotifi({Title = "Fly", Text = "تم الهبوط", Time = 2})
end

local function startFly()
    if Flying then return end
    local char = LP.Character
    if not char then 
        MakeNotifi({Title="Fly", Text="لا توجد شخصية", Time=2})
        return 
    end
    local root = char:FindFirstChild("HumanoidRootPart") or char:FindFirstChild("Torso")
    local humanoid = char:FindFirstChildOfClass("Humanoid")
    if not root or not humanoid then 
        MakeNotifi({Title="Fly", Text="لا يمكن تشغيل الطيران", Time=2})
        return 
    end

    stopFly()
    currentRoot = root
    currentHumanoid = humanoid

    bodyGyro = Instance.new("BodyGyro", root)
    bodyGyro.MaxTorque = Vector3.new(9e9, 9e9, 9e9)
    bodyGyro.P = 9e4
    bodyGyro.CFrame = root.CFrame

    bodyVelocity = Instance.new("BodyVelocity", root)
    bodyVelocity.MaxForce = Vector3.new(9e9, 9e9, 9e9)
    bodyVelocity.Velocity = Vector3.zero

    humanoid.PlatformStand = true
    Flying = true
    FlyPanelCreated = true
    FlyPanel.Visible = true
    FlyStopButton.Text = "🛬 هبوط"
    UpdateMainFlyButton()
    UpdateFlyPanel()

    local camera = workspace.CurrentCamera
    flightConnection = RunService.Heartbeat:Connect(function()
        if not Flying or not currentRoot or not currentHumanoid or currentHumanoid.Health <= 0 then
            stopFly() 
            return
        end
        if not currentRoot.Parent or not currentHumanoid.Parent then
            stopFly()
            return
        end
        pcall(function()
            bodyGyro.CFrame = camera.CFrame
            local moveDir = currentHumanoid.MoveDirection
            if moveDir.Magnitude > 0.05 then
                local camCFrame = camera.CFrame
                local forward = camCFrame.LookVector
                local right = camCFrame.RightVector
                local relativeMove = camCFrame:VectorToObjectSpace(moveDir)
                local finalVel = (forward * -relativeMove.Z) + (right * relativeMove.X)
                bodyVelocity.Velocity = finalVel * FlySpeed
            else
                bodyVelocity.Velocity = Vector3.zero
            end
        end)
    end)
    MakeNotifi({Title = "Fly", Text = "تم تشغيل الطيران", Time = 2})
end

FlyStopButton.Activated:Connect(function()
    if Flying then stopFly() else startFly() end
end)

LP.CharacterAdded:Connect(function() 
    if Flying then stopFly() end 
end)

-- =============================================
-- GUI SCANNER REMOVER
-- =============================================
local PlayerGui = LP:WaitForChild("PlayerGui")
local SuspiciousKeywords = {"warning","anticheat","detected","banned","admin","notification","toast","popup","exploit"}

local function isSuspicious(name)
    name = name:lower()
    for _, kw in ipairs(SuspiciousKeywords) do
        if string.find(name, kw) then return true end
    end
    return false
end

PlayerGui.ChildAdded:Connect(function(child)
    if child:IsA("ScreenGui") and isSuspicious(child.Name) then
        task.wait(0.1)
        pcall(function() child:Destroy() end)
    end
end)

task.wait(0.5)
for _, child in ipairs(PlayerGui:GetChildren()) do
    if child:IsA("ScreenGui") and isSuspicious(child.Name) then
        pcall(function() child:Destroy() end)
    end
end

-- =============================================
-- MAIN UI
-- =============================================
local Menu = Create("Frame", ScreenGui, {
    Size = UDim2.new(0, 530, 0, 320),
    Position = UDim2.new(0.5, -265, 0.5, -160),
    BackgroundColor3 = Configs_HUB.Cor_Hub,
    BackgroundTransparency = 0.05,
    Active = true, Draggable = true, BorderSizePixel = 0,
    ClipsDescendants = true, ZIndex = 1
})
Corner(Menu); Stroke(Menu)

local BackgroundFX = Create("Frame", Menu, {
    Name = "Monetor_BackgroundFX",
    Size = UDim2.new(1, 0, 1, 0),
    BackgroundColor3 = Color3.fromRGB(5, 5, 5),
    BackgroundTransparency = 0, BorderSizePixel = 0,
    ClipsDescendants = true, ZIndex = 0
})
Corner(BackgroundFX)

local RedGlowData = {
    {Size = 170, X = -0.05, Y = 0.05, DX = 0.22, DY = 0.10, Duration = 13},
    {Size = 125, X = 0.20, Y = 0.58, DX = -0.18, DY = 0.13, Duration = 16},
    {Size = 150, X = 0.58, Y = -0.10, DX = 0.16, DY = 0.18, Duration = 15},
    {Size = 105, X = 0.78, Y = 0.48, DX = -0.20, DY = -0.12, Duration = 14},
    {Size = 135, X = 0.38, Y = 0.72, DX = 0.18, DY = -0.16, Duration = 17},
    {Size = 90, X = 0.05, Y = 0.30, DX = 0.15, DY = -0.10, Duration = 12}
}

for index, data in ipairs(RedGlowData) do
    local glow = Create("Frame", BackgroundFX, {
        Name = "RedGlow_" .. index,
        Size = UDim2.new(0, data.Size, 0, data.Size),
        Position = UDim2.new(data.X, 0, data.Y, 0),
        BackgroundColor3 = Color3.fromRGB(170, 0, 0),
        BackgroundTransparency = 0.82, BorderSizePixel = 0, ZIndex = 0
    })
    Corner(glow, {CornerRadius = UDim.new(1, 0)})
    task.spawn(function()
        local baseX, baseY = data.X, data.Y
        while glow.Parent do
            local targetX = math.clamp(baseX + data.DX, -0.12, 0.90)
            local targetY = math.clamp(baseY + data.DY, -0.12, 0.82)
            local tweenOut = TweenService:Create(glow,
                TweenInfo.new(data.Duration, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut),
                {Position = UDim2.new(targetX, 0, targetY, 0), BackgroundTransparency = 0.74})
            tweenOut:Play(); tweenOut.Completed:Wait()
            if not glow.Parent then break end
            local tweenBack = TweenService:Create(glow,
                TweenInfo.new(data.Duration, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut),
                {Position = UDim2.new(baseX, 0, baseY, 0), BackgroundTransparency = 0.84})
            tweenBack:Play(); tweenBack.Completed:Wait()
        end
    end)
end

local IntroSound = Instance.new("Sound")
IntroSound.SoundId = "rbxassetid://8426701399"
IntroSound.Volume = 2
IntroSound.Parent = ScreenGui
pcall(function() IntroSound:Play() end)

local Effect = Create("Frame", ScreenGui, {
    Size = UDim2.new(0, 10, 0, 10),
    Position = UDim2.new(0.5, 0, 0.5, 0),
    AnchorPoint = Vector2.new(0.5, 0.5),
    BackgroundColor3 = Configs_HUB.Cor_Stroke,
    BackgroundTransparency = 0, BorderSizePixel = 0,
    ClipsDescendants = true, ZIndex = 100
})
local EffectCorner = Instance.new("UICorner"); EffectCorner.CornerRadius = UDim.new(0, 18); EffectCorner.Parent = Effect
local EffectStroke = Instance.new("UIStroke"); EffectStroke.Color = Configs_HUB.Cor_Stroke; EffectStroke.Thickness = 2; EffectStroke.Transparency = 0; EffectStroke.Parent = Effect

local EffectGrow = TweenService:Create(Effect, TweenInfo.new(0.55, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
    Size = UDim2.new(0, 560, 0, 350), BackgroundTransparency = 0.75
})
EffectGrow:Play()

task.wait(0.12)
local MenuAppear = TweenService:Create(Menu, TweenInfo.new(0.7, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {BackgroundTransparency = 0.3})
MenuAppear:Play()

task.wait(0.35)
local EffectOut = TweenService:Create(Effect, TweenInfo.new(0.25, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
    Size = UDim2.new(0, 590, 0, 350), BackgroundTransparency = 1
})
EffectOut:Play()
local StrokeOut = TweenService:Create(EffectStroke, TweenInfo.new(0.25, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {Transparency = 1})
StrokeOut:Play()
EffectOut.Completed:Wait()
Effect:Destroy()
IntroSound.Ended:Connect(function() IntroSound:Destroy() end)

local TopBar = Create("Frame", Menu, {
    BackgroundTransparency = 1, Size = UDim2.new(1, 0, 0, 30),
    Visible = false, BorderSizePixel = 0, ZIndex = 10
})
local ButtonsFrame = Create("Frame", TopBar, {
    Size = UDim2.new(0, 40, 1, -5), Position = UDim2.new(1, -10, 0, 2.5),
    AnchorPoint = Vector2.new(1, 0), BackgroundTransparency = 1, ZIndex = 15
})
Create("ImageLabel", TopBar, {
    Size = UDim2.new(0, 24, 0, 24), Position = UDim2.new(0, 8, 0.5, 0),
    AnchorPoint = Vector2.new(0, 0.5), BackgroundTransparency = 1,
    Image = "rbxassetid://89949755377207",
    ImageColor3 = Color3.fromRGB(255, 255, 255), ZIndex = 20
})
local NameFrame = Create("Frame", TopBar, {
    Size = UDim2.new(0, 0, 0, 25), AutomaticSize = Enum.AutomaticSize.X,
    Position = UDim2.new(0, 38, 0.5, 0), AnchorPoint = Vector2.new(0, 0.5),
    BackgroundColor3 = Configs_HUB.Cor_Options, BackgroundTransparency = 0.15, ZIndex = 11
})
Corner(NameFrame); Stroke(NameFrame)
local Title = Create("TextLabel", NameFrame, {
    Size = UDim2.new(0, 0, 1, 0), AutomaticSize = Enum.AutomaticSize.X,
    Position = UDim2.new(0, 10, 0, 0), BackgroundTransparency = 1,
    TextColor3 = Color3.fromRGB(255, 255, 255), Font = Configs_HUB.Text_Font,
    TextXAlignment = Enum.TextXAlignment.Left, TextYAlignment = Enum.TextYAlignment.Center,
    Text = "Monetor X", TextSize = 20, ZIndex = 12
})
local Padding = Instance.new("UIPadding"); Padding.PaddingLeft = UDim.new(0, 10); Padding.PaddingRight = UDim.new(0, 10); Padding.Parent = NameFrame
local TextGradient = Instance.new("UIGradient"); TextGradient.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 255, 255)),
    ColorSequenceKeypoint.new(0.3, Color3.fromRGB(180, 180, 180)),
    ColorSequenceKeypoint.new(0.6, Color3.fromRGB(90, 90, 90)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(15, 15, 15))
}
TextGradient.Parent = Title
task.spawn(function()
    local speed = 0.008; local offset = 0
    while Title and Title.Parent do
        offset = offset + speed
        if offset >= 1 then offset = 0 end
        pcall(function() TextGradient.Offset = Vector2.new(-offset, 0) end)
        RunService.RenderStepped:Wait()
    end
end)

local Minimize_BTN = Create("TextButton", ButtonsFrame, {
    Text = "-", TextColor3 = Configs_HUB.Cor_Text,
    Size = UDim2.new(0.5, 0, 1, 0), BackgroundTransparency = 1,
    Font = Configs_HUB.Text_Font, TextYAlignment = "Bottom", TextSize = 25
})
local IsMinimized = false
Minimize_BTN.MouseButton1Click:Connect(function()
    Minimize_BTN.Text = not IsMinimized and "+" or "-"
    if IsMinimized then
        IsMinimized = false
        CreateTween(Menu, "Size", UDim2.new(0, 530, 0, 320), 0.15, false)
    else
        IsMinimized = true
        CreateTween(Menu, "Size", UDim2.new(0, 530, 0, 25), 0.15, true)
    end
end)

local Close_Button = Create("TextButton", ButtonsFrame, {
    Text = "×", TextYAlignment = "Bottom", TextColor3 = Configs_HUB.Cor_Text,
    Size = UDim2.new(0.5, 0, 1, 0), AnchorPoint = Vector2.new(1, 0),
    Position = UDim2.new(1, 0, 0, 0), BackgroundTransparency = 1,
    Font = Configs_HUB.Text_Font, TextSize = 25
})
local function CreateClose()
    IsMinimized = false
    CreateTween(Menu, "Size", UDim2.new(0, 530, 0, 320), 0.3, false)
    local CloseGui = Create("TextButton", Menu, {
        BackgroundTransparency = 0.5, BackgroundColor3 = Configs_HUB.Cor_Hub,
        Size = UDim2.new(1, 0, 1, 0), AutoButtonColor = false,
        Text = "", Visible = false
    })
    Corner(CloseGui)
    local CloseMenu = Create("Frame", CloseGui, {
        Size = UDim2.new(), AnchorPoint = Vector2.new(0.5, 0.5),
        Position = UDim2.new(0.5, 0, 0.5, 0), BackgroundTransparency = 1,
        BackgroundColor3 = Configs_HUB.Cor_Hub
    })
    Corner(CloseMenu); Stroke(CloseMenu)
    Create("TextLabel", CloseMenu, {
        Size = UDim2.new(0.8, 0, 0.25, 0),
        Text = "هل تريد اغلاق السكربت؟",
        Position = UDim2.new(0.1, 0, 0.2),
        TextColor3 = Configs_HUB.Cor_Text, Font = Configs_HUB.Text_Font,
        TextScaled = true, BackgroundTransparency = 1
    })
    local Confirm = Create("TextButton", CloseMenu, {
        Size = UDim2.new(0.35, 0, 0.3, 0), Position = UDim2.new(0.1, 0, 0.5, 0),
        BackgroundColor3 = Configs_HUB.Cor_Hub, Text = "نعم",
        Font = Configs_HUB.Text_Font, TextColor3 = Color3.fromRGB(240, 0, 0), TextSize = 20
    })
    Corner(Confirm); Stroke(Confirm)
    local Cancel = Create("TextButton", CloseMenu, {
        Size = UDim2.new(0.35, 0, 0.3, 0), Position = UDim2.new(0.9, 0, 0.5, 0),
        AnchorPoint = Vector2.new(1, 0), BackgroundColor3 = Configs_HUB.Cor_Hub,
        Text = "لا", Font = Configs_HUB.Text_Font,
        TextColor3 = Color3.fromRGB(0, 240, 0), TextSize = 20
    })
    Corner(Cancel); Stroke(Cancel)
    local function SetVisible()
        CloseGui.Visible = Menu.Size.Y.Offset > 100
    end
    SetVisible()
    Menu:GetPropertyChangedSignal("Size"):Connect(SetVisible)
    CreateTween(CloseMenu, "BackgroundTransparency", 0, 0.2, false)
    CreateTween(CloseMenu, "Size", UDim2.new(0.7, 0, 0.7, 0), 0.2, false)
    Cancel.MouseButton1Click:Connect(function()
        CreateTween(CloseMenu, "BackgroundTransparency", 1, 0.3, false)
        CreateTween(CloseMenu, "Size", UDim2.new(), 0.2, true)
        CloseGui:Destroy()
    end)
    Confirm.MouseButton1Click:Connect(function()
        CloseGui:Destroy()
        CreateTween(Menu, "Size", UDim2.new(), 0.3, true)
        DestroyScript()
    end)
    CloseGui.MouseButton1Click:Connect(function()
        CreateTween(CloseMenu, "BackgroundTransparency", 1, 0.3, false)
        CreateTween(CloseMenu, "Size", UDim2.new(), 0.2, true)
        CloseGui:Destroy()
    end)
end
Close_Button.MouseButton1Click:Connect(CreateClose)

local AnimMenu = Create("Frame", ScreenGui, {
    Position = UDim2.new(0.5, 0, 0.5, 0), AnchorPoint = Vector2.new(0.5, 0.5),
    Size = UDim2.new(0, 0, 0, 100), BackgroundColor3 = Configs_HUB.Cor_Hub
})
Corner(AnimMenu, {CornerRadius = UDim.new(0, 6)}); Stroke(AnimMenu)
local PlayerHead = Create("ImageLabel", AnimMenu, {
    BackgroundTransparency = 1, Size = UDim2.new(0, 55, 0, 55),
    Position = UDim2.new(0.5, -27.5, 0, 8),
    Image = "rbxthumb://type=AvatarHeadShot&id=" .. LP.UserId .. "&w=150&h=150"
})
Corner(PlayerHead, {CornerRadius = UDim.new(1, 0)}); Stroke(PlayerHead)
local Anim_Credits = Create("TextLabel", AnimMenu, {
    Text = "مرحبا بك في MONETOR X " .. LP.Name,
    BackgroundTransparency = 1, Size = UDim2.new(1, 0, 0, 25),
    Position = UDim2.new(0, 0, 0, 68), Visible = false,
    Font = Configs_HUB.Text_Font, TextTransparency = 1,
    TextColor3 = Configs_HUB.Cor_Text, TextXAlignment = Enum.TextXAlignment.Center,
    TextYAlignment = Enum.TextYAlignment.Center, TextSize = 15
})
CreateTween(AnimMenu, "Size", UDim2.new(0, 180, 0, 100), 0.5, true)
Anim_Credits.Visible = true
for i = 1, 0, -0.1 do task.wait() Anim_Credits.TextTransparency = i end
task.wait(1)
for i = 0, 1, 0.1 do task.wait() Anim_Credits.TextTransparency = i end
Anim_Credits:Destroy(); PlayerHead:Destroy(); AnimMenu:Destroy()

CreateTween(Menu, "Size", UDim2.new(0, 530, 0, 35), 0.5, true)
TopBar.Visible = true
CreateTween(Menu, "Size", UDim2.new(0, 530, 0, 320), 0.3, true)
Menu.Draggable = true

local ScrollBar = Create("ScrollingFrame", Menu, {
    Size = UDim2.new(0, 140, 1, -tonumber(TopBar.Size.Y.Offset + 2)),
    Position = UDim2.new(0, 0, 1, 0), AnchorPoint = Vector2.new(0, 1),
    CanvasSize = UDim2.new(), ScrollingDirection = "Y",
    AutomaticCanvasSize = "Y", BackgroundTransparency = 1, ScrollBarThickness = 2
})
Create("UIPadding", ScrollBar, { PaddingLeft = UDim.new(0, 10), PaddingRight = UDim.new(0, 10), PaddingTop = UDim.new(0, 10), PaddingBottom = UDim.new(0, 10) })
Create("UIListLayout", ScrollBar, { Padding = UDim.new(0, 14) })

local Containers = Create("Frame", Menu, {
    Size = UDim2.new(1, -tonumber(ScrollBar.Size.X.Offset + 2), 1, -tonumber(TopBar.Size.Y.Offset + 2)),
    AnchorPoint = Vector2.new(1, 1), Position = UDim2.new(1, 0, 1, 0),
    BackgroundTransparency = 1
})
Corner(Containers); Stroke(Containers)

local firstVisible = true
local textsize = 15
local textcolor = Configs_HUB.Cor_Text
Menu:GetPropertyChangedSignal("Size"):Connect(function()
    if Menu.Size.Y.Offset > 70 then
        ScrollBar.Visible = true; Containers.Visible = true
    else
        ScrollBar.Visible = false; Containers.Visible = false
    end
end)

-- =============================================
-- MAKETAB
-- =============================================
function MakeTab(Configs)
    local TabName = Configs.Name or "Tab"
    local TabImage = Configs.Image or nil
    local AvatarUsername = Configs.AvatarUsername or nil

    local Frame = Create("Frame", ScrollBar, { Size = UDim2.new(1, 0, 0, 30), BackgroundTransparency = 1 })
    Corner(Frame); Stroke(Frame)
    local TextButton = Create("TextButton", Frame, { Size = UDim2.new(1, 0, 1, 0), BackgroundTransparency = 1, Text = "" })

    local hasIcon = false

    if AvatarUsername then
        hasIcon = true
        local avatar = Create("ImageLabel", Frame, {
            Size = UDim2.new(0, 20, 0, 20), Position = UDim2.new(0, 5, 0.5, -10),
            BackgroundTransparency = 1, BackgroundColor3 = Configs_HUB.Cor_Options,
            Image = "", Name = "TabAvatar", ZIndex = 5
        })
        Corner(avatar, { CornerRadius = UDim.new(1, 0) })
        local avatarStroke = Instance.new("UIStroke")
        avatarStroke.Thickness = 1.5
        avatarStroke.Color = Color3.fromRGB(255, 255, 255)
        avatarStroke.Transparency = 0.15
        avatarStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
        avatarStroke.Parent = avatar
        task.spawn(function()
            local ok, userId = pcall(function() return Players:GetUserIdFromNameAsync(AvatarUsername) end)
            if ok and userId and type(userId) == "number" then
                pcall(function()
                    avatar.Image = "rbxthumb://type=AvatarHeadShot&id=" .. tostring(userId) .. "&w=150&h=150"
                end)
            else
                pcall(function() avatar.Image = "rbxassetid://6031075931" end)
            end
        end)
    elseif TabImage then
        hasIcon = true
        Create("ImageLabel", Frame, {
            Size = UDim2.new(0, 20, 0, 20), Position = UDim2.new(0, 5, 0.5, -10),
            BackgroundTransparency = 1, Image = TabImage, Name = "TabIcon"
        })
    end

    local textOffset = hasIcon and 30 or 5
    local TextLabel = Create("TextLabel", Frame, {
        Size = UDim2.new(1, -textOffset, 1, 0), Position = UDim2.new(0, textOffset, 0, 0),
        BackgroundTransparency = 1, Font = Configs_HUB.Text_Font,
        TextColor3 = textcolor, TextSize = textsize,
        Text = TabName, TextXAlignment = Enum.TextXAlignment.Left
    })

    local Container = Create("ScrollingFrame", Containers, {
        Size = UDim2.new(1, 0, 1, 0), ScrollingDirection = "Y",
        AutomaticCanvasSize = "Y", CanvasSize = UDim2.new(),
        BackgroundTransparency = 1, ScrollBarThickness = 2,
        Visible = firstVisible
    })
    Corner(Container); Stroke(Container)
    Create("UIPadding", Container, { PaddingLeft = UDim.new(0, 10), PaddingRight = UDim.new(0, 10), PaddingTop = UDim.new(0, 10), PaddingBottom = UDim.new(0, 10) })
    Create("UIListLayout", Container, { Padding = UDim.new(0, 5) })

    TextButton.MouseButton1Click:Connect(function()
        for _, container in pairs(Containers:GetChildren()) do
            if container:IsA("ScrollingFrame") then container.Visible = false end
        end
        for _, frame in pairs(ScrollBar:GetChildren()) do
            if frame:IsA("Frame") and frame:FindFirstChild("TextLabel") and frame.TextLabel ~= TextLabel then
                CreateTween(frame.TextLabel, "TextColor3", Configs_HUB.Cor_DarkText, 0.3, false)
                frame.TextLabel.TextSize = 14
            end
        end
        Container.Visible = true
        CreateTween(TextLabel, "TextColor3", Configs_HUB.Cor_Text, 0.3, false)
        TextLabel.TextSize = 15
    end)

    firstVisible = false
    textsize = 14
    textcolor = Configs_HUB.Cor_DarkText
    return Container
end

-- =============================================
-- ADDBUTTON (FIXED - يرجع TextButton)
-- =============================================
function AddButton(parent, Configs)
    local ButtonName = Configs.Name or "Button!!"
    local Callback = Configs.Callback or function() end
    
    local TextButton = Create("TextButton", parent, {
        Size = UDim2.new(1, 0, 0, 28), BackgroundColor3 = Configs_HUB.Cor_Options,
        BackgroundTransparency = 0.5, Name = "Frame", Text = "", AutoButtonColor = false
    })
    Corner(TextButton); Stroke(TextButton)
    local TextLabel = Create("TextLabel", TextButton, {
        TextSize = 14, TextColor3 = Configs_HUB.Cor_Text, Text = ButtonName,
        Size = UDim2.new(1, -40, 1, 0), Position = UDim2.new(0, 35, 0, 0),
        BackgroundTransparency = 1, TextXAlignment = "Left", Font = Configs_HUB.Text_Font
    })
    local ImageLabel = Create("ImageLabel", TextButton, {
        Image = "rbxassetid://99956220697276",
        Size = UDim2.new(0, 20, 0, 20), Position = UDim2.new(0, 5, 0.5, -10),
        BackgroundTransparency = 1, ImageColor3 = Configs_HUB.Cor_Stroke
    })
    TextButton.MouseButton1Click:Connect(function()
        pcall(Callback)
        CreateTween(ImageLabel, "ImageColor3", Color3.fromRGB(30, 140, 200), 0.2, true)
        CreateTween(ImageLabel, "ImageColor3", Configs_HUB.Cor_Stroke, 0.2, false)
    end)
    TextSetColor(TextLabel)
    return TextButton
end

-- =============================================
-- ADDTOGGLEBUTTON
-- =============================================
function AddToggleButton(parent, Configs)
    local ButtonName = Configs.Name or "Toggle!!"
    local OnText = Configs.OnText or (ButtonName .. ": تشغيل")
    local OffText = Configs.OffText or (ButtonName .. ": إيقاف")
    local StartState = Configs.StartState or false
    local OnEnable = Configs.OnEnable or function() end
    local OnDisable = Configs.OnDisable or function() end
    
    local State = StartState
    
    local TextButton = Create("TextButton", parent, {
        Size = UDim2.new(1, 0, 0, 28), BackgroundColor3 = Configs_HUB.Cor_Options,
        BackgroundTransparency = 0.5, Name = "Frame", Text = "", AutoButtonColor = false
    })
    Corner(TextButton); Stroke(TextButton)
    local TextLabel = Create("TextLabel", TextButton, {
        TextSize = 14, TextColor3 = Configs_HUB.Cor_Text,
        Text = State and OffText or OnText,
        Size = UDim2.new(1, -40, 1, 0), Position = UDim2.new(0, 35, 0, 0),
        BackgroundTransparency = 1, TextXAlignment = "Left", Font = Configs_HUB.Text_Font
    })
    local ImageLabel = Create("ImageLabel", TextButton, {
        Image = "rbxassetid://99956220697276",
        Size = UDim2.new(0, 20, 0, 20), Position = UDim2.new(0, 5, 0.5, -10),
        BackgroundTransparency = 1,
        ImageColor3 = State and Color3.fromRGB(80, 220, 120) or Configs_HUB.Cor_Stroke
    })
    TextButton.MouseButton1Click:Connect(function()
        State = not State
        TextLabel.Text = State and OffText or OnText
        CreateTween(ImageLabel, "ImageColor3", Color3.fromRGB(30, 140, 200), 0.2, true)
        CreateTween(ImageLabel, "ImageColor3", State and Color3.fromRGB(80, 220, 120) or Configs_HUB.Cor_Stroke, 0.2, false)
        if State then pcall(OnEnable) else pcall(OnDisable) end
    end)
    TextSetColor(TextLabel)
    return {
        Button = TextButton, TextLabel = TextLabel,
        GetState = function() return State end,
        SetState = function(newState)
            State = newState
            TextLabel.Text = State and OffText or OnText
            ImageLabel.ImageColor3 = State and Color3.fromRGB(80, 220, 120) or Configs_HUB.Cor_Stroke
        end
    }
end

-- =============================================
-- ADDTEXTLABEL
-- =============================================
function AddTextLabel(parent, text)
    local Label = Create("TextLabel", parent, {
        Size = UDim2.new(1, 0, 0, 100), BackgroundTransparency = 1,
        Text = text or "Loading...", TextColor3 = Configs_HUB.Cor_Text,
        TextSize = 13, Font = Configs_HUB.Text_Font,
        TextXAlignment = Enum.TextXAlignment.Left,
        TextYAlignment = Enum.TextYAlignment.Top,
        TextWrapped = true, TextScaled = false,
        AutomaticSize = Enum.AutomaticSize.Y
    })
    return { Update = function(self, newText) Label.Text = newText end, Label = Label }
end

-- =============================================
-- ADDPLAYERPROFILE
-- =============================================
function AddPlayerProfile(parent)
    local ProfileFrame = Create("Frame", parent, {
        Size = UDim2.new(1, 0, 0, 90),
        BackgroundColor3 = Configs_HUB.Cor_Options,
        BackgroundTransparency = 0.4,
        BorderSizePixel = 0,
        Name = "PlayerProfile"
    })
    Corner(ProfileFrame)
    Stroke(ProfileFrame)

    local AvatarImage = Create("ImageLabel", ProfileFrame, {
        Size = UDim2.new(0, 55, 0, 55),
        Position = UDim2.new(0, 12, 0.5, -27.5),
        BackgroundTransparency = 1,
        BackgroundColor3 = Configs_HUB.Cor_Hub,
        Image = "rbxthumb://type=AvatarHeadShot&id=" .. LP.UserId .. "&w=150&h=150",
        ZIndex = 5
    })
    Corner(AvatarImage, { CornerRadius = UDim.new(1, 0) })
    local avStroke = Instance.new("UIStroke")
    avStroke.Thickness = 2
    avStroke.Color = Color3.fromRGB(255, 255, 255)
    avStroke.Transparency = 0.1
    avStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    avStroke.Parent = AvatarImage

    Create("TextLabel", ProfileFrame, {
        Size = UDim2.new(1, -80, 0, 22),
        Position = UDim2.new(0, 75, 0, 8),
        BackgroundTransparency = 1,
        Text = "👤 " .. LP.Name,
        TextColor3 = Color3.fromRGB(255, 255, 255),
        Font = Configs_HUB.Text_Font,
        TextSize = 16,
        TextXAlignment = Enum.TextXAlignment.Left,
        ZIndex = 5
    })

    Create("TextLabel", ProfileFrame, {
        Size = UDim2.new(1, -80, 0, 16),
        Position = UDim2.new(0, 75, 0, 30),
        BackgroundTransparency = 1,
        Text = "Display: " .. LP.DisplayName,
        TextColor3 = Configs_HUB.Cor_DarkText,
        Font = Configs_HUB.Text_Font,
        TextSize = 12,
        TextXAlignment = Enum.TextXAlignment.Left,
        ZIndex = 5
    })

    local placeName = "Unknown"
    pcall(function() placeName = MarketplaceService:GetProductInfo(game.PlaceId).Name end)
    
    Create("TextLabel", ProfileFrame, {
        Size = UDim2.new(1, -80, 0, 16),
        Position = UDim2.new(0, 75, 0, 48),
        BackgroundTransparency = 1,
        Text = "🎮 " .. tostring(placeName),
        TextColor3 = Configs_HUB.Cor_DarkText,
        Font = Configs_HUB.Text_Font,
        TextSize = 12,
        TextXAlignment = Enum.TextXAlignment.Left,
        TextTruncate = Enum.TextTruncate.AtEnd,
        ZIndex = 5
    })

    local StatsLabel = Create("TextLabel", ProfileFrame, {
        Size = UDim2.new(1, -80, 0, 16),
        Position = UDim2.new(0, 75, 0, 66),
        BackgroundTransparency = 1,
        Text = "FPS: -- | Ping: --",
        TextColor3 = Color3.fromRGB(120, 220, 120),
        Font = Configs_HUB.Text_Font,
        TextSize = 12,
        TextXAlignment = Enum.TextXAlignment.Left,
        ZIndex = 5
    })

    local fps = 0
    local frameCount = 0
    local lastTime = tick()
    RunService.Heartbeat:Connect(function()
        frameCount = frameCount + 1
        if tick() - lastTime >= 1 then
            fps = frameCount
            frameCount = 0
            lastTime = tick()
        end
    end)

    task.spawn(function()
        while StatsLabel and StatsLabel.Parent do
            task.wait(1)
            pcall(function()
                local ping = 0
                if LP.GetNetworkPing then
                    ping = math.floor(LP:GetNetworkPing() * 1000)
                end
                StatsLabel.Text = "FPS: " .. fps .. " | Ping: " .. ping .. "ms"
            end)
        end
    end)
end

-- =============================================
-- ADDDROPDOWN
-- =============================================
function AddDropdown(parent, Configs)
    local DropdownName = Configs.Name or "Dropdown!!"
    local Options = Configs.Options or {"1", "2", "3"}
    local Default = Configs.Default or Options[1]
    local Callback = Configs.Callback or function() end
    local Searchable = Configs.Searchable or false
    
    local MainFrame = Create("Frame", parent, {
        Size = UDim2.new(1, 0, 0, 28), BackgroundTransparency = 1, Name = "DropdownContainer"
    })
    
    local TextButton = Create("TextButton", MainFrame, {
        Size = UDim2.new(1, 0, 0, 28), BackgroundColor3 = Configs_HUB.Cor_Options,
        Name = "Header", Text = "", AutoButtonColor = false
    }) 
    
    local dropdownStroke = Stroke(TextButton, { Color = Configs_HUB.Cor_Stroke, Thickness = 2 })
    local gradient = Instance.new("UIGradient")
    gradient.Color = ColorSequence.new{
        ColorSequenceKeypoint.new(0, Configs_HUB.Cor_Stroke),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0, 100, 255)),
        ColorSequenceKeypoint.new(1, Configs_HUB.Cor_Stroke)
    }
    gradient.Rotation = 0
    gradient.Parent = dropdownStroke
    coroutine.wrap(function()
        while dropdownStroke.Parent do
            pcall(function() gradient.Rotation = (gradient.Rotation + 1) % 360 end)
            RunService.RenderStepped:Wait()
        end
    end)()
    Corner(TextButton)
    
    local TextLabel = Create("TextLabel", TextButton, {
        TextSize = 12, TextColor3 = Configs_HUB.Cor_Text, Text = DropdownName,
        Size = UDim2.new(1, -30, 1, 0), Position = UDim2.new(0, 10, 0, 0),
        BackgroundTransparency = 1, TextXAlignment = "Left", Font = Configs_HUB.Text_Font
    })
    TextSetColor(TextLabel)
    
    Create("ImageLabel", TextButton, {
        Image = "rbxassetid://6031090998",
        Size = UDim2.new(0, 20, 0, 20), Position = UDim2.new(1, -25, 0.5, 0),
        AnchorPoint = Vector2.new(0, 0.5), BackgroundTransparency = 1,
        ImageColor3 = Configs_HUB.Cor_Text
    })
    
    local DropdownFrame = Create("ScrollingFrame", MainFrame, {
        Size = UDim2.new(1, 0, 0, 0), Position = UDim2.new(0, 0, 0, 30),
        BackgroundColor3 = Configs_HUB.Cor_Options, BackgroundTransparency = 0.1,
        ClipsDescendants = true, Visible = false, ScrollBarThickness = 3,
        ScrollBarImageColor3 = Configs_HUB.Cor_Stroke, CanvasSize = UDim2.new(0, 0, 0, 0),
        AutomaticCanvasSize = Enum.AutomaticSize.Y, ZIndex = 20
    })
    Corner(DropdownFrame); Stroke(DropdownFrame)
    
    Create("UIListLayout", DropdownFrame, {
        Padding = UDim.new(0, 2), SortOrder = Enum.SortOrder.LayoutOrder
    })
    Create("UIPadding", DropdownFrame, {
        PaddingLeft = UDim.new(0, 4), PaddingRight = UDim.new(0, 4),
        PaddingTop = UDim.new(0, 4), PaddingBottom = UDim.new(0, 4)
    })
    
    local SearchBox
    if Searchable then
        SearchBox = Create("TextBox", DropdownFrame, {
            Size = UDim2.new(1, 0, 0, 25), BackgroundColor3 = Configs_HUB.Cor_Hub,
            TextColor3 = Configs_HUB.Cor_Text, PlaceholderText = "ابحث...",
            Font = Configs_HUB.Text_Font, TextSize = 12,
            TextXAlignment = Enum.TextXAlignment.Left,
            ClearTextOnFocus = false, ZIndex = 25
        })
        Corner(SearchBox); Stroke(SearchBox)
        SearchBox.LayoutOrder = -1
    end
    
    local Selected = Default
    local expanded = false
    local currentSearchText = ""
    
    local function GetTargetHeight()
        local count = #Options
        local totalH = count * 22 + 8
        if Searchable then totalH = totalH + 30 end
        return math.min(totalH, 150)
    end
    
    local function UpdateButtons()
        for _, child in ipairs(DropdownFrame:GetChildren()) do
            if child:IsA("TextButton") then child:Destroy() end
        end
        local filteredOptions = {}
        if currentSearchText ~= "" then
            for _, option in ipairs(Options) do
                if string.find(string.lower(option), string.lower(currentSearchText), 1, true) then
                    table.insert(filteredOptions, option)
                end
            end
        else
            filteredOptions = Options
        end
        
        for _, option in ipairs(filteredOptions) do
            local btn = Create("TextButton", DropdownFrame, {
                Size = UDim2.new(1, 0, 0, 20), BackgroundColor3 = Configs_HUB.Cor_Hub,
                Text = option, TextColor3 = Configs_HUB.Cor_Text, TextSize = 12,
                Font = Configs_HUB.Text_Font, AutoButtonColor = false, ZIndex = 21
            })
            Corner(btn)
            btn.MouseButton1Click:Connect(function()
                Selected = option
                TextLabel.Text = DropdownName .. ": " .. option
                pcall(Callback, option)
                expanded = false
                currentSearchText = ""
                if SearchBox then SearchBox.Text = "" end
                CreateTween(DropdownFrame, "Size", UDim2.new(1, 0, 0, 0), 0.2, true)
                DropdownFrame.Visible = false
                CreateTween(MainFrame, "Size", UDim2.new(1, 0, 0, 28), 0.2, false)
            end)
        end
        
        if expanded then
            local targetH = GetTargetHeight()
            DropdownFrame.Size = UDim2.new(1, 0, 0, targetH)
            MainFrame.Size = UDim2.new(1, 0, 0, 30 + targetH)
        end
    end
    
    UpdateButtons()
    
    if SearchBox then
        SearchBox:GetPropertyChangedSignal("Text"):Connect(function()
            currentSearchText = SearchBox.Text
            UpdateButtons()
        end)
    end
    
    TextButton.MouseButton1Click:Connect(function()
        expanded = not expanded
        if expanded then
            DropdownFrame.Visible = true
            local targetH = GetTargetHeight()
            CreateTween(DropdownFrame, "Size", UDim2.new(1, 0, 0, targetH), 0.2, false)
            CreateTween(MainFrame, "Size", UDim2.new(1, 0, 0, 30 + targetH), 0.2, false)
        else
            CreateTween(DropdownFrame, "Size", UDim2.new(1, 0, 0, 0), 0.2, true)
            DropdownFrame.Visible = false
            CreateTween(MainFrame, "Size", UDim2.new(1, 0, 0, 28), 0.2, false)
        end
    end)
    
    return {
        Update = function(self, newOptions) Options = newOptions UpdateButtons() end,
        GetSelected = function(self) return Selected end
    }
end

-- =============================================
-- TABS
-- =============================================
local MainTab = MakeTab({Name = "الرئيسية"})
local LocalPlayerTab = MakeTab({Name = "شخصيتي"})
local ESPTab = MakeTab({Name = "رادار"})
local TeleportTab = MakeTab({Name = "الانتقال"})
local CombatTab = MakeTab({Name = "قتال"})
local TrollTab = MakeTab({Name = "Troll", Image = "rbxassetid://6031075931"})
local RightsTab = MakeTab({Name = "حقوق", AvatarUsername = "sjbcjjs0"})

-- =============================================
-- MAIN TAB BUTTONS
-- =============================================
AddButton(MainTab, { Name = "🛡 فك حماية الطيران", Callback = function() FullBypass() end })

MainFlyButton = AddButton(MainTab, {
    Name = "✈️ تشغيل وضع طيران",
    Callback = function() if Flying then stopFly() else startFly() end end
})

AddButton(MainTab, {
    Name = "🎛 إظهار لوحة الطيران",
    Callback = function()
        FlyPanelCreated = true
        FlyPanel.Visible = true
        UpdateFlyPanel()
        MakeNotifi({Title="Fly", Text="تم إظهار اللوحة", Time=2})
    end
})

AddButton(MainTab, {
    Name = "⬆ زيادة سرعة الطيران (+50)",
    Callback = function()
        FlySpeed = FlySpeed + 50
        UpdateFlyPanel()
        MakeNotifi({Title="Fly", Text="السرعة: "..FlySpeed, Time=2})
    end
})

AddButton(MainTab, {
    Name = "⬇ تقليل سرعة الطيران (-50)",
    Callback = function()
        if FlySpeed > 50 then FlySpeed = FlySpeed - 50 end
        UpdateFlyPanel()
        MakeNotifi({Title="Fly", Text="السرعة: "..FlySpeed, Time=2})
    end
})

local RespawnKillDropdown = AddDropdown(MainTab, {
    Name = "اختر الأمر",
    Options = {"رفريش (Respawn)", "موت سريع (Kill)"},
    Default = "رفريش (Respawn)",
    Searchable = false
})

AddButton(MainTab, {
    Name = "⚡ تنفيذ الأمر المختار",
    Callback = function()
        local selected = RespawnKillDropdown.GetSelected()
        if selected == "رفريش (Respawn)" then
            pcall(function()
                local hdClient = RepStorage:FindFirstChild("HDAdminHDClient")
                if hdClient then
                    local signals = hdClient:FindFirstChild("Signals")
                    if signals then
                        local rc = signals:FindFirstChild("RequestCommand")
                        if rc then
                            rc:InvokeServer(";refresh")
                            MakeNotifi({Title="أمر", Text="تم ;refresh", Time=2})
                        end
                    end
                end
            end)
        elseif selected == "موت سريع (Kill)" then
            if LP.Character and LP.Character:FindFirstChild("Humanoid") then
                LP.Character.Humanoid.Health = 0
                MakeNotifi({Title="أمر", Text="تم القتل", Time=2})
            end
        end
    end
})

-- =============================================
-- LOCAL PLAYER TAB
-- =============================================
local SpeedEnabled = false
local JumpEnabled = false
local NoclipEnabled = false
local InfJumpEnabled = false

AddToggleButton(LocalPlayerTab, {
    Name = "سرعة عالية", StartState = false,
    OnEnable = function() SpeedEnabled = true MakeNotifi({Title="Player", Text="سرعة عالية On", Time=2}) end,
    OnDisable = function()
        SpeedEnabled = false
        if LP.Character and LP.Character:FindFirstChild("Humanoid") then LP.Character.Humanoid.WalkSpeed = 16 end
        MakeNotifi({Title="Player", Text="سرعة عادية", Time=2})
    end
})

AddToggleButton(LocalPlayerTab, {
    Name = "قفز عالي", StartState = false,
    OnEnable = function() JumpEnabled = true MakeNotifi({Title="Player", Text="قفز عالي On", Time=2}) end,
    OnDisable = function()
        JumpEnabled = false
        if LP.Character and LP.Character:FindFirstChild("Humanoid") then LP.Character.Humanoid.JumpPower = 50 end
        MakeNotifi({Title="Player", Text="قفز عادي", Time=2})
    end
})

AddToggleButton(LocalPlayerTab, {
    Name = "Noclip", StartState = false,
    OnEnable = function() NoclipEnabled = true MakeNotifi({Title="Player", Text="Noclip On", Time=2}) end,
    OnDisable = function() NoclipEnabled = false MakeNotifi({Title="Player", Text="Noclip Off", Time=2}) end
})

AddToggleButton(LocalPlayerTab, {
    Name = "قفز لا نهائي", StartState = false,
    OnEnable = function() InfJumpEnabled = true MakeNotifi({Title="Player", Text="Inf Jump On", Time=2}) end,
    OnDisable = function() InfJumpEnabled = false MakeNotifi({Title="Player", Text="Inf Jump Off", Time=2}) end
})

RunService.Stepped:Connect(function()
    pcall(function()
        if LP.Character and LP.Character:FindFirstChild("Humanoid") then
            if SpeedEnabled then LP.Character.Humanoid.WalkSpeed = 100 end
            if JumpEnabled then LP.Character.Humanoid.JumpPower = 150 end
        end
        if NoclipEnabled and LP.Character then
            for _, v in pairs(LP.Character:GetDescendants()) do
                if v:IsA("BasePart") then v.CanCollide = false end
            end
        end
    end)
end)

UserInputService.JumpRequest:Connect(function()
    if InfJumpEnabled and LP.Character and LP.Character:FindFirstChild("Humanoid") then
        pcall(function()
            LP.Character.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
        end)
    end
end)

-- =============================================
-- ESP TAB
-- =============================================
local ESP_Active = false
local ESP_ShowNames = true
local ESP_ShowBody = true
local ESP_ShowHealth = true
local ESP_ShowDistance = true
local ESP_MaxDistance = 250
local ESP_UpdateInterval = 0.08
local ESP_TARGET_DISTANCE = 1000000000
local ESP_FOLDER_NAME = "Monetor_ESP_RADAR"
local ESP_TargetUserId = nil
local ESP_TargetName = nil
local ESP_TargetDropdown = nil

local function GetESPColor(player)
    local ok, color = pcall(function()
        if player.Team and player.Team.TeamColor then return player.Team.TeamColor.Color end
        return Color3.fromRGB(0, 170, 255)
    end)
    return ok and color or Color3.fromRGB(0, 170, 255)
end

local function GetCharacterRoot(character)
    return character and (character:FindFirstChild("HumanoidRootPart")
        or character:FindFirstChild("UpperTorso")
        or character:FindFirstChild("Torso"))
end

local function IsTarget(player)
    return player and ESP_TargetUserId ~= nil and player.UserId == ESP_TargetUserId
end

local function GetOrCreateESPFolder(character)
    if not character then return nil end
    local folder = character:FindFirstChild(ESP_FOLDER_NAME)
    if folder and folder:IsA("Folder") then return folder end
    if folder then pcall(function() folder:Destroy() end) end
    folder = Instance.new("Folder")
    folder.Name = ESP_FOLDER_NAME
    folder.Parent = character
    return folder
end

local function RemoveESP(player)
    if not player or not player.Character then return end
    local folder = player.Character:FindFirstChild(ESP_FOLDER_NAME)
    if folder then pcall(function() folder:Destroy() end) end
end

local function ClearAllESP()
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LP then RemoveESP(player) end
    end
end

local function GetPlayersSortedByDistance()
    local list = {}
    local myCharacter = LP.Character
    local myRoot = myCharacter and myCharacter:FindFirstChild("HumanoidRootPart")
    local myPosition = myRoot and myRoot.Position
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LP then
            local distance = math.huge
            local character = player.Character
            local root = character and character:FindFirstChild("HumanoidRootPart")
            if myPosition and root then distance = (root.Position - myPosition).Magnitude end
            table.insert(list, {player = player, distance = distance})
        end
    end
    table.sort(list, function(a, b)
        if a.distance == b.distance then return a.player.Name:lower() < b.player.Name:lower() end
        return a.distance < b.distance
    end)
    return list
end

local function UpdateRadarTargetList()
    local options = {}
    for _, entry in ipairs(GetPlayersSortedByDistance()) do
        table.insert(options, entry.player.Name)
    end
    if #options == 0 then options = {"لا يوجد لاعبين"} end
    if ESP_TargetDropdown then
        pcall(function() ESP_TargetDropdown:Update(options) end)
    end
end

AddToggleButton(ESPTab, {
    Name = "الرادار المطور", StartState = false,
    OnEnable = function()
        ESP_Active = true
        for _, player in ipairs(Players:GetPlayers()) do
            if player ~= LP then 
                pcall(function()
                    if player.Character then
                        -- Create basic ESP
                        local character = player.Character
                        local folder = GetOrCreateESPFolder(character)
                        if folder then
                            local highlight = Instance.new("Highlight")
                            highlight.Name = "BodyHighlight"
                            highlight.Adornee = character
                            highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
                            highlight.FillTransparency = 0.84
                            highlight.OutlineTransparency = 0.03
                            local color = IsTarget(player) and Color3.fromRGB(255, 40, 40) or GetESPColor(player)
                            highlight.FillColor = color
                            highlight.OutlineColor = color
                            highlight.Parent = folder
                        end
                    end
                end)
            end
        end
        MakeNotifi({Title="ESP", Text="الرادار On", Time=3})
    end,
    OnDisable = function()
        ESP_Active = false
        ClearAllESP()
        MakeNotifi({Title="ESP", Text="الرادار Off", Time=2})
    end
})

AddToggleButton(ESPTab, {
    Name = "الأسماء", StartState = true,
    OnEnable = function() ESP_ShowNames = true MakeNotifi({Title="ESP", Text="الأسماء On", Time=2}) end,
    OnDisable = function()
        ESP_ShowNames = false
        for _, player in ipairs(Players:GetPlayers()) do
            if player ~= LP and player.Character then
                local folder = player.Character:FindFirstChild(ESP_FOLDER_NAME)
                local billboard = folder and folder:FindFirstChild("NameBillboard")
                if billboard and billboard:IsA("BillboardGui") then billboard.Enabled = false end
            end
        end
        MakeNotifi({Title="ESP", Text="الأسماء Off", Time=2})
    end
})

AddToggleButton(ESPTab, {
    Name = "الجسم", StartState = true,
    OnEnable = function() ESP_ShowBody = true MakeNotifi({Title="ESP", Text="الجسم On", Time=2}) end,
    OnDisable = function() ESP_ShowBody = false MakeNotifi({Title="ESP", Text="الجسم Off", Time=2}) end
})

AddToggleButton(ESPTab, {
    Name = "الصحة", StartState = true,
    OnEnable = function() ESP_ShowHealth = true MakeNotifi({Title="ESP", Text="الصحة On", Time=2}) end,
    OnDisable = function() ESP_ShowHealth = false MakeNotifi({Title="ESP", Text="الصحة Off", Time=2}) end
})

AddToggleButton(ESPTab, {
    Name = "المسافة", StartState = true,
    OnEnable = function() ESP_ShowDistance = true MakeNotifi({Title="ESP", Text="المسافة On", Time=2}) end,
    OnDisable = function() ESP_ShowDistance = false MakeNotifi({Title="ESP", Text="المسافة Off", Time=2}) end
})

ESP_TargetDropdown = AddDropdown(ESPTab, {
    Name = "🎯 اختر المستهدف",
    Options = {"لا يوجد لاعبين"},
    Searchable = true,
    Callback = function(value)
        if value == "لا يوجد لاعبين" then return end
        local targetPlayer = Players:FindFirstChild(value)
        if not targetPlayer or targetPlayer == LP then
            MakeNotifi({Title="Target", Text="غير متوفر", Time=2})
            return
        end
        ESP_TargetUserId = targetPlayer.UserId
        ESP_TargetName = targetPlayer.Name
        MakeNotifi({Title="Target", Text="تم استهداف " .. targetPlayer.Name, Time=3})
    end
})

AddButton(ESPTab, {
    Name = "❌ إلغاء الاستهداف",
    Callback = function()
        ESP_TargetUserId = nil
        ESP_TargetName = nil
        MakeNotifi({Title="Target", Text="تم الإلغاء", Time=2})
    end
})

local ESP_DistanceBox = Create("TextBox", ESPTab, {
    Size = UDim2.new(1, 0, 0, 30),
    BackgroundColor3 = Configs_HUB.Cor_Options,
    BackgroundTransparency = 0.25, BorderSizePixel = 0,
    Text = tostring(ESP_MaxDistance),
    PlaceholderText = "مدى الرادار (متر)",
    TextColor3 = Configs_HUB.Cor_Text,
    PlaceholderColor3 = Color3.fromRGB(150, 150, 150),
    TextSize = 13, Font = Configs_HUB.Text_Font,
    ClearTextOnFocus = false, TextXAlignment = Enum.TextXAlignment.Center, ZIndex = 20
})
Corner(ESP_DistanceBox); Stroke(ESP_DistanceBox)

local function ApplyESPDistance(value)
    value = math.floor(tonumber(value) or 250)
    value = math.clamp(value, 10, 5000)
    ESP_MaxDistance = value
    ESP_DistanceBox.Text = tostring(value)
    MakeNotifi({Title="ESP", Text="مدى الرادار: " .. ESP_MaxDistance .. "م", Time=2})
end

ESP_DistanceBox.FocusLost:Connect(function() ApplyESPDistance(ESP_DistanceBox.Text) end)
AddButton(ESPTab, { Name = "✅ تطبيق مدى الرادار", Callback = function() ApplyESPDistance(ESP_DistanceBox.Text) end })

AddTextLabel(ESPTab, "💡 LITE للموبايل • افتراضي 250 متر • تحديث تلقائي")

Players.PlayerAdded:Connect(function(player)
    UpdateRadarTargetList()
    player.CharacterAdded:Connect(function()
        task.wait(0.25)
    end)
end)

Players.PlayerRemoving:Connect(function(player)
    if IsTarget(player) then
        ESP_TargetName = nil
    end
    RemoveESP(player)
    UpdateRadarTargetList()
end)

UpdateRadarTargetList()

-- =============================================
-- COMBAT TAB
-- =============================================
local AutoRefreshEnabled = false
local AutoRefreshConnection = nil
local REFRESH_THRESHOLD = 50
local AutoRefreshTriggered = false
local LastRefreshTime = 0
local REFRESH_COOLDOWN = 0.15
local RefreshBusy = false

local function GetAutoRefreshHumanoid()
    local character = LP.Character
    if not character then return nil end
    return character:FindFirstChildOfClass("Humanoid")
end

local function SendRefreshCommand()
    if RefreshBusy then return false, "قيد التنفيذ" end
    RefreshBusy = true
    local ok, success, message = pcall(function()
        local hdClient = RepStorage:FindFirstChild("HDAdminHDClient")
        if not hdClient then return false, "HDAdminHDClient غير موجود" end
        local signals = hdClient:FindFirstChild("Signals")
        if not signals then return false, "Signals غير موجود" end
        local rc = signals:FindFirstChild("RequestCommand")
        if not rc or not rc:IsA("RemoteFunction") then return false, "RequestCommand غير موجود" end
        rc:InvokeServer(";refresh")
        return true, "تم ;refresh"
    end)
    RefreshBusy = false
    if not ok then return false, tostring(success) end
    return success == true, message
end

local function CheckHealthAndRefresh()
    if not AutoRefreshEnabled then return end
    local humanoid = GetAutoRefreshHumanoid()
    if not humanoid or humanoid.MaxHealth <= 0 or humanoid.Health <= 0 then
        AutoRefreshTriggered = false
        return
    end
    local healthPercent = (humanoid.Health / humanoid.MaxHealth) * 100
    if healthPercent <= REFRESH_THRESHOLD then
        if not AutoRefreshTriggered and (os.clock() - LastRefreshTime) >= REFRESH_COOLDOWN then
            AutoRefreshTriggered = true
            LastRefreshTime = os.clock()
            local success, message = SendRefreshCommand()
            MakeNotifi({
                Title = success and "Auto Refresh" or "خطأ",
                Text = success and ("تم ;refresh — الدم: " .. math.floor(healthPercent) .. "%") or tostring(message),
                Time = 2
            })
        end
    else
        AutoRefreshTriggered = false
    end
end

AddToggleButton(CombatTab, {
    Name = "رفريش تلقائي (عند الدم)", StartState = false,
    OnEnable = function()
        AutoRefreshEnabled = true
        if AutoRefreshConnection then AutoRefreshConnection:Disconnect() end
        AutoRefreshConnection = RunService.RenderStepped:Connect(CheckHealthAndRefresh)
        MakeNotifi({Title="Auto Refresh", Text="On", Time=3})
    end,
    OnDisable = function()
        AutoRefreshEnabled = false
        if AutoRefreshConnection then AutoRefreshConnection:Disconnect() AutoRefreshConnection = nil end
        MakeNotifi({Title="Auto Refresh", Text="Off", Time=2})
    end
})

AddToggleButton(CombatTab, {
    Name = "رفريش عند الموت", StartState = false,
    OnEnable = function()
        MakeNotifi({Title="Death Refresh", Text="On", Time=3})
    end,
    OnDisable = function()
        MakeNotifi({Title="Death Refresh", Text="Off", Time=2})
    end
})

local GodMode = false
local godConnection = nil

AddToggleButton(CombatTab, {
    Name = "دم حديد", StartState = false,
    OnEnable = function()
        GodMode = true
        if godConnection then godConnection:Disconnect() end
        godConnection = RunService.Heartbeat:Connect(function()
            pcall(function()
                if LP.Character and LP.Character:FindFirstChild("Humanoid") then
                    if LP.Character.Humanoid.Health < LP.Character.Humanoid.MaxHealth then
                        LP.Character.Humanoid.Health = LP.Character.Humanoid.MaxHealth
                    end
                end
            end)
        end)
        MakeNotifi({Title="قتال", Text="دم حديد On", Time=3})
    end,
    OnDisable = function()
        GodMode = false
        if godConnection then godConnection:Disconnect() godConnection = nil end
        MakeNotifi({Title="قتال", Text="دم حديد Off", Time=2})
    end
})

local ShieldMode = false
local function CreateShield()
    if LP.Character then
        local shield = LP.Character:FindFirstChild("Monetor_Shield")
        if not shield then
            shield = Instance.new("ForceField")
            shield.Name = "Monetor_Shield"
            shield.Visible = true
            shield.Parent = LP.Character
        end
    end
end

AddToggleButton(CombatTab, {
    Name = "درع قوي", StartState = false,
    OnEnable = function()
        ShieldMode = true
        CreateShield()
        MakeNotifi({Title="قتال", Text="درع On", Time=3})
    end,
    OnDisable = function()
        ShieldMode = false
        if LP.Character then
            local shield = LP.Character:FindFirstChild("Monetor_Shield")
            if shield then shield:Destroy() end
        end
        MakeNotifi({Title="قتال", Text="درع Off", Time=2})
    end
})

local CombatSpeed = false
local CombatJump = false

AddToggleButton(CombatTab, {
    Name = "سرعة قتالية", StartState = false,
    OnEnable = function()
        CombatSpeed = true
        if LP.Character and LP.Character:FindFirstChild("Humanoid") then LP.Character.Humanoid.WalkSpeed = 150 end
        MakeNotifi({Title="قتال", Text="سرعة 150", Time=3})
    end,
    OnDisable = function()
        CombatSpeed = false
        if LP.Character and LP.Character:FindFirstChild("Humanoid") then LP.Character.Humanoid.WalkSpeed = 16 end
        MakeNotifi({Title="قتال", Text="سرعة عادية", Time=2})
    end
})

AddToggleButton(CombatTab, {
    Name = "قفز خارق", StartState = false,
    OnEnable = function()
        CombatJump = true
        if LP.Character and LP.Character:FindFirstChild("Humanoid") then LP.Character.Humanoid.JumpPower = 300 end
        MakeNotifi({Title="قتال", Text="قفز خارق On", Time=3})
    end,
    OnDisable = function()
        CombatJump = false
        if LP.Character and LP.Character:FindFirstChild("Humanoid") then LP.Character.Humanoid.JumpPower = 50 end
        MakeNotifi({Title="قتال", Text="قفز عادي", Time=2})
    end
})

RunService.Stepped:Connect(function()
    pcall(function()
        if LP.Character and LP.Character:FindFirstChild("Humanoid") then
            if CombatSpeed then LP.Character.Humanoid.WalkSpeed = 150 end
            if CombatJump then LP.Character.Humanoid.JumpPower = 300 end
        end
    end)
end)

-- =============================================
-- TATWIR (Spin)
-- =============================================
local TATWIR_Active = false
local TATWIR_SpinSpeed = 200
local TATWIR_SpeedBox = nil
local TATWIR_SpinConnection = nil
local TATWIR_TouchConnection = nil
local TATWIR_BodyAngular = nil

local function TATWIR_Cleanup()
    TATWIR_Active = false
    if TATWIR_SpinConnection then TATWIR_SpinConnection:Disconnect() TATWIR_SpinConnection = nil end
    if TATWIR_TouchConnection then TATWIR_TouchConnection:Disconnect() TATWIR_TouchConnection = nil end
    if TATWIR_BodyAngular then pcall(function() TATWIR_BodyAngular:Destroy() end) TATWIR_BodyAngular = nil end
    if LP.Character then
        local myRoot = LP.Character:FindFirstChild("HumanoidRootPart")
        if myRoot then
            local oldBV = myRoot:FindFirstChild("Tatwir_Spin")
            if oldBV then pcall(function() oldBV:Destroy() end) end
        end
    end
end

local function TATWIR_Start()
    if TATWIR_Active then return end
    local char = LP.Character
    if not char then return end
    local myRoot = char:FindFirstChild("HumanoidRootPart")
    local myHum = char:FindFirstChildOfClass("Humanoid")
    if not myRoot or not myHum then return end
    TATWIR_Active = true
    
    TATWIR_BodyAngular = Instance.new("BodyAngularVelocity")
    TATWIR_BodyAngular.Name = "Tatwir_Spin"
    TATWIR_BodyAngular.MaxTorque = Vector3.new(9e9, 9e9, 9e9)
    TATWIR_BodyAngular.AngularVelocity = Vector3.new(0, math.rad(TATWIR_SpinSpeed), 0)
    TATWIR_BodyAngular.P = 9e9
    TATWIR_BodyAngular.Parent = myRoot
    
    TATWIR_TouchConnection = myRoot.Touched:Connect(function(hit)
        if not TATWIR_Active then return end
        local model = hit:FindFirstAncestorOfClass("Model")
        if not model or model == char then return end
        if not model:FindFirstChildOfClass("Humanoid") then return end
        local plr = Players:GetPlayerFromCharacter(model)
        if not plr or plr == LP then return end
        local targetRoot = model:FindFirstChild("HumanoidRootPart") or model:FindFirstChild("Torso")
        if not targetRoot then return end
        pcall(function()
            local bv = Instance.new("BodyVelocity")
            bv.Name = "Tatwir_Launch"
            bv.MaxForce = Vector3.new(9e9, 9e9, 9e9)
            bv.Velocity = Vector3.new(math.random(-50,50), 250, math.random(-50,50))
            bv.Parent = targetRoot
            task.delay(0.35, function() if bv and bv.Parent then bv:Destroy() end end)
        end)
    end)
    
    MakeNotifi({Title = "تطوير", Text = "On", Time = 3})
end

local function TATWIR_Stop()
    if not TATWIR_Active then return end
    TATWIR_Cleanup()
    MakeNotifi({Title="تطوير", Text="Off", Time=2})
end

AddToggleButton(CombatTab, {
    Name = "تطوير", StartState = false,
    OnEnable = function() TATWIR_Start() end,
    OnDisable = function() TATWIR_Stop() end
})

TATWIR_SpeedBox = Create("TextBox", CombatTab, {
    Size = UDim2.new(1, 0, 0, 30), BackgroundColor3 = Configs_HUB.Cor_Options,
    BackgroundTransparency = 0.25, BorderSizePixel = 0,
    Text = tostring(TATWIR_SpinSpeed),
    PlaceholderText = "سرعة الدوران",
    TextColor3 = Configs_HUB.Cor_Text,
    PlaceholderColor3 = Color3.fromRGB(150, 150, 150),
    TextSize = 13, Font = Configs_HUB.Text_Font,
    ClearTextOnFocus = false, TextXAlignment = Enum.TextXAlignment.Center, ZIndex = 20
})
Corner(TATWIR_SpeedBox); Stroke(TATWIR_SpeedBox)

local function TATWIR_ApplySpeed()
    local v = tonumber(TATWIR_SpeedBox.Text) or TATWIR_SpinSpeed
    v = math.clamp(math.floor(v), 0, 100000)
    TATWIR_SpinSpeed = v
    TATWIR_SpeedBox.Text = tostring(v)
    if TATWIR_BodyAngular then
        TATWIR_BodyAngular.AngularVelocity = Vector3.new(0, math.rad(v), 0)
    end
    MakeNotifi({Title="تطوير", Text="السرعة: " .. v, Time=2})
end

TATWIR_SpeedBox.FocusLost:Connect(function() TATWIR_ApplySpeed() end)
AddButton(CombatTab, { Name = "✅ تطبيق سرعة الدوران", Callback = function() TATWIR_ApplySpeed() end })

-- =============================================
-- TELEPORT TAB
-- =============================================
local TPSelectedPlayer = ""

local TPDropdown = AddDropdown(TeleportTab, {
    Name = "اختر لاعب للانتقال",
    Options = {},
    Searchable = true,
    Callback = function(value) TPSelectedPlayer = value end
})

AddButton(TeleportTab, {
    Name = "🌀 انتقال للاعب المختار",
    Callback = function()
        if TPSelectedPlayer == "" or TPSelectedPlayer == "لا يوجد لاعبين" then
            MakeNotifi({Title="تنبيه", Text="اختر لاعب", Time=2})
            return
        end
        local targetPlayer = Players:FindFirstChild(TPSelectedPlayer)
        if targetPlayer and targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart") then
            if LP.Character and LP.Character:FindFirstChild("HumanoidRootPart") then
                LP.Character.HumanoidRootPart.CFrame = targetPlayer.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, 3)
                MakeNotifi({Title="Teleport", Text="تم الانتقال", Time=2})
            end
        else
            MakeNotifi({Title="خطأ", Text="غير متوفر", Time=2})
        end
    end
})

-- =============================================
-- TROLL TAB
-- =============================================
local selectedPlayerName = ""
local playerDropdown = AddDropdown(TrollTab, {
    Name = "اختر لاعب",
    Options = {},
    Searchable = true,
    Callback = function(value) selectedPlayerName = value end
})

AddButton(TrollTab, {
    Name = "⚔️ اقتل (تتبع + إخفاء)",
    Callback = function()
        if selectedPlayerName == "" or selectedPlayerName == "لا يوجد لاعبين" then
            MakeNotifi({Title="تنبيه", Text="اختر لاعب", Time=3})
            return
        end
        pcall(function()
            local hdClient = RepStorage:FindFirstChild("HDAdminHDClient")
            if hdClient then
                local signals = hdClient:FindFirstChild("Signals")
                if signals then
                    local rc = signals:FindFirstChild("RequestCommand")
                    if rc then rc:InvokeServer(";r6") end
                end
            end
        end)
        MakeNotifi({Title="Troll", Text="التحول R6...", Time=4})
    end
})

local function UpdateTrollPlayerList()
    local names = {}
    for _, entry in ipairs(GetPlayersSortedByDistance()) do
        table.insert(names, entry.player.Name)
    end
    if #names == 0 then table.insert(names, "لا يوجد لاعبين") end
    if playerDropdown then pcall(function() playerDropdown:Update(names) end) end
    if TPDropdown then pcall(function() TPDropdown:Update(names) end) end
end

UpdateTrollPlayerList()
Players.PlayerAdded:Connect(UpdateTrollPlayerList)
Players.PlayerRemoving:Connect(UpdateTrollPlayerList)

-- =============================================
-- RIGHTS TAB
-- =============================================
AddPlayerProfile(RightsTab)

Create("Frame", RightsTab, {
    Size = UDim2.new(1, 0, 0, 2),
    BackgroundColor3 = Configs_HUB.Cor_Stroke,
    BorderSizePixel = 0,
    BackgroundTransparency = 0.5
})

local UsagePanel = nil
local function CreateUsagePanel()
    if UsagePanel and UsagePanel.Parent then
        UsagePanel.Visible = not UsagePanel.Visible
        return
    end

    UsagePanel = Create("Frame", ScreenGui, {
        Name = "Monetor_UsagePanel",
        Size = UDim2.new(0, 400, 0, 480),
        Position = UDim2.new(0.5, -200, 0.5, -240),
        BackgroundColor3 = Configs_HUB.Cor_Hub,
        BackgroundTransparency = 0.05,
        BorderSizePixel = 0, Active = true, Draggable = true, ZIndex = 200
    })
    Corner(UsagePanel); Stroke(UsagePanel)

    local Header = Create("Frame", UsagePanel, {
        Size = UDim2.new(1, 0, 0, 40),
        BackgroundTransparency = 1, ZIndex = 201
    })

    Create("TextLabel", Header, {
        Size = UDim2.new(1, -50, 1, 0),
        Position = UDim2.new(0, 15, 0, 0),
        BackgroundTransparency = 1,
        Text = "📖 طريقة الاستخدام",
        TextColor3 = Configs_HUB.Cor_Text,
        Font = Configs_HUB.Text_Font,
        TextSize = 20,
        TextXAlignment = Enum.TextXAlignment.Left,
        ZIndex = 202
    })

    local CloseBtn = Create("TextButton", Header, {
        Size = UDim2.new(0, 30, 0, 30),
        Position = UDim2.new(1, -40, 0, 5),
        BackgroundTransparency = 1,
        Text = "×",
        TextColor3 = Configs_HUB.Cor_Text,
        Font = Configs_HUB.Text_Font,
        TextSize = 24,
        AutoButtonColor = false, ZIndex = 202
    })
    CloseBtn.Activated:Connect(function() UsagePanel.Visible = false end)

    local ContentScroll = Create("ScrollingFrame", UsagePanel, {
        Size = UDim2.new(1, -20, 1, -50),
        Position = UDim2.new(0, 10, 0, 45),
        BackgroundTransparency = 1,
        CanvasSize = UDim2.new(),
        AutomaticCanvasSize = "Y",
        ScrollingDirection = "Y",
        ScrollBarThickness = 3,
        ScrollBarImageColor3 = Configs_HUB.Cor_Stroke,
        ZIndex = 201
    })
    Corner(ContentScroll)

    local UsageText = [[
🎯 الأزرار الرئيسية:

━━━━━━━━━━━━━━━━━━━━━

✈️ الطيران:
1. اضغط "تشغيل وضع طيران"
2. تحرك بالأسهم
3. اضغط "هبوط" لإيقافه

━━━━━━━━━━━━━━━━━━━━━

📡 الرادار (ESP):
1. اضغط "الرادار المطور"
2. اختر المستهدف
3. المستهدف يصير أحمر دائم

━━━━━━━━━━━━━━━━━━━━━

⚔️ القتال:
• دم حديد — صحة لا نهائية
• درع قوي — ForceField
• رفريش تلقائي — عند الدم

━━━━━━━━━━━━━━━━━━━━━

⚠️ ملاحظات:
• كل الميزات تحتاج تفعيل يدوي
• استخدم زر "فك حماية الطيران" أول شي
]]

    Create("TextLabel", ContentScroll, {
        Size = UDim2.new(1, -10, 0, 0),
        Position = UDim2.new(0, 5, 0, 5),
        BackgroundTransparency = 1,
        Text = UsageText,
        TextColor3 = Configs_HUB.Cor_Text,
        Font = Configs_HUB.Text_Font,
        TextSize = 14,
        TextXAlignment = Enum.TextXAlignment.Left,
        TextYAlignment = Enum.TextYAlignment.Top,
        TextWrapped = true,
        AutomaticSize = Enum.AutomaticSize.Y,
        ZIndex = 202
    })
end

AddButton(RightsTab, {
    Name = "📖 طريقة الاستخدام (اقرأني)",
    Callback = function()
        CreateUsagePanel()
        MakeNotifi({Title = "Monetor X", Text = "تم فتح دليل الاستخدام", Time = 2})
    end
})

local CreditsPanel = nil
local function CreateCreditsPanel()
    if CreditsPanel and CreditsPanel.Parent then
        CreditsPanel.Visible = not CreditsPanel.Visible
        return
    end

    CreditsPanel = Create("Frame", ScreenGui, {
        Name = "Monetor_CreditsPanel",
        Size = UDim2.new(0, 320, 0, 380),
        Position = UDim2.new(0.5, -160, 0.5, -190),
        BackgroundColor3 = Configs_HUB.Cor_Hub,
        BackgroundTransparency = 0.05,
        BorderSizePixel = 0, Active = true, Draggable = true, ZIndex = 200
    })
    Corner(CreditsPanel); Stroke(CreditsPanel)

    local CloseBtn = Create("TextButton", CreditsPanel, {
        Size = UDim2.new(0, 30, 0, 30),
        Position = UDim2.new(1, -35, 0, 5),
        BackgroundTransparency = 1,
        Text = "×",
        TextColor3 = Configs_HUB.Cor_Text,
        Font = Configs_HUB.Text_Font,
        TextSize = 24,
        AutoButtonColor = false, ZIndex = 205
    })
    CloseBtn.Activated:Connect(function() CreditsPanel.Visible = false end)

    Create("TextLabel", CreditsPanel, {
        Size = UDim2.new(1, 0, 0, 30),
        Position = UDim2.new(0, 0, 0, 12),
        BackgroundTransparency = 1,
        Text = "🏆 حقوق Monetor X",
        TextColor3 = Configs_HUB.Cor_Text,
        Font = Configs_HUB.Text_Font,
        TextSize = 22,
        TextXAlignment = Enum.TextXAlignment.Center, ZIndex = 202
    })

    local devAvatar = Create("ImageLabel", CreditsPanel, {
        Size = UDim2.new(0, 80, 0, 80),
        Position = UDim2.new(0.5, -40, 0, 50),
        BackgroundTransparency = 1,
        BackgroundColor3 = Configs_HUB.Cor_Options,
        Image = "rbxassetid://0", ZIndex = 202
    })
    Corner(devAvatar, { CornerRadius = UDim.new(1, 0) })
    local devStroke = Instance.new("UIStroke")
    devStroke.Thickness = 2
    devStroke.Color = Color3.fromRGB(255, 255, 255)
    devStroke.Transparency = 0.1
    devStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    devStroke.Parent = devAvatar

    task.spawn(function()
        local ok, userId = pcall(function() return Players:GetUserIdFromNameAsync("sjbcjjs0") end)
        if ok and userId and type(userId) == "number" then
            pcall(function()
                devAvatar.Image = "rbxthumb://type=AvatarHeadShot&id=" .. tostring(userId) .. "&w=150&h=150"
            end)
        end
    end)

    Create("TextLabel", CreditsPanel, {
        Size = UDim2.new(1, 0, 0, 25),
        Position = UDim2.new(0, 0, 0, 138),
        BackgroundTransparency = 1,
        Text = "@sjbcjjs0",
        TextColor3 = Color3.fromRGB(255, 255, 255),
        Font = Configs_HUB.Text_Font,
        TextSize = 18,
        TextXAlignment = Enum.TextXAlignment.Center, ZIndex = 202
    })

    Create("TextLabel", CreditsPanel, {
        Size = UDim2.new(1, 0, 0, 18),
        Position = UDim2.new(0, 0, 0, 163),
        BackgroundTransparency = 1,
        Text = "المطور الرئيسي",
        TextColor3 = Configs_HUB.Cor_DarkText,
        Font = Configs_HUB.Text_Font,
        TextSize = 12,
        TextXAlignment = Enum.TextXAlignment.Center, ZIndex = 202
    })

    local infoBox = Create("Frame", CreditsPanel, {
        Size = UDim2.new(1, -30, 0, 130),
        Position = UDim2.new(0, 15, 0, 190),
        BackgroundColor3 = Configs_HUB.Cor_Options,
        BackgroundTransparency = 0.4,
        BorderSizePixel = 0, ZIndex = 202
    })
    Corner(infoBox); Stroke(infoBox)

    local infoText = [[
📜 Script: MONETOR X
🔢 Version: 2.0 Fixed Edition
🎮 Platform: Roblox
⚙️ Type: Universal Multi-Feature

✨ Features:
 • Flight System
 • Advanced ESP
 • Combat Tools
 • Auto Refresh
 • Troll System
 • Bypass Protection
]]

    Create("TextLabel", infoBox, {
        Size = UDim2.new(1, -20, 1, -20),
        Position = UDim2.new(0, 10, 0, 10),
        BackgroundTransparency = 1,
        Text = infoText,
        TextColor3 = Configs_HUB.Cor_Text,
        Font = Configs_HUB.Text_Font,
        TextSize = 12,
        TextXAlignment = Enum.TextXAlignment.Left,
        TextYAlignment = Enum.TextYAlignment.Top,
        TextWrapped = true, ZIndex = 203
    })

    Create("TextLabel", CreditsPanel, {
        Size = UDim2.new(1, 0, 0, 18),
        Position = UDim2.new(0, 0, 1, -25),
        BackgroundTransparency = 1,
        Text = "© 2026 Monetor X — All Rights Reserved",
        TextColor3 = Configs_HUB.Cor_DarkText,
        Font = Configs_HUB.Text_Font,
        TextSize = 10,
        TextXAlignment = Enum.TextXAlignment.Center, ZIndex = 202
    })
end

AddButton(RightsTab, {
    Name = "🏆 حقوق المطور والنسخة",
    Callback = function()
        CreateCreditsPanel()
        MakeNotifi({Title = "Monetor X", Text = "حقوق @sjbcjjs0", Time = 3})
    end
})

Create("Frame", RightsTab, {
    Size = UDim2.new(1, 0, 0, 2),
    BackgroundColor3 = Configs_HUB.Cor_Stroke,
    BorderSizePixel = 0,
    BackgroundTransparency = 0.5
})

AddButton(RightsTab, {
    Name = "📢 قناة التليجرام",
    Callback = function()
        pcall(function() if setclipboard then setclipboard('https://t.me/Speed_loder') end end)
        MakeNotifi({Title = "Monetor X", Text = "تم نسخ رابط القناة", Time = 2})
    end
})

AddButton(RightsTab, {
    Name = "💬 حساب التليجرام",
    Callback = function()
        pcall(function() if setclipboard then setclipboard('@LXEKE') end end)
        MakeNotifi({Title = "Monetor X", Text = "تم نسخ الحساب", Time = 2})
    end
})

AddButton(RightsTab, {
    Name = "🎵 حساب تيك توك",
    Callback = function()
        pcall(function() if setclipboard then setclipboard('@monetor_') end end)
        MakeNotifi({Title = "Monetor X", Text = "تم نسخ الحساب", Time = 2})
    end
})

AddButton(RightsTab, {
    Name = "👤 حساب المطور",
    Callback = function()
        pcall(function() if setclipboard then setclipboard('@sjbcjjs0') end end)
        MakeNotifi({Title = "Monetor X", Text = "تم نسخ: @sjbcjjs0", Time = 2})
    end
})

AddTextLabel(RightsTab, "\nشكراً لاستخدامك MONETOR X\nصنع بـ ❤️ بواسطة @sjbcjjs0\nجميع الحقوق محفوظة © 2026")

-- =============================================
-- FINAL INIT
-- =============================================
task.wait(1.5)
pcall(FullBypass)

task.wait(0.5)
MakeNotifi({
    Title = "Monetor X",
    Text = "✅ تم تفعيل Monetor X بنجاح! استمتع",
    Time = 3
})

print("✅ Monetor X Loaded Successfully! by @sjbcjjs0 (FIXED)")